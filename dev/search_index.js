var documenterSearchIndex = {"docs":
[{"category":"section","location":"95-reference/10-graph-types/#graph-types-reference","page":"Graph & Edge Types","text":"Here we explain how to build a causal graph and briefly describe the meaning of each causal graph class. For a comprehensive introduction to these and the underlying theory, see, for instance, Pearl (2009) or Peters et al. (2017); Richardson and Spirtes (2002) covers AG/MAG specifically, and Zhang (2008) covers PAG.","title":"Graph & Edge Types"},{"category":"section","location":"95-reference/10-graph-types/#constructing-graphs","page":"Graph & Edge Types","text":"Each graph type is its own constructor: call DAG, ADMG, PAG, etc. with some edges (and, if you have isolated nodes, some nodes).","title":"Constructing graphs"},{"category":"section","location":"95-reference/10-graph-types/#String-syntax","page":"Graph & Edge Types","text":"The string form uses a compact syntax instead of composing CausalEdge values by hand. Statements are separated by commas (or newlines); each connects node names with an edge marker built from <, -, o, >:\n\nMarker Equivalent constructor\n--> directed(src, dst)\n<-- directed(dst, src)\n--- undirected(src, dst)\n<-> bidirected(src, dst)\no-> partially_directed(src, dst)\n<-o partially_directed(dst, src)\no-- partially_undirected(src, dst)\n--o partially_undirected(dst, src)\no-o partial(src, dst)\n\n+ fans a marker out to (or in from) several nodes at once, and chaining markers connects consecutive node groups pairwise, so \"A --> B --> C\" yields two edges (A-->B, B-->C) while \"A --> B + C\" yields A-->B and A-->C. A statement with no marker (e.g. \"F\") declares isolated node(s).","title":"String syntax"},{"category":"section","location":"95-reference/10-graph-types/#Graph-classes","page":"Graph & Edge Types","text":"Every graph type is a subtype of CausalGraph, and each graph class is verified on construction to be a valid graph.","title":"Graph classes"},{"category":"section","location":"95-reference/10-graph-types/#Directed-Acyclic-Graphs","page":"Graph & Edge Types","text":"A DAG (Directed Acyclic Graph) is the standard causal graph. All edges are directed (-->), and the graph contains no directed cycles.","title":"Directed Acyclic Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Partially-Directed-Acyclic-Graphs","page":"Graph & Edge Types","text":"The subtypes of AbstractPDAG all have directed (-->) and undirected (---) edges, and the graph contains no directed cycles. Undirected edges represent edges whose orientation is not specified. A PDAG is the general case; a CPDAG is the special PDAG that represents an entire Markov equivalence class of DAGs (an edge stays undirected exactly when its orientation varies across the class); an MPDAG is a PDAG in which background knowledge may specify orientations that are not determined by the underlying equivalence class.\n\nSee Equivalence Classes for worked examples of all three.","title":"Partially Directed Acyclic Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Acyclic-Directed-Mixed-Graphs","page":"Graph & Edge Types","text":"An ADMG allows directed (-->) and bidirected (<->) edges. Directed edges represent causal relations, while bidirected edges represent unobserved confounding between their endpoints. For example, an ADMG is what you obtain when you use latent_project to project latent variables out of a DAG.","title":"Acyclic Directed Mixed Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Ancestral-Graphs","page":"Graph & Edge Types","text":"AbstractAG allows for directed (-->), bidirected (<->), and undirected edges (---). In an ancestral graph, an arrowhead at a node indicates that the node is not an ancestor of the other endpoint. Thus, a bidirected edge (<->) indicates that neither endpoint is an ancestor of the other. In causal applications, such edges commonly represent unobserved confounding. Undirected edges have a different meaning here than in a PDAG: rather than representing uncertain orientation, they represent selection bias, arising from conditioning on variables that induce associations through common effects.","title":"Ancestral Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Partial-Ancestral-Graphs","page":"Graph & Edge Types","text":"A PAG plays a role for MAGs analogous to that of a CPDAG for DAGs: it represents a Markov equivalence class of MAGs (Zhang, 2008). PAG edges can have three endpoint marks: a tail, an arrowhead, or a circle. A circle indicates that the corresponding endpoint mark is not determined by the Markov equivalence class.","title":"Partial Ancestral Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Undirected-Graphs","page":"Graph & Edge Types","text":"Not commonly used in causal inference, but still present for users that want it. Only undirected (---) edges are allowed.","title":"Undirected Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Unknown-Graphs","page":"Graph & Edge Types","text":"UNKNOWN imposes no structural constraints on the graph. It accepts all supported edge types, including self-loops and parallel edges. Use it when you need to represent a graph that does not fit any of the other graph classes.","title":"Unknown Graphs"},{"category":"section","location":"95-reference/10-graph-types/#Edges","page":"Graph & Edge Types","text":"","title":"Edges"},{"category":"section","location":"95-reference/10-graph-types/#Background-knowledge","page":"Graph & Edge Types","text":"Sometimes you want to impose some knowledge into your graph, such as A must cause B, or that C definitely doesn't cause D.","title":"Background knowledge"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.node","page":"Graph & Edge Types","text":"node(name::Symbol) -> GraphNode\n\nWrap a symbol as an isolated node for inclusion in a graph constructor such as DAG.\n\nExamples\n\njulia> DAG(node(:A), node(:B), node(:C))\nDAG with 3 nodes and 0 edges:\n  nodes: A, B, C\n  edges:\n    (none)\n\n\n\n\n\n","title":"CausalStructures.node"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.CausalGraph","page":"Graph & Edge Types","text":"CausalGraph\n\nAbstract supertype for all causal graph classes. Concrete subtypes: DAG, UG, AbstractPDAG, ADMG, AbstractAG, PAG, and UNKNOWN.\n\n\n\n\n\n","title":"CausalStructures.CausalGraph"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.DAG","page":"Graph & Edge Types","text":"DAG(items...) -> DAG\nDAG(s::AbstractString) -> DAG\n\nA Directed Acyclic Graph. Directed edges only, and no directed cycles allowed.\n\nExamples\n\njulia> dag = DAG(directed(:A, :B), directed(:B, :C))\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\njulia> dag_iso = DAG(directed(:A, :B), node(:C))\nDAG with 3 nodes and 1 edge:\n  nodes: A, B, C\n  edges:\n    A --> B\n\njulia> DAG(\"A --> B --> C\")\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\n\n\n\n\n","title":"CausalStructures.DAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.AbstractPDAG","page":"Graph & Edge Types","text":"AbstractPDAG <: CausalGraph\n\nAbstract supertype for partially directed acyclic graphs. Concrete subtypes: PDAG, CPDAG, and MPDAG.\n\n\n\n\n\n","title":"CausalStructures.AbstractPDAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.PDAG","page":"Graph & Edge Types","text":"PDAG(items...) -> PDAG\nPDAG(s::AbstractString) -> PDAG\n\nA Partially Directed Acyclic Graph. Directed and undirected edges only, and no directed cycles allowed.\n\nExamples\n\njulia> PDAG(directed(:A, :B), undirected(:B, :C))\nPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --- C\n\njulia> PDAG(\"A --> B --- C\")\nPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --- C\n\n\n\n\n\n","title":"CausalStructures.PDAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.CPDAG","page":"Graph & Edge Types","text":"CPDAG(items...) -> CPDAG\nCPDAG(s::AbstractString) -> CPDAG\n\nA Completed Partially Directed Acyclic Graph. The unique graph representing a Markov equivalence class (MEC) of DAGs. Directed edges represent compelled orientations shared by all DAGs in the class. Undirected edges represent adjacencies whose orientation differs across DAGs in the class. Consequently, every edge is directed exactly when its orientation is invariant within the MEC.\n\nExamples\n\njulia> CPDAG(\"A --- B --- C\")\nCPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\nReferences\n\nChickering (2002)\n\n\n\n\n\n","title":"CausalStructures.CPDAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.MPDAG","page":"Graph & Edge Types","text":"MPDAG(items...) -> MPDAG\nMPDAG(s::AbstractString) -> MPDAG\n\nA Maximally Partially Directed Acyclic Graph. A PDAG that is closed under Meek's orientation rules R1-R4: no further edge orientation can be implied. MPDAGs arise when background knowledge (forced edge orientations) is present.\n\nExamples\n\njulia> MPDAG(\"A --> B --> C\")\nMPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\nReferences\n\nMeek (1995)\n\n\n\n\n\n","title":"CausalStructures.MPDAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.ADMG","page":"Graph & Edge Types","text":"ADMG(items...) -> ADMG\nADMG(s::AbstractString) -> ADMG\n\nAn Acyclic Directed Mixed Graph. Directed and bidirected edges only, and no directed cycles allowed.\n\nExamples\n\njulia> ADMG(\"X --> Y, X <-> Y\")\nADMG with 2 nodes and 2 edges:\n  nodes: X, Y\n  edges:\n    X --> Y, X <-> Y\n\nReferences\n\nRichardson (2003)\n\n\n\n\n\n","title":"CausalStructures.ADMG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.AbstractAG","page":"Graph & Edge Types","text":"AbstractAG <: CausalGraph\n\nAbstract supertype for ancestral graphs. Concrete subtypes: AG and MAG.\n\n\n\n\n\n","title":"CausalStructures.AbstractAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.AG","page":"Graph & Edge Types","text":"AG(items...) -> AG\nAG(s::AbstractString) -> AG\n\nAn Ancestral Graph. Directed, undirected, and bidirected edges only. It contains no directed cycles, and if X <-> Y then neither X is an ancestor of Y nor Y of X. Additionally, nodes incident to an undirected edge have no arrowheads pointing at them on any adjacent edge (i.e., no parents or spouses).\n\nExamples\n\nEvery MAG is an AG, but not every AG is a MAG: below, A and B are non-adjacent but no subset of {C, D} m-separates them, so this AG is not maximal.\n\njulia> ag = AG(\"C <-> A <-> B <-> D, A --> D, B --> C\")\nAG with 4 nodes and 5 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> C, A <-> B, B <-> D, A --> D, B --> C\n\njulia> is_mag(ag)\nfalse\n\nReferences\n\nRichardson and Spirtes (2002)\n\n\n\n\n\n","title":"CausalStructures.AG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.MAG","page":"Graph & Edge Types","text":"MAG(items...) -> MAG\nMAG(s::AbstractString) -> MAG\n\nA Maximal Ancestral Graph. An AG in which every pair of non-adjacent nodes is m-separated by some subset of the remaining nodes. MAGs are the canonical representatives of equivalence classes of DAGs with hidden variables.\n\nExamples\n\njulia> MAG(\"A <-> B, C --> B --> D\")\nMAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> B, C --> B, B --> D\n\nReferences\n\nRichardson and Spirtes (2002)\n\n\n\n\n\n","title":"CausalStructures.MAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.PAG","page":"Graph & Edge Types","text":"PAG(items...) -> PAG\nPAG(s::AbstractString) -> PAG\n\nA Partial Ancestral Graph. The graph representing a Markov equivalence class of MAGs (and thus of DAGs with latent confounders and selection bias). It has the same skeleton as every MAG in the class, and each endpoint carries an invariant mark shared by every MAG in the class: an arrowhead (>), a tail (-), or a circle (o) where the mark varies across the class. All six edge kinds are allowed (-->, ---, <->, o->, o--, o-o).\n\nValidation is checked by doing a round-trip conversion to a MAG and back, and verifying that the original PAG is recovered.\n\nExamples\n\njulia> PAG(\"A o-> B <-o C\")\nPAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A o-> B, C o-> B\n\nReferences\n\nZhang (2008)\n\n\n\n\n\n","title":"CausalStructures.PAG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.UG","page":"Graph & Edge Types","text":"UG(items...) -> UG\nUG(s::AbstractString) -> UG\n\nAn Undirected Graph. Undirected edges only.\n\nExamples\n\njulia> UG(undirected(:A, :B), undirected(:B, :C))\nUG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\njulia> UG(\"A --- B --- C\")\nUG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\n\n\n\n\n","title":"CausalStructures.UG"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.UNKNOWN","page":"Graph & Edge Types","text":"UNKNOWN(items...) -> UNKNOWN\nUNKNOWN(s::AbstractString) -> UNKNOWN\n\nA graph with no structural constraints enforced. Accepts all edge types, including self-loops and multiple edges between the same pair of nodes. Intended as a fallback for graph classes not yet natively supported.\n\nExamples\n\njulia> UNKNOWN(\"A --> B + C, D o-> E\")\nUNKNOWN with 5 nodes and 3 edges:\n  nodes: A, B, C, D, E\n  edges:\n    A --> B, A --> C, D o-> E\n\n\n\n\n\n","title":"CausalStructures.UNKNOWN"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.CausalEdge","page":"Graph & Edge Types","text":"CausalEdge\n\nAn edge between two nodes, specified by source (src), destination (dst), and endpoint marks at each end (src_end, dst_end). Use the edge constructor functions (directed, undirected, bidirected, etc.) rather than constructing CausalEdge directly.\n\nAn edge whose two endpoint marks are identical (---, <->, o-o) means the same thing either way round, so its endpoints are stored in a canonical order: src is whichever node name sorts first.\n\n\n\n\n\n","title":"CausalStructures.CausalEdge"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.directed","page":"Graph & Edge Types","text":"directed(src, dst) -> CausalEdge   # src --> dst\n\nExamples\n\njulia> DAG(directed(:A, :B), directed(:B, :C))\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\n\n\n\n\n","title":"CausalStructures.directed"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.undirected","page":"Graph & Edge Types","text":"undirected(src, dst) -> CausalEdge   # src --- dst\n\nExamples\n\njulia> UG(undirected(:A, :B), undirected(:B, :C))\nUG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\n\n\n\n\n","title":"CausalStructures.undirected"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.bidirected","page":"Graph & Edge Types","text":"bidirected(src, dst) -> CausalEdge   # src <-> dst\n\nExamples\n\njulia> ADMG(bidirected(:A, :B), bidirected(:B, :C))\nADMG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A <-> B, B <-> C\n\n\n\n\n\n","title":"CausalStructures.bidirected"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.partially_directed","page":"Graph & Edge Types","text":"partially_directed(src, dst) -> CausalEdge   # src o-> dst\n\nExamples\n\njulia> UNKNOWN(partially_directed(:A, :B), partially_directed(:B, :C))\nUNKNOWN with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A o-> B, B o-> C\n\n\n\n\n\n","title":"CausalStructures.partially_directed"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.partially_undirected","page":"Graph & Edge Types","text":"partially_undirected(src, dst) -> CausalEdge   # src o-- dst\n\nExamples\n\njulia> UNKNOWN(partially_undirected(:A, :B), partially_undirected(:B, :C))\nUNKNOWN with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A o-- B, B o-- C\n\n\n\n\n\n","title":"CausalStructures.partially_undirected"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.partial","page":"Graph & Edge Types","text":"partial(src, dst) -> CausalEdge   # src o-o dst\n\nExamples\n\njulia> UNKNOWN(partial(:A, :B), partial(:B, :C))\nUNKNOWN with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A o-o B, B o-o C\n\n\n\n\n\n","title":"CausalStructures.partial"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.RequiredEdge","page":"Graph & Edge Types","text":"RequiredEdge\n\nA background-knowledge constraint stating that the directed edge src --> dst must be present, displayed as src --> dst. It is not a graph edge (unlike the CausalEdge from directed): it only carries meaning inside BackgroundKnowledge and is rejected by graph constructors such as DAG. Use required_directed to construct one.\n\n\n\n\n\n","title":"CausalStructures.RequiredEdge"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.ForbiddenEdge","page":"Graph & Edge Types","text":"ForbiddenEdge\n\nA background-knowledge constraint stating that the directed edge src --> dst must not be present, displayed as src !--> dst. It is not a graph edge: it only carries meaning inside BackgroundKnowledge and is rejected by graph constructors such as DAG. Use forbidden_directed to construct one.\n\n\n\n\n\n","title":"CausalStructures.ForbiddenEdge"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.required_directed","page":"Graph & Edge Types","text":"required_directed(src, dst) -> RequiredEdge   # src --> dst\n\nDeclare the directed edge src --> dst as required background knowledge, for use in BackgroundKnowledge.\n\nExamples\n\njulia> required_directed(:A, :B)\nA --> B\n\n\n\n\n\n","title":"CausalStructures.required_directed"},{"category":"function","location":"95-reference/10-graph-types/#CausalStructures.forbidden_directed","page":"Graph & Edge Types","text":"forbidden_directed(src, dst) -> ForbiddenEdge   # src !--> dst\n\nDeclare the directed edge src --> dst as forbidden background knowledge, for use in BackgroundKnowledge.\n\nExamples\n\njulia> forbidden_directed(:C, :D)\nC !--> D\n\n\n\n\n\n","title":"CausalStructures.forbidden_directed"},{"category":"type","location":"95-reference/10-graph-types/#CausalStructures.BackgroundKnowledge","page":"Graph & Edge Types","text":"BackgroundKnowledge(items...) -> BackgroundKnowledge\nBackgroundKnowledge(s::AbstractString) -> BackgroundKnowledge\n\nCausal background knowledge: a set of required direct causes (A --> B must be present) and forbidden direct causes (C --> D must be absent).\n\nitems may be any combination of RequiredEdge values from required_directed and ForbiddenEdge values from forbidden_directed. The string form uses the DAG string syntax restricted to the markers -->, <--, !-->, and !<--.\n\nLocally contradictory knowledge (the same edge both required and forbidden, or both directions required) raises an error at construction.\n\nApply to a graph with apply_background_knowledge or dag_to_mpdag.\n\nExamples\n\njulia> BackgroundKnowledge(required_directed(:A, :B), forbidden_directed(:C, :D))\nBackgroundKnowledge with 1 required and 1 forbidden:\n  required: A --> B\n  forbidden: C !--> D\n\njulia> BackgroundKnowledge(\"A --> B, C !--> D\")\nBackgroundKnowledge with 1 required and 1 forbidden:\n  required: A --> B\n  forbidden: C !--> D\n\nReferences\n\nMeek (1995)\n\n\n\n\n\n","title":"CausalStructures.BackgroundKnowledge"},{"category":"section","location":"95-reference/40-adjustment/#identification-reference","page":"Causal Identification","text":"See the Causal Identification guide for worked examples.","title":"Causal Identification"},{"category":"section","location":"95-reference/40-adjustment/#Adjustment","page":"Causal Identification","text":"","title":"Adjustment"},{"category":"section","location":"95-reference/40-adjustment/#Backdoor","page":"Causal Identification","text":"","title":"Backdoor"},{"category":"section","location":"95-reference/40-adjustment/#Frontdoor","page":"Causal Identification","text":"","title":"Frontdoor"},{"category":"section","location":"95-reference/40-adjustment/#Instrumental-variables","page":"Causal Identification","text":"","title":"Instrumental variables"},{"category":"section","location":"95-reference/40-adjustment/#Identification","page":"Causal Identification","text":"","title":"Identification"},{"category":"section","location":"95-reference/40-adjustment/#Estimands","page":"Causal Identification","text":"","title":"Estimands"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.adjustment_set","page":"Causal Identification","text":"adjustment_set(cg::ADMG, x, y) -> Union{Nothing,Vector{Symbol}}\n\nReturn a valid inclusion-minimal adjustment set for the causal effect of x on y in cg, or nothing if none exists.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nUnlike the DAG/AbstractPDAG methods of adjustment_set, this method takes no type keyword.\n\nExamples\n\njulia> admg = ADMG(\"L --> X --> Y, L --> Y\");\n\njulia> adjustment_set(admg, :X, :Y)\n1-element Vector{Symbol}:\n :L\n\njulia> admg2 = ADMG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> sort(adjustment_set(admg2, [:X1, :X2], :Y))\n2-element Vector{Symbol}:\n :L1\n :L2\n\njulia> admg3 = ADMG(directed(:X, :Y), bidirected(:X, :Y));  # direct edge plus latent confounder\n\njulia> adjustment_set(admg3, :X, :Y) === nothing\ntrue\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nadjustment_set(cg::AbstractAG, x, y) -> Union{Nothing,Vector{Symbol}}\n\nReturn a inclusion-minimal valid adjustment set for the causal effect of x on y in cg, or nothing if none exists.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nUnlike the DAG/AbstractPDAG methods of adjustment_set, this method takes no type keyword.\n\nExamples\n\njulia> mag = MAG(\"A <-> X, A --> M --> Y, X --> Y\");\n\njulia> adjustment_set(mag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> mag2 = MAG(\"A <-> X1, B <-> X2, A --> M1 --> Y, B --> M2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> sort(adjustment_set(mag2, [:X1, :X2], :Y))\n2-element Vector{Symbol}:\n :A\n :B\n\njulia> mag3 = MAG(directed(:X, :Y));  # invisible edge: no witness rules out a latent confounder\n\njulia> adjustment_set(mag3, :X, :Y) === nothing\ntrue\n\nReferences\n\nPerković et al. (2018)\nvan der Zander and Liśkiewicz (2020)\n\n\n\n\n\nadjustment_set(cg::PAG, x, y) -> Union{Nothing,Vector{Symbol}}\n\nReturn a inclusion-minimalvalid adjustment set for the causal effect of x on y in cg, or nothing if none exists.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nUnlike the DAG/AbstractPDAG methods of adjustment_set, this method takes no type keyword.\n\nExamples\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> adjustment_set(pag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> mag2 = MAG(\n           \"B1 --> X1, A1 <-> X1, A1 --> Y, X1 --> Y,\n           B2 --> X2, A2 <-> X2, A2 --> Y, X2 --> Y\");\n\njulia> pag2 = mag_to_pag(mag2);\n\njulia> sort(adjustment_set(pag2, [:X1, :X2], :Y))\n2-element Vector{Symbol}:\n :A1\n :A2\n\njulia> pag3 = mag_to_pag(MAG(directed(:A, :X), directed(:X, :Y), directed(:A, :Y)));\n\njulia> adjustment_set(pag3, :X, :Y) === nothing  # not adjustment-amenable\ntrue\n\nReferences\n\nPerković et al. (2018)\nvan der Zander and Liśkiewicz (2020)\n\n\n\n\n\nadjustment_set(cg::DAG, x, y; type::Symbol = :optimal) -> Union{Nothing,Vector{Symbol}}\n\nCompute an adjustment set for the causal effect of x on y in cg, or nothing if no valid adjustment set exists.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nThree types are supported:\n\n:parents: bigcup mathrmPa(x) setminus x y.\n:backdoor: Pearl backdoor formula.\n:optimal: O-set mathrmPa(mathrmcn(xy)) setminus mathrmForb(xy), where mathrmcn(xy) is the set of nodes other than x on proper causal paths from x to y (paths that meet x only at their first node) and mathrmForb(xy) = mathrmDe(mathrmcn(xy)) cup x.\n\nThe O-set (Henckel et al., 2022) is defined when every node in y is a descendant of x. It is then a valid adjustment set whenever any valid adjustment set exists, and it is asymptotically optimal among them. If some node in y is not a descendant of x (so x has no causal effect on it), :optimal emits a warning: its result is then not guaranteed to be optimal, and it may return nothing even though a valid adjustment set exists (e.g. x = :X, y = :Y in A --> X, A --> Y); use :backdoor or all_adjustment_sets instead.\n\nThe type keyword is specific to the DAG/AbstractPDAG methods; the ADMG/AbstractAG/PAG methods of adjustment_set take no type keyword and always return a fixed inclusion-minimal valid adjustment set.\n\nExamples\n\njulia> dag = DAG(\n           \"C --> X, X --> F, X --> D --> Y, A --> X,\n           A --> K --> Y, D --> G, Y --> H\");\n\njulia> sort(adjustment_set(dag, :X, :Y; type = :parents))\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> adjustment_set(dag, :X, :Y; type = :backdoor)\n1-element Vector{Symbol}:\n :A\n\njulia> adjustment_set(dag, :X, :Y; type = :optimal)\n1-element Vector{Symbol}:\n :K\n\njulia> dag2 = DAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> sort(adjustment_set(dag2, [:X1, :X2], :Y; type = :optimal))\n2-element Vector{Symbol}:\n :L1\n :L2\n\nReferences\n\nHenckel et al. (2022)\nPearl (2009)\n\n\n\n\n\nadjustment_set(cg::AbstractPDAG, x, y; type::Symbol = :optimal)\n    -> Union{Nothing,Vector{Symbol}}\n\nCompute an adjustment set for the causal effect of x on y in cg, or nothing if no valid adjustment set exists.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nTwo types are supported:\n\n:parents: directed parents of x.\n:optimal: O-set mathrmPa(mathrmCn(xy)) setminus mathrmForb(xy), where mathrmCn(xy) is the set of nodes on proper possibly directed paths from x to y and mathrmForb(xy) is the forbidden set (see is_valid_adjustment).\n\nThe O-set (Henckel et al., 2022) is defined when every node in y lies on a proper possibly causal path from x. If additionally the effect is amenable (see is_valid_adjustment), it is a valid adjustment set whenever any valid adjustment set exists, and it is asymptotically optimal among them in every DAG that cg represents. If some node in y lies on no such path (so x has no causal effect on it in any represented DAG), :optimal emits a warning: its result is then not guaranteed to be optimal, and it may return nothing even though a valid adjustment set exists; use all_adjustment_sets instead.\n\nThe type keyword is specific to the DAG/AbstractPDAG methods; the ADMG/AbstractAG/PAG methods of adjustment_set take no type keyword and always return a fixed inclusion-minimal valid adjustment set.\n\nExamples\n\njulia> pdag = PDAG(\"A --> X --> Y, A --> Y\");\n\njulia> adjustment_set(pdag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> is_valid_adjustment(pdag, :X, :Y, :A)\ntrue\n\njulia> pdag2 = PDAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> sort(adjustment_set(pdag2, [:X1, :X2], :Y))\n2-element Vector{Symbol}:\n :L1\n :L2\n\nReferences\n\nHenckel et al. (2022)\n\n\n\n\n\n","title":"CausalStructures.adjustment_set"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.is_valid_adjustment","page":"Causal Identification","text":"is_valid_adjustment(cg::DAG, x, y, z = Symbol[]) -> Bool\n\nReturn true if z is a valid adjustment set for estimating the total causal effect of x on y in cg using the Generalized Adjustment Criterion (GAC).\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nFor a DAG this reduces to the adjustment criterion of Shpitser (2012), which is sound and complete for adjustment – unlike is_valid_backdoor (Pearl's backdoor criterion, sound but not complete), z may include descendants of x as long as they are not on a causal path from x to y, so this criterion accepts some valid sets the backdoor criterion rejects.\n\nExamples\n\njulia> dag = DAG(\"A --> X --> Y, A --> Y\");\n\njulia> is_valid_adjustment(dag, :X, :Y)\nfalse\n\njulia> is_valid_adjustment(dag, :X, :Y, :A)\ntrue\n\njulia> dag2 = DAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> is_valid_adjustment(dag2, [:X1, :X2], :Y, [:L1, :L2])\ntrue\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nis_valid_adjustment(cg::Union{ADMG,AbstractAG}, x, y, z = Symbol[]) -> Bool\n\nReturn true if z is a valid adjustment set for estimating the total causal effect of x on y in cg using the Generalized Adjustment Criterion (GAC).\n\nx, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nA set z is valid if it contains no forbidden node (no node in De(cn(x,y) \\ {y}) ∪ {x}, where cn(x,y) are the proper causal nodes from x to y) and x and y are m-separated by z in the proper backdoor graph of cg.\n\nExamples\n\njulia> admg = ADMG(\"L --> X --> Y, L --> Y\");\n\njulia> is_valid_adjustment(admg, :X, :Y)       # empty Z does not block L --> Y\nfalse\n\njulia> is_valid_adjustment(admg, :X, :Y, :L) # conditioning on L blocks the backdoor path\ntrue\n\njulia> mag = MAG(\"A <-> X, A --> M --> Y, X --> Y\");\n\njulia> is_valid_adjustment(mag, :X, :Y)\nfalse\n\njulia> is_valid_adjustment(mag, :X, :Y, :A)\ntrue\n\njulia> admg2 = ADMG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> is_valid_adjustment(admg2, [:X1, :X2], :Y, [:L1, :L2])\ntrue\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nis_valid_adjustment(cg::PAG, x, y, z = Symbol[]) -> Bool\n\nReturn true if z is a valid adjustment set for estimating the total causal effect of x on y in cg using the Generalized Adjustment Criterion (GAC).\n\nx, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nCircle marks collapse to tails for reachability purposes, and edges out of x are only removed from the proper backdoor graph if they are visible: there must be a witness node with an arrowhead into x (or reaching x via a collider path through parents of the edge's target) that is not adjacent to that target, ruling out a latent confounder riding along the edge (Zhang, 2006). If cg is not adjustment-amenable relative to (x, y) – some proper possibly-directed path from x to y starts with an invisible edge – no set satisfies the criterion, including the empty set.\n\nExamples\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> is_valid_adjustment(pag, :X, :Y)\nfalse\n\njulia> is_valid_adjustment(pag, :X, :Y, :A)\ntrue\n\njulia> mag2 = MAG(\n           \"B1 --> X1, A1 <-> X1, A1 --> Y, X1 --> Y,\n           B2 --> X2, A2 <-> X2, A2 --> Y, X2 --> Y\");\n\njulia> pag2 = mag_to_pag(mag2);\n\njulia> is_valid_adjustment(pag2, [:X1, :X2], :Y, [:A1, :A2])\ntrue\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nis_valid_adjustment(cg::AbstractPDAG, x, y, z = Symbol[]) -> Bool\n\nReturn true if z is a valid adjustment set for estimating the total causal effect of x on y in cg using the Generalized Adjustment Criterion (GAC).\n\nx, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nThe effect must be amenable (every proper possibly causal path from x to y starts with a directed edge), z must avoid the forbidden set (possible descendants of nodes on proper possibly causal paths), and z must block every proper non-causal path of definite status. A z overlapping y is never valid.\n\nA PDAG is first closed under Meek's rules (see meek_closure), since the criterion is stated for MPDAGs.\n\nExamples\n\njulia> pdag = PDAG(\"A --> X --> Y, A --> Y\");\n\njulia> is_valid_adjustment(pdag, :X, :Y)\nfalse\n\njulia> is_valid_adjustment(pdag, :X, :Y, :A)\ntrue\n\njulia> pdag2 = PDAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> is_valid_adjustment(pdag2, [:X1, :X2], :Y, [:L1, :L2])\ntrue\n\nReferences\n\nPerković et al. (2018)\nPerković et al. (2017)\n\n\n\n\n\n","title":"CausalStructures.is_valid_adjustment"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.all_adjustment_sets","page":"Causal Identification","text":"all_adjustment_sets(cg::DAG, x, y;\n                    minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all valid adjustment sets for the total causal effect of x on y in cg, up to size max_size.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nSets are validated using is_valid_adjustment. When minimal = true (default), only inclusion-minimal sets are returned.\n\nExamples\n\njulia> dag = DAG(\"A --> X, B --> X, X --> Y, A --> Y\");\n\njulia> all_adjustment_sets(dag, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:A]\n\njulia> all_adjustment_sets(dag, :X, :Y; minimal=false)\n2-element Vector{Vector{Symbol}}:\n [:A]\n [:A, :B]\n\njulia> dag2 = DAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> all_adjustment_sets(dag2, [:X1, :X2], :Y)\n1-element Vector{Vector{Symbol}}:\n [:L1, :L2]\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nall_adjustment_sets(cg::Union{ADMG,AbstractAG,AbstractPDAG}, x, y;\n                    minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all valid adjustment sets for the total causal effect of x on y in cg, up to size max_size.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nBruteforces over subsets of the allowed universe of nodes (nodes that are not forbidden and not y), checking each for validity using is_valid_adjustment. When minimal = true (default), only inclusion-minimal sets are returned.\n\nExamples\n\njulia> admg = ADMG(\"L --> X --> Y, L --> Y\");\n\njulia> all_adjustment_sets(admg, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:L]\n\njulia> mag = MAG(\"A <-> X, A --> M --> Y, X --> Y\");\n\njulia> all_adjustment_sets(mag, :X, :Y)\n2-element Vector{Vector{Symbol}}:\n [:A]\n [:M]\n\njulia> mpdag = MPDAG(\"A --> X, B --> X, X --> Y, A --> Y, B --- K, K --> Y\");\n\njulia> all_adjustment_sets(mpdag, :X, :Y)\n2-element Vector{Vector{Symbol}}:\n [:A, :B]\n [:A, :K]\n\njulia> all_adjustment_sets(mpdag, :X, :Y, minimal = false)\n3-element Vector{Vector{Symbol}}:\n [:A, :B]\n [:A, :K]\n [:A, :B, :K]\n\njulia> admg2 = ADMG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> all_adjustment_sets(admg2, [:X1, :X2], :Y)\n1-element Vector{Vector{Symbol}}:\n [:L1, :L2]\n\n\n\n\n\nall_adjustment_sets(cg::PAG, x, y;\n                    minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all valid adjustment sets for the total causal effect of x on y in cg, up to size max_size.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nSets are validated using is_valid_adjustment. When minimal = true (default), only inclusion-minimal sets are returned.\n\nExamples\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> all_adjustment_sets(pag, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:A]\n\njulia> mag2 = MAG(\n           \"B1 --> X1, A1 <-> X1, A1 --> Y, X1 --> Y,\n           B2 --> X2, A2 <-> X2, A2 --> Y, X2 --> Y\");\n\njulia> pag2 = mag_to_pag(mag2);\n\njulia> all_adjustment_sets(pag2, [:X1, :X2], :Y)\n1-element Vector{Vector{Symbol}}:\n [:A1, :A2]\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\nall_adjustment_sets(cg::AbstractPDAG, x, y;\n                    minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all valid adjustment sets for the total causal effect of x on y in cg, up to size max_size.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nSets are validated using is_valid_adjustment. When minimal = true (default), only inclusion-minimal sets are returned.\n\nExamples\n\njulia> mpdag = MPDAG(\"A --> X, B --> X, X --> Y, A --> Y, B --- K, K --> Y\");\n\njulia> all_adjustment_sets(mpdag, :X, :Y)\n2-element Vector{Vector{Symbol}}:\n [:A, :B]\n [:A, :K]\n\njulia> all_adjustment_sets(mpdag, :X, :Y, minimal = false)\n3-element Vector{Vector{Symbol}}:\n [:A, :B]\n [:A, :K]\n [:A, :B, :K]\n\njulia> pdag2 = PDAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> all_adjustment_sets(pdag2, [:X1, :X2], :Y)\n1-element Vector{Vector{Symbol}}:\n [:L1, :L2]\n\nReferences\n\nPerković et al. (2018)\nPerković et al. (2017)\n\n\n\n\n\n","title":"CausalStructures.all_adjustment_sets"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.possible_optimal_adjustment_sets","page":"Causal Identification","text":"possible_optimal_adjustment_sets(cg::AbstractPDAG, x::Symbol, y::Symbol) ->\n    Vector{Union{Vector{Symbol},Nothing}}\n\nReturn the optimal adjustment set (Henckel, Perkovic & Maathuis 2022) for each locally valid orientation of x's undirected neighbors in cg. The graph part of optimal-adjustment IDA (Witte, Henckel, Maathuis & Didelez 2020).\n\nFor each subset S of x's undirected neighbors that possible_parent_sets would accept (no new v-structure with collider x), this directs S --> x and x's remaining undirected neighbors away from x, closes the result under Meek's rules (apply_background_knowledge), and returns adjustment_set(mpdag, x, y; type = :optimal) for the resulting MPDAG. Entries are in the same order (one per locally valid orientation) as possible_parent_sets(cg, x), so pairing the two element-wise recovers, for each orientation, both its parent set and its optimal adjustment set.\n\nAn entry is nothing when y is among the resulting parents of x for that orientation, so no adjustment set applies, or when y is not a possible descendant of x in the resulting MPDAG. In the latter case the O-set is undefined and adjustment_set warns, but a valid adjustment set may still exist.\n\nExamples\n\njulia> cpdag = CPDAG(\"X1 --- X2 + X3 + X4, X3 + X4 --> Y\");\n\njulia> possible_parent_sets(cpdag, :X1)\n4-element Vector{Vector{Symbol}}:\n []\n [:X2]\n [:X3]\n [:X4]\n\njulia> possible_optimal_adjustment_sets(cpdag, :X1, :Y)\n4-element Vector{Union{Nothing, Vector{Symbol}}}:\n Symbol[]\n Symbol[]\n [:X3]\n [:X4]\n\nReferences\n\nMaathuis et al. (2009)\nHenckel et al. (2022)\nWitte et al. (2020)\n\n\n\n\n\n","title":"CausalStructures.possible_optimal_adjustment_sets"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.pagcauses","page":"Causal Identification","text":"pagcauses(cg::PAG, x::Symbol, y::Symbol) -> Vector{Vector{Symbol}}\n\nReturn adjustment sets for the effect of x on y that together give every possible causal effect identifiable by adjustment in some DAG consistent with cg (Wang et al. 2025, Algorithm 1, \"PAGcauses\"; Theorem 4).\n\nReturns Vector{Symbol}[] if x is not a possible ancestor of y. If the effect is directly identifiable in cg, returns the corresponding backdoor_set.\n\nThrows ArgumentError if cg contains selection bias (undirected edges), which is not covered by the algorithm.\n\nAlgorithm\n\nRather than enumerating the O(3^(d^2-d)2) MAGs consistent with a PAG and checking D-SEP in each, the algorithm performs a graphical check for each of the O(2^d) candidate sets. For each possible local structure at x (possible_local_structures), it constructs the corresponding maximal local MAG (maximal_local_mag) and searches for adjustment sets satisfying Theorem 2, pruned by DD-SEP (Definition 7). The overall complexity is O(5^d d^6) (Sec. 3.4).\n\nParallelizes over Threads.nthreads() when there are enough candidates.\n\nExamples\n\njulia> pag = PAG(\"X o-> Y, X o-> C, X o-> A, Y o-o C, C o-o A, B o-> C, B o-> Y, B o-> A\");\n\njulia> sort(sort.(pagcauses(pag, :X, :Y)))\n5-element Vector{Vector{Symbol}}:\n []\n [:A, :B]\n [:A, :B, :C]\n [:B, :C]\n [:C]\n\nReferences\n\nWang et al. (2025)\n\n\n\n\n\n","title":"CausalStructures.pagcauses"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.is_valid_backdoor","page":"Causal Identification","text":"is_valid_backdoor(cg::Union{DAG,ADMG}, x, y, z = Symbol[]) -> Bool\n\nReturn true if z satisfies the backdoor criterion for the causal effect of x on y in cg.\n\nx, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nz is a valid backdoor set if (1) no node in z is a descendant of x, and (2) z blocks every backdoor path from x to y.\n\nDAG: equivalently, every parent of x is d-separated from y given z ∪ x.\nADMG: equivalently, z m-separates x from y in the graph obtained by removing every directed edge out of x.\n\nExamples\n\njulia> dag = DAG(\"A --> X --> Y, A --> Y\");\n\njulia> is_valid_backdoor(dag, :X, :Y)       # empty Z leaves the backdoor path A --> Y open\nfalse\n\njulia> is_valid_backdoor(dag, :X, :Y, :A) # conditioning on A blocks the backdoor path\ntrue\n\njulia> admg = ADMG(\"A --> X --> Y, A <-> Y\");\n\njulia> is_valid_backdoor(admg, :X, :Y)       # A <-> Y is an unobserved confounder of the path\nfalse\n\njulia> is_valid_backdoor(admg, :X, :Y, :A) # conditioning on A still blocks it\ntrue\n\njulia> dag2 = DAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> is_valid_backdoor(dag2, [:X1, :X2], :Y)              # both confounding paths are open\nfalse\n\njulia> is_valid_backdoor(dag2, [:X1, :X2], :Y, [:L1, :L2])  # conditioning on both blocks them\ntrue\n\nReferences\n\nPearl (2009)\n\n\n\n\n\n","title":"CausalStructures.is_valid_backdoor"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.all_backdoor_sets","page":"Causal Identification","text":"all_backdoor_sets(cg::Union{DAG,ADMG}, x, y;\n                  minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all sets satisfying the backdoor criterion for the causal effect of x on y in cg, up to size max_size.\n\nx and y may each be a single Symbol or an AbstractVector{Symbol}.\n\nBruteforces over subsets of the allowed universe of nodes (nodes that are not descendants of x and not y), checking each for validity using is_valid_backdoor. When minimal = true (default), only inclusion-minimal sets are returned. For ADMG, the universe is drawn from the graph's observed nodes; latent confounders are already summarized by bidirected edges and are never candidates.\n\nExamples\n\njulia> dag = DAG(\"A --> X --> Y, A --> Y\");\n\njulia> all_backdoor_sets(dag, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:A]\n\njulia> admg = ADMG(\"A --> X --> Y, A <-> Y\");\n\njulia> all_backdoor_sets(admg, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:A]\n\njulia> dag2 = DAG(\"L1 --> X1, L1 --> Y, L2 --> X2, L2 --> Y, X1 --> Y, X2 --> Y\");\n\njulia> all_backdoor_sets(dag2, [:X1, :X2], :Y)\n1-element Vector{Vector{Symbol}}:\n [:L1, :L2]\n\n\n\n\n\n","title":"CausalStructures.all_backdoor_sets"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.backdoor_set","page":"Causal Identification","text":"backdoor_set(cg::DAG, x::Symbol, y::Symbol) -> Union{Vector{Symbol},Nothing}\n\nReturn a generalized back-door set relative to (x, y) and cg using the Generalized Backdoor Criterion (GBC; Maathuis and Colombo (2015), Corollary 4.1), or nothing if none exists.\n\nFor a DAG this reduces to Pearl's original result (Pearl (2009)): a generalized back-door set exists if and only if y is not a parent of x, and when it exists, parents(cg, x) is such a set (not necessarily minimal).\n\nExamples\n\njulia> dag = DAG(\"A --> X --> Y, A --> Y\");\n\njulia> backdoor_set(dag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> backdoor_set(dag, :Y, :A) === nothing  # A is a parent of Y\ntrue\n\nReferences\n\nMaathuis and Colombo (2015)\nPearl (2009)\n\n\n\n\n\nbackdoor_set(cg::ADMG, x::Symbol, y::Symbol) -> Union{Vector{Symbol},Nothing}\n\nReturn a generalized back-door set relative to (x, y) and cg, or nothing if none exists.\n\nExamples\n\njulia> admg = ADMG(\"A --> X --> Y, A --> Y\");\n\njulia> backdoor_set(admg, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> admg2 = ADMG(\"A --> X --> Y, A <-> Y\");  # latent confounder A also causes Y\n\njulia> backdoor_set(admg2, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> admg3 = ADMG(directed(:X, :Y), bidirected(:X, :Y));  # direct edge plus latent confounder\n\njulia> backdoor_set(admg3, :X, :Y) === nothing  # Y stays adjacent to X in M_X via X <-> Y\ntrue\n\nReferences\n\nMaathuis and Colombo (2015)\n\n\n\n\n\nbackdoor_set(cg::CPDAG, x::Symbol, y::Symbol) -> Union{Vector{Symbol},Nothing}\n\nReturn a generalized back-door set relative to (x, y) and cg using the Generalized Backdoor Criterion (GBC; Maathuis and Colombo (2015), Corollary 4.2), or nothing if none exists.\n\nLet C_X be cg with every directed edge out of x removed. A generalized back-door set exists if and only if y is not a parent of x and y is not a possible descendant of x in C_X; when it exists, parents(cg, x) is such a set (not necessarily minimal).\n\nDeliberately not defined for MPDAG or plain PDAG: Corollary 4.2 relies on a CPDAG-specific fact (every back-door path into x passes through a compelled parent) that fails once background knowledge introduces a partially directed cycle.\n\nExamples\n\njulia> cpdag = CPDAG(\"A --> X <-- C, X --> Y\");  # A --> X <-- C protects both edges into X\n\njulia> sort(backdoor_set(cpdag, :X, :Y))\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> cpdag2 = CPDAG(\"X --- Y\");\n\njulia> backdoor_set(cpdag2, :X, :Y) === nothing  # Y is a possible descendant of X in C_X\ntrue\n\nReferences\n\nMaathuis and Colombo (2015)\n\n\n\n\n\nbackdoor_set(cg::MAG, x::Symbol, y::Symbol) -> Union{Vector{Symbol},Nothing}\n\nReturn a generalized back-door set relative to (x, y) and cg using the Generalized Backdoor Criterion (GBC; Maathuis and Colombo (2015), Corollary 4.3), or nothing if none exists.\n\nLet M_X be cg with every visible directed edge out of x removed (Definition 4.2). A generalized back-door set exists if and only if y is not adjacent to x in M_X and D-SEP(x, y, M_X) does not intersect the descendants of x in cg; when it exists, D-SEP(x, y, M_X) is such a set (not necessarily minimal).\n\nExamples\n\njulia> mag = MAG(\"A <-> X, A --> M --> Y, X --> Y\");\n\njulia> backdoor_set(mag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\njulia> mag2 = MAG(\"X --> Y\");\n\njulia> backdoor_set(mag2, :X, :Y) === nothing  # X --> Y is invisible here, so Y ∈ adj(X, M_X)\ntrue\n\nReferences\n\nMaathuis and Colombo (2015)\n\n\n\n\n\nbackdoor_set(cg::PAG, x::Symbol, y::Symbol) -> Union{Vector{Symbol},Nothing}\n\nReturn a generalized back-door set relative to (x, y) and cg using the Generalized Backdoor Criterion (GBC; Maathuis and Colombo (2015), Theorem 4.1), or nothing if none exists.\n\nLet R be any MAG in the Markov equivalence class of cg with the same number of edges into x as cg (Definition 4.2; such an R always exists, Lemma 7.6), and let R_X be R with every directed edge out of x that is visible in cg removed. A generalized back-door set exists if and only if y is not adjacent to x in R_X and D-SEP(x, y, R_X) does not intersect the possible descendants of x in cg; when it exists, D-SEP(x, y, R_X) is such a set (not necessarily minimal; Theorem 4.1 shows the result does not depend on which admissible R is used).\n\nExamples\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);  # A o-> X, A --> Y, B o-> X, X --> Y\n\njulia> sort(backdoor_set(pag, :X, :Y))\n2-element Vector{Symbol}:\n :A\n :B\n\nReferences\n\nMaathuis and Colombo (2015)\n\n\n\n\n\n","title":"CausalStructures.backdoor_set"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.frontdoor_set","page":"Causal Identification","text":"frontdoor_set(cg::Union{DAG,ADMG}, x, y; include=[], restrict=nothing)\n    -> Vector{Symbol} or nothing\n\nReturn a front-door adjustment set Z with include ⊆ Z ⊆ restrict satisfying all three front-door conditions relative to (x, y) in cg, or nothing if no such set exists.\n\nx, y, include, and restrict may each be a single Symbol or an AbstractVector{Symbol}.\n\ninclude: nodes forced into the set.\nrestrict: candidate pool from which the set is drawn. Defaults to all nodes in cg except x and y.\n\nImplements Algorithm 1 of (Jeong et al., 2022):\n\nStep 1 (GETCAND2NDFDC): drop candidates that have a backdoor path from X.\nStep 2 (GETCAND3RDFDC): drop candidates for which condition 3 cannot be met.\nStep 3: check that the remaining set blocks all directed paths X –> Y.\n\nThe returned set is the full R'' from Steps 1-2 (not necessarily minimal). For ADMG, backdoor and condition-3 checks are done via m-separation, with bidirected edges treated as latent-confounder edges (a spouse contributes an arrowhead into a node just like a directed parent does).\n\nExamples\n\njulia> dag = DAG(\"U --> X + Y, X --> Z --> Y\");\n\njulia> frontdoor_set(dag, :X, :Y; restrict = :Z)\n1-element Vector{Symbol}:\n :Z\n\nJeong et al. (2022) Fig. 1b:\n\njulia> dag = DAG(\"U1 --> X + Y, U2 --> X + D, X --> A, A --> B + C + D, B + C + D --> Y\");\n\njulia> frontdoor_set(dag, :X, :Y; restrict = [:A, :B, :C, :D])\n3-element Vector{Symbol}:\n :A\n :B\n :C\n\njulia> frontdoor_set(dag, :X, :Y; include = :C, restrict = [:A, :C])\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> frontdoor_set(dag, :X, :Y; include = :D, restrict = [:A, :B, :C, :D]) === nothing\ntrue\n\nFig. 1b latent-projected to an ADMG: U1 -> X <-> Y, U2 -> X <-> D:\n\njulia> admg = ADMG(\"X <-> Y, X <-> D, X --> A, A --> B + C + D, B + C + D --> Y\");\n\njulia> frontdoor_set(admg, :X, :Y; restrict = [:A, :B, :C, :D])\n3-element Vector{Symbol}:\n :A\n :B\n :C\n\njulia> dag2 = DAG(\"U1 --> X1 + Y1, U2 --> X2 + Y2, X1 --> M, X2 --> M, M --> Y1 + Y2\");\n\njulia> frontdoor_set(dag2, [:X1, :X2], [:Y1, :Y2]; restrict = :M)\n1-element Vector{Symbol}:\n :M\n\nReferences\n\nJeong et al. (2022)\n\n\n\n\n\n","title":"CausalStructures.frontdoor_set"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.is_valid_frontdoor","page":"Causal Identification","text":"is_valid_frontdoor(cg::DAG, x, y, z = Symbol[]) -> Bool\n\nReturn true if z satisfies the front-door criterion for the causal effect of x on y in cg.\n\nx, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nz is a valid front-door set if:\n\nz intercepts all directed paths from x to y.\nThere are no unblocked backdoor paths from x to any node in z (given the empty set).\nFor every node zi in z, all backdoor paths from zi to y are blocked by x together with the remaining nodes in z (i.e., conditioned on x ∪ (z \\ {zi})).\n\nWhen these conditions hold, the causal effect is identified by the front-door formula, even in the presence of unmeasured confounders between x and y.\n\nExamples\n\njulia> dag = DAG(\"U --> X --> M --> Y, U --> Y\");\n\njulia> is_valid_frontdoor(dag, :X, :Y, :M)  # M mediates X -> Y and satisfies all conditions\ntrue\n\njulia> is_valid_frontdoor(dag, :X, :Y)         # empty Z leaves directed path X -> M -> Y open\nfalse\n\njulia> is_valid_frontdoor(dag, :X, :Y, :U)   # U does not intercept X -> M -> Y\nfalse\n\njulia> dag2 = DAG(\"U1 --> X1 + Y1, U2 --> X2 + Y2, X1 --> M, X2 --> M, M --> Y1 + Y2\");\n\njulia> is_valid_frontdoor(dag2, [:X1, :X2], [:Y1, :Y2], :M)  # M mediates every X --> Y path\ntrue\n\nReferences\n\nPearl (2009)\nJeong et al. (2022)\n\n\n\n\n\n","title":"CausalStructures.is_valid_frontdoor"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.all_frontdoor_sets","page":"Causal Identification","text":"all_frontdoor_sets(cg::Union{DAG,ADMG}, x, y; include=[], restrict=nothing)\n    -> Vector{Vector{Symbol}}\n\nReturn all front-door adjustment sets Z with include ⊆ Z ⊆ restrict relative to (x, y) in cg.\n\nx, y, include, and restrict may each be a single Symbol or an AbstractVector{Symbol}.\n\ninclude: nodes forced into every returned set.\nrestrict: candidate pool from which sets are drawn. Defaults to all nodes in cg except x and y.\n\nImplements Algorithm 2 (LISTFDSETS) of (Jeong et al., 2022). The algorithm has polynomial-delay guarantees: it outputs the first result in polynomial time and takes polynomial time between consecutive results. For ADMG, see the note on m-separation in frontdoor_set.\n\nExamples\n\njulia> dag = DAG(\"U --> X + Y, X --> Z --> Y\");\n\njulia> all_frontdoor_sets(dag, :X, :Y; restrict = :Z)\n1-element Vector{Vector{Symbol}}:\n [:Z]\n\nJeong et al. (2022) Fig. 1b:\n\njulia> dag = DAG(\"U1 --> X + Y, U2 --> X + D, X --> A, A --> B + C + D, B + C + D --> Y\");\n\njulia> sort(all_frontdoor_sets(dag, :X, :Y; restrict = [:A, :B, :C, :D]))\n4-element Vector{Vector{Symbol}}:\n [:A]\n [:A, :B]\n [:A, :B, :C]\n [:A, :C]\n\nFig. 1b latent-projected to an ADMG: U1 -> X <-> Y, U2 -> X <-> D:\n\njulia> admg = ADMG(\"X <-> Y, X <-> D, X --> A, A --> B + C + D, B + C + D --> Y\");\n\njulia> sort(all_frontdoor_sets(admg, :X, :Y; restrict = [:A, :B, :C, :D]))\n4-element Vector{Vector{Symbol}}:\n [:A]\n [:A, :B]\n [:A, :B, :C]\n [:A, :C]\n\njulia> dag2 = DAG(\"U1 --> X1 + Y1, U2 --> X2 + Y2, X1 --> M, X2 --> M, M --> Y1 + Y2\");\n\njulia> all_frontdoor_sets(dag2, [:X1, :X2], [:Y1, :Y2]; restrict = :M)\n1-element Vector{Vector{Symbol}}:\n [:M]\n\nReferences\n\nJeong et al. (2022)\n\n\n\n\n\n","title":"CausalStructures.all_frontdoor_sets"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.is_valid_iv","page":"Causal Identification","text":"is_valid_iv(cg::Union{DAG,ADMG}, x::Symbol, y, z) -> Bool\n\nReturn true if z is a valid instrumental set for the causal effect of x on y in cg.\n\ny and z may each be a single Symbol or an AbstractVector{Symbol}. x must be a single Symbol, since the instrumental-set criterion is defined for one structural coefficient x -> y.\n\nz is a valid instrumental set if:\n\nEvery zi ∈ z is d-/m-separated from y in the graph obtained from G by deleting the first edge x --> c of every causal path from x to y. This is the exclusion restriction: z can only affect y through x.\nAt least one zi ∈ z is d-/m-connected to x in G. This is the relevance condition: z must be associated with the treatment.\n\nWhen the only causal path is the edge x --> y, this is Definition 3.1 of van der Zander et al. (2015); deleting the first edge of every causal path extends it to the total effect.\n\nExamples\n\nClassic IV graph: Z --> X --> Y with hidden confounder U --> X, U --> Y:\n\njulia> dag = DAG(\"Z --> X --> Y, U --> X + Y\");\n\njulia> is_valid_iv(dag, :X, :Y, :Z)  # Z is a valid instrument\ntrue\n\njulia> is_valid_iv(dag, :X, :Y, :U)  # U confounds X and Y; fails exclusion restriction\nfalse\n\nADMG: X <-> Y encodes the hidden confounder directly:\n\njulia> admg = ADMG(\"X <-> Y, Z --> X --> Y\");\n\njulia> is_valid_iv(admg, :X, :Y, :Z)\ntrue\n\nZ instruments X, which affects two outcomes Y1 and Y2:\n\njulia> dag2 = DAG(\"Z --> X, X --> Y1 + Y2, U --> X + Y1 + Y2\");\n\njulia> is_valid_iv(dag2, :X, [:Y1, :Y2], :Z)\ntrue\n\nReferences\n\nBrito and Pearl (2002)\nPearl (2009)\nvan der Zander et al. (2015)\n\n\n\n\n\n","title":"CausalStructures.is_valid_iv"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.all_iv_sets","page":"Causal Identification","text":"all_iv_sets(cg::Union{DAG,ADMG}, x::Symbol, y;\n            minimal::Bool = true, max_size::Int = 3)\n    -> Vector{Vector{Symbol}}\n\nReturn all valid instrumental sets for the causal effect of x on y in cg, up to size max_size.\n\ny may be a single Symbol or an AbstractVector{Symbol}; x must be a single Symbol (see is_valid_iv).\n\nBruteforces over subsets of the allowed universe of nodes (nodes that are not x or y), checking each for validity using is_valid_iv. When minimal = true (default), only inclusion-minimal sets are returned.\n\nExamples\n\njulia> dag = DAG(\"Z1 --> X, Z2 --> X, X --> Y, U --> X + Y\");\n\njulia> all_iv_sets(dag, :X, :Y)\n2-element Vector{Vector{Symbol}}:\n [:Z1]\n [:Z2]\n\njulia> dag2 = DAG(\"Z1 --> X, Z2 --> X, X --> Y1 + Y2, U --> X + Y1 + Y2\");\n\njulia> all_iv_sets(dag2, :X, [:Y1, :Y2])\n2-element Vector{Vector{Symbol}}:\n [:Z1]\n [:Z2]\n\nReferences\n\nBrito and Pearl (2002)\n\n\n\n\n\n","title":"CausalStructures.all_iv_sets"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.id","page":"Causal Identification","text":"id(cg::Union{DAG,ADMG}, x, y) -> Union{Estimand,Nothing}\n\nReturn the interventional distribution P(y | do(x)) as an Estimand, or nothing if the effect is not identifiable.\n\nThis is the ID algorithm of Shpitser and Pearl (2008).\n\nx and y may each be a Symbol or a vector of them, and must be disjoint.\n\nBidirected edges represent latent confounders. To identify an effect in a DAG with unobserved variables, project them out first with latent_project.\n\nnote: Extra free variables\nThe returned formula may mention variables outside x and y. Line 3 of the algorithm enlarges the intervention set by a set W whenever intervening on W provably changes nothing, and the resulting expression is then a function of those values too.\n\nExamples\n\nBack-door adjustment is recovered as the g-formula:\n\njulia> dag = DAG(\"Z --> X + Y, X --> Y\");\n\njulia> id(dag, :X, :Y)\nΣ_{Z} P(Y | X, Z) P(Z)\n\nThe front-door graph, where X and Y are confounded but the effect is still identifiable through the mediator M:\n\njulia> admg = ADMG(\"X --> M, M --> Y, X <-> Y\");\n\njulia> id(admg, :X, :Y)\nΣ_{M} P(M | X) (Σ_{X'} P(X') P(Y | M, X'))\n\nThe inner sum ranges over a value of X distinct from the one intervened on, so it is printed under a primed name; summation indices are always renamed away from the variables of the query.\n\nThe bow arc, the canonical unidentifiable effect:\n\njulia> bow = ADMG(\"X --> Y, X <-> Y\");\n\njulia> id(bow, :X, :Y) === nothing\ntrue\n\nReferences\n\nShpitser and Pearl (2008)\nTian and Pearl (2002)\n\n\n\n\n\n","title":"CausalStructures.id"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.idc","page":"Causal Identification","text":"idc(cg::Union{DAG,ADMG}, x, y; given) -> Union{Estimand,Nothing}\n\nReturn the conditional interventional distribution P(y | do(x), given) as an Estimand, or nothing if it is not identifiable.\n\nThis is the IDC algorithm of Shpitser and Pearl (2008). Each variable in given that satisfies rule 2 of do-calculus is moved from the conditioning set into the intervention set; whatever remains is handled by id and normalized:\n\nP(y  do(x) z) = ID(y  z x)  Σ_y ID(y  z x)\n\nx, y, and given must be pairwise disjoint. With an empty given this reduces to id.\n\nExamples\n\njulia> dag = DAG(\"Z --> X + Y, X --> Y\");\n\njulia> idc(dag, :X, :Y; given = :Z)\nP(Y | X, Z)\n\nConditioning on the mediator of the front-door graph leaves an effect that no longer depends on the intervened value at all:\n\njulia> admg = ADMG(\"X --> M, M --> Y, X <-> Y\");\n\njulia> idc(admg, :X, :Y; given = :M)\nΣ_{X'} P(X') P(Y | M, X')\n\nReferences\n\nShpitser and Pearl (2008)\n\n\n\n\n\n","title":"CausalStructures.idc"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.idp","page":"Causal Identification","text":"idp(cg::PAG, x, y) -> Union{Estimand,Nothing}\n\nReturn the interventional distribution P(y | do(x)) as an Estimand, or nothing if the effect is not identifiable from the PAG cg.\n\nThis is the IDP algorithm of Jaber et al. (2022), complete for identifying marginal effects from a partial ancestral graph (a Markov equivalence class of causal diagrams), generalizing id from a single ADMG to the equivalence-class setting.\n\nx and y may each be a Symbol or a vector of them, and must be disjoint.\n\nExamples\n\nEffect identifiable through a witnessed backdoor:\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> idp(pag, :X, :Y) === nothing\nfalse\n\nEffect not identifiable, since the PAG has no orientable structure at all (every conditional independence in the underlying equivalence class is consistent with several different causal directions):\n\njulia> pag = mag_to_pag(MAG(\"Z --> X, Z --> Y, X --> Y\"));\n\njulia> idp(pag, :X, :Y) === nothing\ntrue\n\nReferences\n\nJaber et al. (2022)\nTian and Pearl (2002)\n\n\n\n\n\n","title":"CausalStructures.idp"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.cidp","page":"Causal Identification","text":"cidp(cg::PAG, x, y; given) -> Union{Estimand,Nothing}\n\nReturn the conditional interventional distribution P(y | do(x), given) as an Estimand, or nothing if it is not identifiable from the PAG cg.\n\nThis is the CIDP algorithm of Jaber et al. (2022), complete for identifying conditional effects from a partial ancestral graph, generalizing idc from a single ADMG to the equivalence-class setting.\n\nx, y, and given must be pairwise disjoint. With an empty given this reduces to idp.\n\nExamples\n\nEffect identifiable through a witnessed backdoor, conditioning on the confounder:\n\njulia> mag = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> cidp(pag, :X, :Y; given = :A) === nothing\nfalse\n\nEffect not identifiable, for the same reason as in idp's example:\n\njulia> pag = mag_to_pag(MAG(\"Z --> X, Z --> Y, X --> Y\"));\n\njulia> cidp(pag, :X, :Y; given = :Z) === nothing\ntrue\n\nReferences\n\nJaber et al. (2022)\n\n\n\n\n\n","title":"CausalStructures.cidp"},{"category":"type","location":"95-reference/40-adjustment/#CausalStructures.Estimand","page":"Causal Identification","text":"Estimand\n\nAbstract supertype for symbolic causal estimands: formulas expressed purely in terms of the observational distribution.\n\nThe concrete subtypes are Prob, Marginal, Product, and Quotient. Build them with the smart constructors prob, marginal, product, and quotient.\n\nOutput formats\n\nIt can be rendered as text, rendered as LaTeX, or taken apart field by field:\n\njulia> e = marginal([:Z], product([prob(:Y; given = [:X, :Z]), prob(:Z)]));\n\njulia> string(e)\n\"Σ_{Z} P(Y | X, Z) P(Z)\"\n\njulia> e.index\n1-element Vector{Symbol}:\n :Z\n\njulia> e.term.terms[1].given\n2-element Vector{Symbol}:\n :X\n :Z\n\nThe LaTeX form comes from the MIME\"text/latex\" method, so repr(MIME(\"text/latex\"), e) gives $\\sum_{Z} P(Y \\mid X, Z) P(Z)$, used for frontend documentation.\n\n\n\n\n\n","title":"CausalStructures.Estimand"},{"category":"type","location":"95-reference/40-adjustment/#CausalStructures.Prob","page":"Causal Identification","text":"Prob(vars, given)\n\nA probability term P(vars | given), the leaf of an Estimand tree.\n\nBoth variable lists are stored sorted, so that terms differing only in the order the variables were listed compare equal. Use prob to construct.\n\n\n\n\n\n","title":"CausalStructures.Prob"},{"category":"type","location":"95-reference/40-adjustment/#CausalStructures.Marginal","page":"Causal Identification","text":"Marginal(index, term)\n\nThe sum of term over all values of the variables in index. Use marginal to construct.\n\n\n\n\n\n","title":"CausalStructures.Marginal"},{"category":"type","location":"95-reference/40-adjustment/#CausalStructures.Product","page":"Causal Identification","text":"Product(terms)\n\nThe product of terms. An empty product is the multiplicative identity 1. Use product to construct.\n\n\n\n\n\n","title":"CausalStructures.Product"},{"category":"type","location":"95-reference/40-adjustment/#CausalStructures.Quotient","page":"Causal Identification","text":"Quotient(num, den)\n\nThe ratio num / den. Use quotient to construct.\n\n\n\n\n\n","title":"CausalStructures.Quotient"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.prob","page":"Causal Identification","text":"prob(vars; given = Symbol[]) -> Estimand\n\nBuild the probability term P(vars | given).\n\nvars may be a single Symbol or a vector of them. Both lists are sorted and de-duplicated, and any variable appearing in given is dropped from vars (since P(X, Y | X) = P(Y | X)). A term left with an empty head is the constant 1.\n\nExamples\n\njulia> prob([:Y, :W]; given = [:X])\nP(W, Y | X)\n\njulia> prob(:Y)\nP(Y)\n\n\n\n\n\n","title":"CausalStructures.prob"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.marginal","page":"Causal Identification","text":"marginal(index, term) -> Estimand\n\nSum term over the variables in index.\n\nAn empty index returns term unchanged, and nested sums are flattened into a single one. Summing a probability term over part of its own head marginalizes it directly: Σ_a P(a, b | c) becomes P(b | c).\n\nSums over a product are narrowed as far as they go: factors that do not mention the summation index are constants of the sum and are pulled out in front of it, and a factor whose whole head is summed over, and whose head no other factor under the sum mentions, sums to 1 and drops out. Under a ratio, the part of the index the denominator does not mention is summed in the numerator alone.\n\nExamples\n\nHere W is in the conditioning set rather than the head, so the sum stands:\n\njulia> marginal([:W], prob(:Y; given = [:W, :X]))\nΣ_{W} P(Y | W, X)\n\nSumming over a variable in the head marginalizes it away instead:\n\njulia> marginal([:W], prob([:W, :Y]; given = [:X]))\nP(Y | X)\n\njulia> marginal(Symbol[], prob(:Y))\nP(Y)\n\nP(Z | X) does not mention W, so it leaves the sum, and what remains sums to one:\n\njulia> marginal([:W], product([prob(:Z; given = [:X]), prob(:W; given = [:X, :Z])]))\nP(Z | X)\n\n\n\n\n\n","title":"CausalStructures.marginal"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.product","page":"Causal Identification","text":"product(terms) -> Estimand\n\nMultiply terms together.\n\nNested products are flattened and factors equal to 1 are dropped. A single-factor product collapses to that factor, and an empty product is 1. The order of the remaining factors is preserved.\n\nExamples\n\njulia> product([prob(:W; given = [:X]), prob(:Y; given = [:W, :X])])\nP(W | X) P(Y | W, X)\n\njulia> product([prob(:Y)])\nP(Y)\n\n\n\n\n\n","title":"CausalStructures.product"},{"category":"function","location":"95-reference/40-adjustment/#CausalStructures.quotient","page":"Causal Identification","text":"quotient(num, den) -> Estimand\n\nForm the ratio num / den.\n\nA denominator of 1 returns num unchanged. When the denominator is itself a / b and the numerator is exactly a, the ratio collapses to b; this is what most often turns _reduce_bucket's divisions back into something readable. Otherwise, a factor appearing on both sides cancels, and a numerator factor and a denominator factor related by the chain rule P(a, b | c) = P(a | b, c) P(b | c) divide out: P(a, b | c) / P(b | c) becomes P(a | b, c), and P(a, b | c) / P(a | b, c) becomes P(b | c). That is what turns the ratios of marginals produced by the ID recursion back into ordinary conditionals.\n\nCancellation assumes the cancelled factor is non-zero, which is the positivity assumption the identification results are stated under anyway.\n\nExamples\n\njulia> quotient(prob([:Y, :Z]; given = [:X]), prob(:Z; given = [:X]))\nP(Y | X, Z)\n\njulia> quotient(prob([:W, :Y, :Z]), prob(:W; given = [:Y, :Z]))\nP(Y, Z)\n\njulia> a = quotient(prob([:Y, :Z]), prob(:X));\n\njulia> b = marginal(:W, quotient(prob([:W, :Y, :Z]), prob(:W; given = [:X])));\n\njulia> quotient(a, quotient(a, b))\nΣ_{W} (P(W, Y, Z) / P(W | X))\n\nThe ratio is kept when it is not a conditional, here because the two terms condition on different things:\n\njulia> quotient(prob([:Y, :Z]; given = [:X]), prob(:Z))\nP(Y, Z | X) / P(Z)\n\n\n\n\n\n","title":"CausalStructures.quotient"},{"category":"section","location":"95-reference/20-queries/#queries-reference","page":"Queries","text":"","title":"Queries"},{"category":"section","location":"95-reference/20-queries/#Basic-accessors","page":"Queries","text":"","title":"Basic accessors"},{"category":"section","location":"95-reference/20-queries/#Traversal","page":"Queries","text":"","title":"Traversal"},{"category":"section","location":"95-reference/20-queries/#Local-structure","page":"Queries","text":"","title":"Local structure"},{"category":"section","location":"95-reference/20-queries/#Separation","page":"Queries","text":"","title":"Separation"},{"category":"section","location":"95-reference/20-queries/#Equivalence-class-queries","page":"Queries","text":"","title":"Equivalence-class queries"},{"category":"section","location":"95-reference/20-queries/#Graph-class-predicates","page":"Queries","text":"","title":"Graph class predicates"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.nodes","page":"Queries","text":"nodes(cg::CausalGraph) -> Vector{Symbol}\n\nReturn the nodes of cg in alphabetical order.\n\nExamples\n\njulia> dag = DAG(\"B --> A --> C\");\n\njulia> nodes(dag)\n3-element Vector{Symbol}:\n :A\n :B\n :C\n\n\n\n\n\n","title":"CausalStructures.nodes"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.edges","page":"Queries","text":"edges(cg::CausalGraph) -> Vector{CausalEdge}\n\nReturn the edges of cg.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> edges(dag)\n2-element Vector{CausalEdge}:\n A --> B\n B --> C\n\n\n\n\n\n","title":"CausalStructures.edges"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.has_edge","page":"Queries","text":"has_edge(cg::CausalGraph, src::Symbol, dst::Symbol) -> Bool\n\nReturn true if there is any edge between src and dst in cg.\n\nExamples\n\njulia> dag = DAG(\"A --> B\");\n\njulia> has_edge(dag, :A, :B)\ntrue\n\n\n\n\n\n","title":"CausalStructures.has_edge"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.topological_sort","page":"Queries","text":"topological_sort(cg::DAG) -> Vector{Symbol}\n\nReturn the nodes of cg in topological order.\n\nFor every directed edge u --> v in cg, u appears before v in the returned vector.\n\nExamples\n\njulia> dag = DAG(\"A <-- B, A --> C\");\n\njulia> topological_sort(dag)\n3-element Vector{Symbol}:\n :B\n :A\n :C\n\nReferences\n\nKahn (1962)\n\n\n\n\n\n","title":"CausalStructures.topological_sort"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.ancestors","page":"Queries","text":"ancestors(cg::Union{DAG,AbstractPDAG,ADMG,AbstractAG}, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the ancestors of node in cg: all nodes from which node is reachable by following directed edges forward.\n\nWhen open = true (open definition, default), node itself is excluded from the result. When open = false (closed definition), node is included. The default can be changed project-wide via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false) (restart Julia after).\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> ancestors(dag, :A)\nSymbol[]\n\njulia> ancestors(dag, :A, open = false)\n1-element Vector{Symbol}:\n :A\n\njulia> ancestors(dag, :C)\n2-element Vector{Symbol}:\n :A\n :B\n\n\n\n\n\n","title":"CausalStructures.ancestors"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.descendants","page":"Queries","text":"descendants(cg::Union{DAG,AbstractPDAG,ADMG,AbstractAG}, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the descendants of node in cg: all nodes reachable from node by following directed edges forward.\n\nWhen open = true (open definition, default), node itself is excluded from the result. When open = false (closed definition), node is included. The default can be changed project-wide via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false) (restart Julia after).\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> descendants(dag, :A)\n2-element Vector{Symbol}:\n :B\n :C\n\njulia> descendants(dag, :C)\nSymbol[]\n\njulia> descendants(dag, :A, open = false)\n3-element Vector{Symbol}:\n :A\n :B\n :C\n\n\n\n\n\n","title":"CausalStructures.descendants"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.anteriors","page":"Queries","text":"anteriors(cg::Union{DAG,ADMG,AbstractPDAG,AbstractAG}, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the anteriors of node in cg: all nodes from which node is reachable by following directed edges backward or traversing undirected edges.\n\nFor a DAG or ADMG, anteriors are equivalent to ancestors (no undirected edges exist). For AbstractPDAG and AbstractAG, undirected edges extend the reachable set beyond strict ancestors.\n\nWhen open = true (open definition, default), node itself is excluded from the result. When open = false (closed definition), node is included. The default can be changed project-wide via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false) (restart Julia after).\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> anteriors(dag, :C)  # same as ancestors for a DAG\n2-element Vector{Symbol}:\n :A\n :B\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> anteriors(pdag, :A)\nSymbol[]\n\njulia> anteriors(pdag, :C)  # C reaches B via undirected edge, then A via directed\n2-element Vector{Symbol}:\n :A\n :B\n\n\n\n\n\n","title":"CausalStructures.anteriors"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.posteriors","page":"Queries","text":"posteriors(cg::Union{DAG,ADMG,AbstractPDAG,AbstractAG}, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the posteriors of node in cg: all nodes reachable from node by following directed edges forward or traversing undirected edges.\n\nFor a DAG or ADMG, posteriors are equivalent to descendants (no undirected edges exist). For AbstractPDAG and AbstractAG, undirected edges extend the reachable set beyond strict descendants.\n\nWhen open = true (open definition, default), node itself is excluded from the result. When open = false (closed definition), node is included. The default can be changed project-wide via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false) (restart Julia after).\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> posteriors(dag, :A)  # same as descendants for a DAG\n2-element Vector{Symbol}:\n :B\n :C\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> posteriors(pdag, :A)  # B via directed edge, C via undirected edge from B\n2-element Vector{Symbol}:\n :B\n :C\n\njulia> posteriors(pdag, :C)  # C reaches B via undirected edge only\n1-element Vector{Symbol}:\n :B\n\n\n\n\n\n","title":"CausalStructures.posteriors"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.exogenous_nodes","page":"Queries","text":"exogenous_nodes(cg::Union{DAG,ADMG,AG}) -> Vector{Symbol}\nexogenous_nodes(cg::AbstractPDAG; undirected_as_parents = false) -> Vector{Symbol}\nexogenous_nodes(cg::UG) -> Vector{Symbol}\n\nReturn all exogenous nodes in cg: nodes with no incoming directed edges.\n\nFor UG, where no directed edges exist, a node is considered exogenous if and only if it is isolated (no neighbors at all).\n\nFor AbstractPDAG, the undirected_as_parents keyword controls how undirected edges are treated. When true, a node incident to any undirected edge is not considered exogenous.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> exogenous_nodes(dag)\n1-element Vector{Symbol}:\n :A\n\njulia> ug = UG(\"A --- B, C\");\n\njulia> exogenous_nodes(ug)  # only the isolated node C\n1-element Vector{Symbol}:\n :C\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> exogenous_nodes(pdag)\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> exogenous_nodes(pdag, undirected_as_parents = true)\n1-element Vector{Symbol}:\n :A\n\n\n\n\n\n","title":"CausalStructures.exogenous_nodes"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.parents","page":"Queries","text":"parents(cg, node::Symbol) -> Vector{Symbol}\n\nReturn the parents of node in cg: nodes p such that p --> node is an edge in cg.\n\nEquivalent to neighbors(cg, node; mode = :in). Applicable to DAG, AbstractPDAG, ADMG, AbstractAG, PAG, and UNKNOWN. For a PAG only definite parents (p --> node) are returned; a circle endpoint at node is not a parent.\n\nExamples\n\njulia> dag = DAG(\"A --> B <-- C\");\n\njulia> parents(dag, :B)\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> parents(dag, :A)\nSymbol[]\n\n\n\n\n\n","title":"CausalStructures.parents"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.children","page":"Queries","text":"children(cg, node::Symbol) -> Vector{Symbol}\n\nReturn the children of node in cg: nodes c such that node --> c is an edge in cg.\n\nEquivalent to neighbors(cg, node; mode = :out). Applicable to DAG, AbstractPDAG, ADMG, AbstractAG, PAG, and UNKNOWN. For a PAG only definite children (node --> c) are returned; a circle endpoint at the child is not a definite child.\n\nExamples\n\njulia> dag = DAG(\"A --> B + C\");\n\njulia> children(dag, :A)\n2-element Vector{Symbol}:\n :B\n :C\n\njulia> children(dag, :B)\nSymbol[]\n\n\n\n\n\n","title":"CausalStructures.children"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.spouses","page":"Queries","text":"spouses(cg::Union{ADMG,AbstractAG,PAG}, node::Symbol) -> Vector{Symbol}\n\nReturn the spouses of node in cg: nodes connected to node via a bidirected edge (node <-> spouse).\n\nExamples\n\njulia> admg = ADMG(\"A --> B, A <-> C\");\n\njulia> spouses(admg, :A)\n1-element Vector{Symbol}:\n :C\n\njulia> spouses(admg, :B)\nSymbol[]\n\n\n\n\n\n","title":"CausalStructures.spouses"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.neighbors","page":"Queries","text":"neighbors(cg::CausalGraph, node::Symbol; mode::Symbol = :all) -> Vector{Symbol}\n\nReturn neighbors of node in cg, filtered by edge type via mode.\n\nmode values:\n\n:all: all adjacent nodes\n:in: parents: nodes p with p --> node\n:out: children: nodes c with node --> c\n:undirected: undirected neighbors node --- c\n:bidirected: bidirected neighbors node <-> c\n\nFor a PAG the :in, :out, :undirected, and :bidirected modes return only neighbors joined by the corresponding definite (circle-free) edge. Neighbors joined by circle-mark edges (o->, o--, o-o) are reported by :all only.\n\nExamples\n\njulia> dag = DAG(\"A --> B <-- C\");\n\njulia> neighbors(dag, :B)\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> neighbors(dag, :B, mode = :in)\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> neighbors(dag, :A, mode = :out)\n1-element Vector{Symbol}:\n :B\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> neighbors(pdag, :B, mode = :undirected)\n1-element Vector{Symbol}:\n :C\n\n\n\n\n\n","title":"CausalStructures.neighbors"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.markov_blanket","page":"Queries","text":"markov_blanket(cg::Union{DAG,AbstractPDAG,ADMG,AbstractAG,PAG}, node::Symbol) -> Vector{Symbol}\n\nReturn the Markov blanket of node in cg. The Markov blanket is the minimal set of nodes that renders node conditionally independent of all other nodes in the graph.\n\nFor a DAG, the Markov blanket is the set of parents, children, and co-parents (other parents of node's children). For a AbstractPDAG, undirected neighbors are also included. For an ADMG or AbstractAG, it is parents, children, co-parents, spouses, and (for an AbstractAG) undirected neighbors, plus every node reachable by a collider path (Pellet and Elisseeff, 2008): a path of length >= 2 whose interior nodes are all colliders. For a PAG, the blanket is computed on a underlying MAG.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C, D --> C\");\n\njulia> markov_blanket(dag, :B)\n3-element Vector{Symbol}:\n :A\n :C\n :D\n\njulia> markov_blanket(dag, :C)\n2-element Vector{Symbol}:\n :B\n :D\n\njulia> mag = MAG(\"A <-> B <-> C\");\n\njulia> markov_blanket(mag, :A)\n2-element Vector{Symbol}:\n :B\n :C\n\nReferences\n\nPellet and Elisseeff (2008)\n\n\n\n\n\n","title":"CausalStructures.markov_blanket"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.districts","page":"Queries","text":"districts(cg::Union{ADMG,AbstractAG}) -> Vector{Vector{Symbol}}\n\nReturn all districts (c-components) of cg.\n\nA district is a maximal set of nodes connected via bidirected edges. Singleton nodes with no bidirected edges each form their own district.\n\nExamples\n\njulia> admg = ADMG(\"A --> B, A <-> C, D <-> E\");\n\njulia> districts(admg)\n3-element Vector{Vector{Symbol}}:\n [:A, :C]\n [:B]\n [:D, :E]\n\n\n\n\n\n","title":"CausalStructures.districts"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.d_separated","page":"Queries","text":"d_separated(cg::Union{DAG,AbstractPDAG}, x, y, z = Symbol[]) -> Bool\n\nReturn true iff every node in x is d-separated from every node in y given z in cg. x, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nTwo nodes are d-separated given a conditioning set z if every path between them is blocked. A path is blocked if it contains either a non-collider node in z, or a collider node (and all its descendants) not in z.\n\nFor DAG: restricts to the ancestor graph of x, y, and z, then runs a Bayes-ball traversal from x (blocked at conditioned non-colliders, passing through conditioned colliders) and checks whether y is reached.\n\nFor AbstractPDAG: restricts to the anterior set (nodes reachable via directed parents or undirected edges), then runs the same Bayes-ball traversal treating undirected edges as never forming a collider endpoint.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> d_separated(dag, :A, :C)        # chain A --> B --> C is open\nfalse\n\njulia> d_separated(dag, :A, :C, :B) # conditioning on B blocks the chain\ntrue\n\njulia> coll = DAG(\"A --> C <-- B\");\n\njulia> d_separated(coll, :A, :B)         # collider A --> C <-- B: blocked without conditioning\ntrue\n\njulia> d_separated(coll, :A, :B, :C)  # conditioning on collider C opens the path\nfalse\n\njulia> mpdag = MPDAG(\"A --- B --> C\");\n\njulia> d_separated(mpdag, :A, :C, :B) # B blocks whether A --> B or A <-- B\ntrue\n\njulia> d_separated(mpdag, :A, :C)       # B is possibly a non-collider: open path exists\nfalse\n\njulia> chain = DAG(\"A --> C <-- B, C --> D\");\n\njulia> d_separated(chain, [:A, :B], :D)         # C lies on both A-->C-->D and B-->C-->D: paths open\nfalse\n\njulia> d_separated(chain, [:A, :B], :D, :C)  # conditioning on chain node C blocks both paths\ntrue\n\nReferences\n\nLauritzen et al. (1990)\nHauser and Bühlmann (2012)\n\n\n\n\n\n","title":"CausalStructures.d_separated"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.m_separated","page":"Queries","text":"m_separated(cg::Union{DAG,ADMG,AbstractAG,PAG}, x, y, z = Symbol[]) -> Bool\n\nReturn true iff every node in x is m-separated from every node in y given z in cg. x, y, and z may each be a single Symbol or an AbstractVector{Symbol}.\n\nM-separation generalizes d-separation to graphs with bidirected and undirected edges. For a DAG, m-separation is equivalent to d_separated.\n\nDAG / ADMG: restricts to the ancestor graph of x, y, and z, then runs a Bayes-ball traversal from x treating parents and spouses alike as arrowhead endpoints, and checks whether y is reached.\n\nAbstractAG: same traversal extended with undirected edges, restricted to the anterior set of x, y, and z (Richardson & Spirtes, 2002).\n\nPAG: same traversal as AbstractAG, with circle marks collapsing to tails (as for possible_ancestors/possible_descendants).\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> m_separated(dag, :A, :C)        # equivalent to d_separated on a DAG\nfalse\n\njulia> m_separated(dag, :A, :C, :B)\ntrue\n\njulia> admg = ADMG(\"A --> B, A <-> C\");\n\njulia> m_separated(admg, :B, :C)        # B and C are connected via the bidirected edge at A\nfalse\n\njulia> m_separated(admg, :B, :C, :A) # conditioning on A blocks the path\ntrue\n\njulia> admg2 = ADMG(\"A <-> C <-> B\");\n\njulia> m_separated(admg2, [:A, :B], :C)  # both A and B are m-connected to C\nfalse\n\nReferences\n\nRichardson and Spirtes (2002)\n\n\n\n\n\n","title":"CausalStructures.m_separated"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.minimal_separator","page":"Queries","text":"minimal_separator(cg::Union{DAG,ADMG,AbstractAG,PAG,AbstractPDAG}, x, y; include=Symbol[], restrict=nothing)\n\nFind a minimal d-separator (DAG, AbstractPDAG) or m-separator (ADMG, AbstractAG, PAG) between nodes x and y.\n\nA set Z separates x from y if conditioning on Z renders them d/m-independent. The returned set is minimal: no proper subset (excluding forced include nodes) still separates x from y.\n\nReturns a Vector{Symbol} of node names, or nothing when no valid separator exists within the allowed candidate set.\n\nArguments\n\ncg: A DAG, ADMG, AbstractAG, PAG, or AbstractPDAG.\nx, y: The nodes to separate. Each may be a single Symbol or an AbstractVector{Symbol}, in which case the returned set separates every node in x from every node in y.\ninclude: Nodes forced into the separator. Must be a subset of restrict (or the default candidate set). May be a single Symbol or an AbstractVector{Symbol}.\nrestrict: Candidate pool from which the separator is drawn. Defaults to all nodes except x and y. May be a single Symbol or an AbstractVector{Symbol}.\n\nAlgorithm\n\nImplements FINDMINSEP from van der Zander and Liśkiewicz (2020), running in O(n + m) time. FINDNEARESTSEP is called twice; once from x, once from y restricted to the first result, and the outputs are intersected.\n\nDAG: directional Bayes-ball restricted to ancestors of {x, y} ∪ include.\nADMG: mark-based Bayes-ball over directed and bidirected edges, restricted to ancestors.\nAbstractAG: same as ADMG but uses anteriors (ancestors reachable via directed or undirected edges) and handles undirected edge marks.\nPAG: same as AbstractAG, with circle marks collapsing to tails (as for possible_ancestors/possible_descendants).\nAbstractPDAG: mark-based Bayes-ball over directed and undirected edges, restricted to anteriors.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> minimal_separator(dag, :A, :C)  # chain A --> B --> C: separator is {B}\n1-element Vector{Symbol}:\n :B\n\njulia> dag_coll = DAG(\"A --> C <-- B\");\n\njulia> minimal_separator(dag_coll, :A, :B)  # collider A --> C <-- B: already d-separated\nSymbol[]\n\njulia> dag_edge = DAG(\"A --> B\");\n\njulia> minimal_separator(dag_edge, :A, :B) === nothing  # direct edge: no separator exists\ntrue\n\njulia> dag4 = DAG(\"A --> X --> M --> Y, A --> Y\");\n\njulia> minimal_separator(dag4, :X, :Y)  # two paths require both A and M\n2-element Vector{Symbol}:\n :A\n :M\n\njulia> minimal_separator(dag4, :X, :Y, include = :M)  # force M in; A still needed\n2-element Vector{Symbol}:\n :A\n :M\n\njulia> minimal_separator(dag4, :X, :Y, restrict = :M) === nothing  # M alone cannot block X <-- A --> Y\ntrue\n\njulia> dag5 = DAG(\"A --> M1 --> Y, B --> M2 --> Y\");\n\njulia> minimal_separator(dag5, [:A, :B], :Y)  # both mediators are needed to block both sources\n2-element Vector{Symbol}:\n :M1\n :M2\n\njulia> admg = ADMG(\"A --> B --> C\");\n\njulia> minimal_separator(admg, :A, :C)\n1-element Vector{Symbol}:\n :B\n\njulia> pag = mag_to_pag(MAG(\"A --> X --> M --> Y, A --> Y\"));\n\njulia> minimal_separator(pag, :A, :M)\n1-element Vector{Symbol}:\n :X\n\njulia> mag2 = MAG(\"A <-> X1, B <-> X2, A --> M1 --> Y, B --> M2 --> Y\");\n\njulia> sort(minimal_separator(mag2, [:X1, :X2], :Y))\n2-element Vector{Symbol}:\n :A\n :B\n\nReferences\n\nvan der Zander and Liśkiewicz (2020)\n\n\n\n\n\n","title":"CausalStructures.minimal_separator"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_d_sep","page":"Queries","text":"possible_d_sep(cg::AbstractAG, x::Symbol, y::Union{Symbol,AbstractVector{Symbol}}) ->\n    Vector{Symbol}\n\nReturn D-SEP(x, y, cg): every node V != x for which there is a collider path between x and V in cg on which every node, including V, is an ancestor of x or of some member of y.\n\nD-SEP is the set that backdoor_set (for MAG and PAG) uses as the generalized back-door set (Maathuis & Colombo 2015), computed there on M_X (cg with x's visible edges removed) rather than on cg directly.\n\nExamples\n\njulia> mag = MAG(\"A <-> X, A --> M --> Y, X --> Y\");\n\njulia> possible_d_sep(mag, :X, :Y)\n3-element Vector{Symbol}:\n :A\n :M\n :Y\n\njulia> backdoor_set(mag, :X, :Y)  # M_X drops the visible edge X --> Y, excluding M and Y\n1-element Vector{Symbol}:\n :A\n\nReferences\n\nMaathuis and Colombo (2015)\n\n\n\n\n\n","title":"CausalStructures.possible_d_sep"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_ancestors","page":"Queries","text":"possible_ancestors(cg::AbstractPDAG, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the possible ancestors of node in cg: all nodes V for which there exists at least one DAG in the equivalence class represented by cg in which V is an ancestor of node.\n\nV is a possible ancestor of node if there is a b-possibly directed path from V to node (Perković, Kalisch & Maathuis 2017/2018): a path on which no node, however far back, has a directed edge into an earlier node on the path. For CPDAG this reduces to the simpler \"no edge compelled away from node\" rule (checking only consecutive steps suffices there, Meek 1995, Lemma 1); for MPDAG checking only consecutive steps is unsound, since background knowledge can create partially directed cycles.\n\nWhen open = true (default), node itself is excluded. When open = false (closed definition), node is included. The default can be changed via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false).\n\nExamples\n\njulia> cpdag = CPDAG(\"A --- B --- C\");\n\njulia> ancestors(cpdag, :C)\nSymbol[]\n\njulia> possible_ancestors(cpdag, :C)\n2-element Vector{Symbol}:\n :A\n :B\n\njulia> mpdag = MPDAG(\"A --- B --- C --- D --- A, D --> B\");\n\njulia> :B in possible_ancestors(mpdag, :D)  # B --> ... --> D would create a cycle with D --> B\nfalse\n\nReferences\n\nPerković et al. (2018)\nPerković et al. (2017)\n\n\n\n\n\npossible_ancestors(cg::PAG, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the possible ancestors of node in cg: all nodes V for which there exists at least one MAG in the equivalence class represented by cg in which V is an ancestor of node.\n\nV is a possible ancestor of node if there is a possibly directed path from V to node: a path where no edge has an arrowhead at the source side of each step. Each step traverses a neighbor whose far-mark (the mark at the neighbor's endpoint) is not an arrowhead: parents (<--), undirected (---), circle-parents (<-o), circle-undirected-out (o--), and circle-circle (o-o) edges all qualify. Spouses (<->), children (-->), and circle-children (o->) are excluded because the far-mark is a fixed arrowhead. Circle-undirected-in (--o) edges are also excluded: the near-mark is an invariant tail, so the neighbor can never be an ancestor of node through that edge in any MAG.\n\nWhen open = true (default), node itself is excluded. When open = false (closed definition), node is included. The default can be changed via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false).\n\nExamples\n\njulia> pag = PAG(\"A o-> B <-o C\");\n\njulia> sort(possible_ancestors(pag, :B))\n2-element Vector{Symbol}:\n :A\n :C\n\njulia> possible_ancestors(pag, :A)\nSymbol[]\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\n","title":"CausalStructures.possible_ancestors"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_descendants","page":"Queries","text":"possible_descendants(cg::AbstractPDAG, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the possible descendants of node in cg: all nodes V for which there exists at least one DAG in the equivalence class represented by cg in which V is a descendant of node.\n\nV is a possible descendant of node if there is a b-possibly directed path from node to V (Perković, Kalisch & Maathuis 2017/2018): a path on which no node, however far along, has a directed edge into an earlier node on the path. For CPDAG this reduces to the simpler \"no edge compelled away from node\" rule (checking only consecutive steps suffices there, Meek 1995, Lemma 1); for MPDAG checking only consecutive steps is unsound, since background knowledge can create partially directed cycles.\n\nWhen open = true (default), node itself is excluded. When open = false (closed definition), node is included. The default can be changed via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false).\n\nExamples\n\njulia> cpdag = CPDAG(\"A --- B --- C\");\n\njulia> descendants(cpdag, :A)\nSymbol[]\n\njulia> possible_descendants(cpdag, :A)\n2-element Vector{Symbol}:\n :B\n :C\n\njulia> mpdag = MPDAG(\"A --- B --- C --- D --- A, D --> B\");\n\njulia> :D in possible_descendants(mpdag, :B)  # B --> ... --> D would create a cycle with D --> B\nfalse\n\nReferences\n\nPerković et al. (2018)\nPerković et al. (2017)\n\n\n\n\n\npossible_descendants(cg::PAG, node::Symbol; open::Bool = true) -> Vector{Symbol}\n\nReturn the possible descendants of node in cg: all nodes V for which there exists at least one MAG in the equivalence class represented by cg in which V is a descendant of node.\n\nV is a possible descendant of node if there is a possibly directed path from node to V: a path where no edge has an arrowhead at the source side of each step. Each step traverses a neighbor where the near-mark at the current node is not an arrowhead: children (-->), undirected (---), circle-children (o->), circle-undirected-in (--o), and circle-circle (o-o) edges all qualify. Parents (<--), spouses (<->), and circle-parents (<-o) are excluded because the near-mark is a fixed arrowhead. Circle-undirected-out (o--) edges are also excluded: the far-mark is an invariant tail, so node can never be an ancestor of the neighbor through that edge in any MAG.\n\nWhen open = true (default), node itself is excluded. When open = false (closed definition), node is included. The default can be changed via Preferences.jl: set_preferences!(CausalStructures, \"open\" => false).\n\nExamples\n\njulia> pag = PAG(\"A o-> B <-o C\");\n\njulia> possible_descendants(pag, :A)\n1-element Vector{Symbol}:\n :B\n\njulia> possible_descendants(pag, :B)\nSymbol[]\n\nReferences\n\nPerković et al. (2018)\n\n\n\n\n\n","title":"CausalStructures.possible_descendants"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_parent_sets","page":"Queries","text":"possible_parent_sets(cg::AbstractPDAG, x::Symbol) -> Vector{Vector{Symbol}}\n\nReturn the possible parent sets of x implied by cg, using the graph-only half of local IDA (Algorithm 3 of (Maathuis et al., 2009)). For each accepted orientation of the undirected neighbors of x, include pa(x) ∪ S, where S is the subset oriented into x. On MPDAGs, acceptance requires a valid full Meek closure; on CPDAGs, the local v-structure check suffices. This is the xs = [x] special case of possible_joint_parent_sets.\n\nUnlike all_adjustment_sets, returns one entry per accepted subset, not per DAG in the Markov equivalence class.\n\nExamples\n\njulia> cpdag = CPDAG(\"X1 --- X2 + X3 + X4, X3 + X4 --> Y\");\n\njulia> sort(possible_parent_sets(cpdag, :X1); by = length)\n4-element Vector{Vector{Symbol}}:\n []\n [:X2]\n [:X3]\n [:X4]\n\nReferences\n\nMaathuis et al. (2009)\nPerković et al. (2017)\n\n\n\n\n\n","title":"CausalStructures.possible_parent_sets"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_joint_parent_sets","page":"Queries","text":"possible_joint_parent_sets(cg::AbstractPDAG, xs) ->\n    Vector{Vector{Vector{Symbol}}}\n\nReturn every locally valid joint parental structure of xs implied by cg.\n\nxs may be a single Symbol or an AbstractVector{Symbol}.\n\nThis is the graph part of joint-IDA (Nandy et al. (2017)), generalizing possible_parent_sets from a single intervention node to a set of simultaneous interventions.\n\nEvery undirected edge incident to at least one node in xs is jointly reoriented, including edges directly between two nodes of xs. A candidate orientation is accepted if it introduces no new v-structure at any node that gains a parent from it (not only at nodes in xs: two non-adjacent members of xs directing an edge into the same outside neighbor is also a new v-structure), and if the result extends to a DAG represented by cg.\n\nEach returned entry is a vector of parent sets in the same order as xs (entry i is a valid pa(xs[i]) for that joint orientation). Since a node xs[j] can appear in the parent set of xs[i], comparing entries pairwise recovers a valid causal ordering among xs for that orientation (the ordering downstream recursive-regression methods like RRC or MCD need); nodes of xs with no directed relation between them in a given entry are left unordered by that orientation, as either relative order is valid.\n\nExamples\n\njulia> cpdag = CPDAG(\"X1 --- X2, X1 --- A, X2 --- B, A --> Y, B --> Y\");\n\njulia> for pa in possible_joint_parent_sets(cpdag, [:X1, :X2])\n           println(pa)\n       end\n[[:X2], [:B]]\n[[:A], [:X1]]\n[Symbol[], [:X1]]\n[[:X2], Symbol[]]\n\nFour of the eight ways to jointly orient the three edges touching {X1, X2} survive: e.g. A --> X1 --> X2 <-- B is rejected because it creates a new v-structure at X2 between the non-adjacent X1 and B, even though neither endpoint of that collision is in xs.\n\nReferences\n\nMaathuis et al. (2009)\nNandy et al. (2017)\n\n\n\n\n\n","title":"CausalStructures.possible_joint_parent_sets"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.possible_local_structures","page":"Queries","text":"possible_local_structures(cg::PAG, x::Symbol) -> Vector{Vector{Symbol}}\n\nReturn every valid local structure at x in cg (Proposition 2 of Wang et al. (2023)).\n\nEach entry is a subset C of x's circle-marked neighbors for which a MAG consistent with cg exists with x <-> v for every v in C and x --> v for every other circle-marked neighbor. This is the graph-only ingredient PAGcauses (Wang et al. (2025)) enumerates local structures with; pair an entry with maximal_local_mag to get the corresponding graph.\n\nThrows ArgumentError if cg has selection bias (undirected edges), since both source papers assume none throughout.\n\nReferences\n\nWang et al. (2023)\nWang et al. (2025)\n\n\n\n\n\n","title":"CausalStructures.possible_local_structures"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_dag","page":"Queries","text":"is_dag(cg::CausalGraph)   -> Bool\n\nCheck whether cg satisfies the structural constraints of a DAG (directed acyclic graph), independent of its declared graph class.\n\nExamples\n\njulia> pdag = PDAG(\"A --> B --> C\");\n\njulia> is_dag(pdag)\ntrue\n\n\n\n\n\n","title":"CausalStructures.is_dag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_pdag","page":"Queries","text":"is_pdag(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a PDAG (partially directed acyclic graph), independent of its declared graph class.\n\nExamples\n\njulia> unk = UNKNOWN(\"A --> B --- C\");\n\njulia> is_pdag(unk)\ntrue\n\n\n\n\n\n","title":"CausalStructures.is_pdag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_cpdag","page":"Queries","text":"is_cpdag(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a CPDAG (completed partially directed acyclic graph), independent of its declared graph class.\n\nExamples\n\njulia> dag = DAG(\"A --> B\");\n\njulia> is_cpdag(dag)\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_cpdag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_mpdag","page":"Queries","text":"is_mpdag(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a MPDAG (maximally oriented partially directed acyclic graph), independent of its declared graph class.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> is_mpdag(dag)\ntrue\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> is_mpdag(pdag)\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_mpdag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_ug","page":"Queries","text":"is_ug(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a UG (undirected graph), independent of its declared graph class.\n\nExamples\n\njulia> pdag = PDAG(\"A --- B\");\n\njulia> is_ug(pdag)\ntrue\n\n\n\n\n\n","title":"CausalStructures.is_ug"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_admg","page":"Queries","text":"is_admg(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a ADMG (acyclic directed mixed graph), independent of its declared graph class.\n\nExamples\n\njulia> pdag = UNKNOWN(\"A <-> B\");\n\njulia> is_admg(pdag)\ntrue\n\n\n\n\n\n","title":"CausalStructures.is_admg"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_ag","page":"Queries","text":"is_ag(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a AG (ancestral graph), independent of its declared graph class.\n\nExamples\n\njulia> pdag = UNKNOWN(\"A <-> B\");\n\njulia> is_ag(pdag)\ntrue\n\n\n\n\n\n","title":"CausalStructures.is_ag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_mag","page":"Queries","text":"is_mag(cg::CausalGraph) -> Bool\n\nCheck whether cg satisfies the structural constraints of a MAG (maximal ancestral graph), independent of its declared graph class.\n\nExamples\n\njulia> ag = AG(\"A --> B --> C\");\n\njulia> is_mag(ag)\ntrue\n\njulia> ag = AG(\"Z <-> X <-> Y, Z <-> W, X --> W, Z --> Y\");\n\njulia> is_mag(ag)\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_mag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_pag","page":"Queries","text":"is_pag(cg::CausalGraph) -> Bool\n\nCheck whether cg is a valid PAG (partial ancestral graph): the marks are the invariant marks of the Markov equivalence class of some MAG, independent of cg's declared graph class. Verified by resolving cg to a MAG with mag_from_pag and checking that mag_to_pag recovers cg.\n\nExamples\n\njulia> pag = mag_to_pag(MAG(\"A --> B <-- C\"));\n\njulia> is_pag(pag)\ntrue\n\njulia> is_pag(UNKNOWN(\"A --> B\"))\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_pag"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_simple","page":"Queries","text":"is_simple(cg::CausalGraph) -> Bool\n\nReturn true if cg has no self-loops and no parallel edges between the same pair of nodes.\n\nFor all graph classes except UNKNOWN, simplicity is guaranteed by construction.\n\nExamples\n\njulia> dag = DAG(\"A --> B\");\n\njulia> is_simple(dag)\ntrue\n\njulia> unk = UNKNOWN(\"A --> B, A --> B\");\n\njulia> is_simple(unk)\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_simple"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.is_acyclic","page":"Queries","text":"is_acyclic(cg::CausalGraph) -> Bool\n\nReturn true if cg contains no directed cycle.\n\nFor all graph classes except UNKNOWN, acyclicity is guaranteed by construction.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> is_acyclic(dag)\ntrue\n\njulia> unk = UNKNOWN(\"A --> B, B --> A\");\n\njulia> is_acyclic(unk)\nfalse\n\n\n\n\n\n","title":"CausalStructures.is_acyclic"},{"category":"function","location":"95-reference/20-queries/#CausalStructures.markov_equivalent","page":"Queries","text":"markov_equivalent(cg1::DAG, cg2::DAG) -> Bool\n\nReturn true if cg1 and cg2 belong to the same Markov equivalence class (MEC), i.e. they encode exactly the same conditional independences.\n\nTwo DAGs are Markov equivalent if and only if they share the same skeleton (undirected adjacency structure) and the same set of v-structures (unshielded colliders a –> b <– c with a,c non-adjacent). This is the Verma-Pearl characterization (Verma & Pearl, 1990).\n\nExamples\n\njulia> cg1 = DAG(\"A --> B <-- C\");\n\njulia> cg2 = DAG(\"A --> B <-- C\");\n\njulia> markov_equivalent(cg1, cg2)\ntrue\n\njulia> cg3 = DAG(\"A --> B --> C\");\n\njulia> markov_equivalent(cg1, cg3)\nfalse\n\nReferences\n\nVerma and Pearl (1990)\n\n\n\n\n\nmarkov_equivalent(cg1::MAG, cg2::MAG) -> Bool\n\nReturn true if cg1 and cg2 belong to the same Markov equivalence class, i.e. they impose the same m-separation constraints.\n\nExamples\n\nA <-> B <-> C and A --> B <-- C both have an unshielded collider at B, so they share the PAG A o-> B <-o C and are Markov equivalent. Replacing the B <-> C edge with B --> C removes that collider, changing the equivalence class:\n\njulia> m1 = MAG(\"A <-> B <-> C\");\n\njulia> m2 = MAG(\"A --> B <-- C\");\n\njulia> markov_equivalent(m1, m2)\ntrue\n\njulia> m3 = MAG(\"A <-> B --> C\");\n\njulia> markov_equivalent(m1, m3)\nfalse\n\nReferences\n\nAli et al. (2009)\n\n\n\n\n\n","title":"CausalStructures.markov_equivalent"},{"category":"section","location":"95-reference/60-editing/#editing-reference","page":"Editing","text":"","title":"Editing"},{"category":"function","location":"95-reference/60-editing/#CausalStructures.add_edges","page":"Editing","text":"add_edges(cg::CausalGraph, es::CausalEdge...) -> CausalGraph\n\nReturn a new graph of the same class with edges es added. Nodes referenced by any edge are added automatically if not already present.\n\nExamples\n\njulia> dag = DAG(\"A --> B\");\n\njulia> add_edges(dag, directed(:B, :C))\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\njulia> add_edges(dag, directed(:B, :C), directed(:C, :D))\nDAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A --> B, B --> C, C --> D\n\n\n\n\n\n","title":"CausalStructures.add_edges"},{"category":"function","location":"95-reference/60-editing/#CausalStructures.remove_edges","page":"Editing","text":"remove_edges(cg::CausalGraph, es::CausalEdge...) -> CausalGraph\n\nReturn a new graph of the same class with edges es removed. Nodes that become isolated are retained. Throws ArgumentError if any edge in es is not present.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> remove_edges(dag, directed(:A, :B), directed(:B, :C))\nDAG with 3 nodes and 0 edges:\n  nodes: A, B, C\n  edges:\n    (none)\n\n\n\n\n\n","title":"CausalStructures.remove_edges"},{"category":"function","location":"95-reference/60-editing/#CausalStructures.add_nodes","page":"Editing","text":"add_nodes(cg::CausalGraph, ns::Symbol...) -> CausalGraph\n\nReturn a new graph of the same class with isolated nodes ns added. Nodes already present are ignored.\n\nExamples\n\njulia> g = DAG(\"A --> B\");\n\njulia> add_nodes(g, :C, :D)\nDAG with 4 nodes and 1 edge:\n  nodes: A, B, C, D\n  edges:\n    A --> B\n\njulia> add_nodes(g, :A) === g\ntrue\n\n\n\n\n\n","title":"CausalStructures.add_nodes"},{"category":"function","location":"95-reference/60-editing/#CausalStructures.remove_nodes","page":"Editing","text":"remove_nodes(cg::CausalGraph, ns::Symbol...) -> CausalGraph\n\nReturn a new graph of the same class with nodes ns and all their incident edges removed. Throws ArgumentError if any node in ns is not present.\n\nExamples\n\njulia> g = DAG(\"A --> B --> C\");\n\njulia> remove_nodes(g, :B)\nDAG with 2 nodes and 0 edges:\n  nodes: A, C\n  edges:\n    (none)\n\njulia> remove_nodes(g, :A, :B)\nDAG with 1 node and 0 edges:\n  nodes: C\n  edges:\n    (none)\n\n\n\n\n\n","title":"CausalStructures.remove_nodes"},{"category":"function","location":"95-reference/60-editing/#CausalStructures.reclass","page":"Editing","text":"reclass(cg::CausalGraph, T::Type{<:CausalGraph}) -> T\n\nReturn a new graph of class T with the same nodes and edges as cg. Throws an error if the edges violate the structural constraints of T.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> pdag = reclass(dag, PDAG)\nPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\n\n\n\n\n","title":"CausalStructures.reclass"},{"category":"section","location":"91-developer/#Developer-Docs","page":"Developer Docs","text":"Some notes on performance. Before doing any of these, please benchmark/profile the code, to see if it affects the performance.","title":"Developer Docs"},{"category":"section","location":"91-developer/#Skipping-validation","page":"Developer Docs","text":"Every graph constructor takes validate::Bool. You can use validate=false if the algorithm is proven to give a valid graph type, e.g. dag_to_cpdag's last step.\n\nThere's a stronger version of the same idea: constructing a graph directly from (edges, backend), bypassing build_backend too. This can be useful when the algorithm already has the information needed to construct the backend, so rebuilding it from scratch would be unnecessarily expensive.\n\nFor example, enumerate_dags assembles colptr/deg/rowval directly from index sets it already knows are sorted, rather than rebuilding the backend from scratch.","title":"Skipping validation"},{"category":"section","location":"91-developer/#Index-based-traversal","page":"Developer Docs","text":"parents, children, spouses, and neighbors are the public API and work with Symbol node names. Internally, algorithm code should not call these in hot loops, since each call involves a dictionary lookup and an array allocation.\n\nInstead, work directly with the backend using _parents_slice and related functions. These return a @view of node indices into the backend's CSR rowval array, avoiding the repeated lookups and allocations.","title":"Index-based traversal"},{"category":"function","location":"91-developer/#CausalStructures._build_graph","page":"Developer Docs","text":"T(node_set, edges::Vector{CausalEdge}; validate::Bool = true) -> T\n\nEvery concrete graph type T (DAG, UG, PDAG, CPDAG, MPDAG, ADMG, AG, MAG, UNKNOWN, PAG) has this constructor, which builds a graph directly from a node set and an edge vector, bypassing the items... collection and the string DSL that the other constructor forms (T(items...), T(s::AbstractString)) go through to get there.\n\nPass validate = false to skip the structural check for performance reasons.\n\nExamples\n\njulia> DAG(Set([:A, :B]), [directed(:A, :B)])\nDAG with 2 nodes and 1 edge:\n  nodes: A, B\n  edges:\n    A --> B\n\njulia> DAG(Set([:A, :B]), [directed(:A, :B), directed(:B, :A)]; validate = false)\nDAG with 2 nodes and 2 edges:\n  nodes: A, B\n  edges:\n    A --> B, B --> A\n\n\n\n\n\n","title":"CausalStructures._build_graph"},{"category":"function","location":"91-developer/#CausalStructures.bucket_slice","page":"Developer Docs","text":"bucket_slice(B, i, bucket) -> AbstractVector{Int}\n\nIndices of node i's neighbors in relationship bucket, as a view into B.rowval. Not meaningful on its own: bucket numbers only make sense per backend type.\n\n\n\n\n\n","title":"CausalStructures.bucket_slice"},{"category":"function","location":"91-developer/#CausalStructures._all_nbrs_slice","page":"Developer Docs","text":"_all_nbrs_slice(B, i) -> AbstractVector{Int}\n\nIndices of all of node i's neighbors, across every relationship bucket, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._all_nbrs_slice"},{"category":"function","location":"91-developer/#CausalStructures._parents_slice","page":"Developer Docs","text":"_parents_slice(B, i) -> AbstractVector{Int}\n\nIndices of the parents of node i (p --> i), as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._parents_slice"},{"category":"function","location":"91-developer/#CausalStructures._children_slice","page":"Developer Docs","text":"_children_slice(B, i) -> AbstractVector{Int}\n\nIndices of the children of node i (i --> c), as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._children_slice"},{"category":"function","location":"91-developer/#CausalStructures._spouses_slice","page":"Developer Docs","text":"_spouses_slice(B, i) -> AbstractVector{Int}\n\nIndices of the spouses of node i (i <-> s), as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._spouses_slice"},{"category":"function","location":"91-developer/#CausalStructures._undirected_slice","page":"Developer Docs","text":"_undirected_slice(B, i) -> AbstractVector{Int}\n\nIndices of node i's undirected neighbors (i --- u), as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._undirected_slice"},{"category":"function","location":"91-developer/#CausalStructures._circle_children_slice","page":"Developer Docs","text":"_circle_children_slice(B::PAGBackend, i) -> AbstractVector{Int}\n\nIndices of j with i o-> j, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._circle_children_slice"},{"category":"function","location":"91-developer/#CausalStructures._circle_parents_slice","page":"Developer Docs","text":"_circle_parents_slice(B::PAGBackend, i) -> AbstractVector{Int}\n\nIndices of j with i <-o j, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._circle_parents_slice"},{"category":"function","location":"91-developer/#CausalStructures._circle_undirected_out_slice","page":"Developer Docs","text":"_circle_undirected_out_slice(B::PAGBackend, i) -> AbstractVector{Int}\n\nIndices of j with i o-- j, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._circle_undirected_out_slice"},{"category":"function","location":"91-developer/#CausalStructures._circle_undirected_in_slice","page":"Developer Docs","text":"_circle_undirected_in_slice(B::PAGBackend, i) -> AbstractVector{Int}\n\nIndices of j with i --o j, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._circle_undirected_in_slice"},{"category":"function","location":"91-developer/#CausalStructures._circle_circle_slice","page":"Developer Docs","text":"_circle_circle_slice(B::PAGBackend, i) -> AbstractVector{Int}\n\nIndices of j with i o-o j, as a view into B.rowval.\n\n\n\n\n\n","title":"CausalStructures._circle_circle_slice"},{"category":"section","location":"20-causal-identification/#causal-identification-guide","page":"Causal Identification","text":"This guide expands on the Getting Started tutorial with more advanced identification techniques.\n\nusing CausalStructures\nusing CairoMakie\nusing Sugiyama","title":"Causal Identification"},{"category":"section","location":"20-causal-identification/#Adjustment-strategies","page":"Causal Identification","text":"The adjustment_set function supports three different strategies for finding adjustment sets. Let's build a DAG with multiple confounders and mediators – Figure 6.5 of Peters et al. (2017):\n\ndag = DAG(\n    \"C --> X, A --> X + K, X --> F + D, K --> Y, D --> Y + G, Y --> H\")\nplot(dag)\nFigureAxisPlot()\n\nParents strategy: Adjusts for all parents of X. Simple but often includes unnecessary variables:\n\nadjustment_set(dag, :X, :Y; type = :parents)\n2-element Vector{Symbol}:\n :A\n :C\n\nBackdoor strategy: Applies Pearl's backdoor criterion to find valid adjustment sets that block all confounding paths:\n\nadjustment_set(dag, :X, :Y; type = :backdoor)\n1-element Vector{Symbol}:\n :A\n\nOptimal strategy (default): Computes the O-set, which minimizes the asymptotic variance of the causal effect estimator:\n\nadjustment_set(dag, :X, :Y; type = :optimal)\n1-element Vector{Symbol}:\n :K","title":"Adjustment strategies"},{"category":"section","location":"20-causal-identification/#Minimal-separator","page":"Causal Identification","text":"Sometimes we need the smallest set of variables that d-separates two nodes. The minimal_separator function finds one:\n\nminimal_separator(dag, :X, :Y)\n2-element Vector{Symbol}:\n :A\n :D\n\nThe restrict keyword limits the search to a specific pool of variables.\n\nminimal_separator(dag, :X, :Y; restrict = [:D, :K])\n2-element Vector{Symbol}:\n :D\n :K\n\nThe include keyword forces certain variables to always be in the result:\n\nminimal_separator(dag, :X, :Y; include = :K)\n2-element Vector{Symbol}:\n :D\n :K","title":"Minimal separator"},{"category":"section","location":"20-causal-identification/#Frontdoor-adjustment","page":"Causal Identification","text":"When confounders are unobserved, the backdoor criterion doesn't apply. Consider a graph where an unobserved confounder U affects both the treatment X and outcome Y:\n\ndag2 = DAG(\"U --> X + Y, X --> M --> Y\")\nplot(dag2)\nFigureAxisPlot()\n\nThe mediator M is a descendant of X, so adjusting for it violates the backdoor criterion:\n\nis_valid_backdoor(dag2, :X, :Y, :M)\nfalse\n\nHowever, M intercepts every causal path from X to Y and is itself unconfounded; this is exactly the frontdoor criterion:\n\nis_valid_frontdoor(dag2, :X, :Y, :M)\ntrue\n\nfrontdoor_set(dag2, :X, :Y)\n1-element Vector{Symbol}:\n :M","title":"Frontdoor adjustment"},{"category":"section","location":"20-causal-identification/#Instrumental-variables","page":"Causal Identification","text":"When hidden confounders block backdoor adjustment and no suitable mediator exists for the frontdoor criterion, an instrumental variable (IV) can still identify the causal effect. An instrument Z must satisfy two conditions: it must be associated with the treatment X (relevance), and it can only affect the outcome Y through X (exclusion restriction).\n\nConsider a graph where U is an unobserved confounder and Z is an available instrument, but there is no mediator on the path from X to Y:\n\ndag3 = DAG(\"U --> X + Y, Z --> X --> Y\")\nplot(dag3)\nFigureAxisPlot()\n\nWithout a mediator, the frontdoor criterion cannot apply here:\n\nis_valid_frontdoor(dag3, :X, :Y, :Z)\nfalse\n\nHowever, Z is a valid instrument since it is d-connected to X and d-separated from Y given X in the interventional graph G_overlineX (the graph with all incoming edges to X removed):\n\nis_valid_iv(dag3, :X, :Y, :Z)\ntrue","title":"Instrumental variables"},{"category":"section","location":"20-causal-identification/#ADMG-adjustment","page":"Causal Identification","text":"In an ADMG (Acyclic Directed Mixed Graph), bidirected edges <-> represent hidden common causes directly, without explicitly keeping the unobserved nodes in the graph. An ADMG arises naturally by projecting unobserved variables out of a DAG via latent_project. Let's project U out of dag3:\n\nadmg = latent_project(dag3, :U)\nADMG with 3 nodes and 3 edges:\n  nodes: X, Y, Z\n  edges:\n    X --> Y, X <-> Y, Z --> X\n\n\nRemoving U introduces X <-> Y, which captures its confounding effect. The functions discussed above also work on ADMGs, e.g.:\n\nis_valid_iv(admg, :X, :Y, :Z)\ntrue","title":"ADMG adjustment"},{"category":"section","location":"20-causal-identification/#General-identification","page":"Causal Identification","text":"The methods discussed above are each sufficient, not necessary. CausalStructures also implements the ID algorithm of Shpitser and Pearl (2008) and returns the estimand as an Estimand.\n\nOn a graph with an observed confounder, it recovers the familiar g-formula:\n\ndag4 = DAG(\"Z --> X + Y, X --> Y\")\nid(dag4, :X, :Y)\nΣ_{Z} P(Y | X, Z) P(Z)\n\nProjecting U out of the front-door graph gives an ADMG where no adjustment set exists, yet the effect is identified through the mediator:\n\nadmg2 = latent_project(dag2, :U)\nid(admg2, :X, :Y)\nΣ_{M} P(M | X) (Σ_{X'} P(X') P(Y | M, X'))\n\nwhich is the standard front-door adjustment formula. The primed X' is a summation index distinct from the intervened value of X, following the usual convention.\n\nIn the following graph the causal effect cannot be identified:\n\nadmg\nADMG with 3 nodes and 3 edges:\n  nodes: X, Y, Z\n  edges:\n    X --> Y, X <-> Y, Z --> X\n\n\nid(admg, :X, :Y) === nothing\ntrue\n\nidc extends id to conditional effects P(y mid do(x) z):\n\nidc(admg2, :X, :Y; given = :M)\nΣ_{X'} P(X') P(Y | M, X')","title":"General identification"},{"category":"section","location":"20-causal-identification/#Working-with-the-result","page":"Causal Identification","text":"The result is an immutable tree of Prob, Marginal, Product, and Quotient nodes, so it can be inspected or transformed programmatically:\n\ne = id(dag4, :X, :Y)\n(typeof(e), e.index, typeof.(e.term.terms))\n(Marginal, [:Z], DataType[Prob, Prob])","title":"Working with the result"},{"category":"section","location":"85-changelog/#CausalStructures-changelog","page":"Changelog","text":"All notable changes to this project will be documented in this file.\n\nThe format is based on Keep a Changelog, and this project adheres to Semantic Versioning.","title":"CausalStructures changelog"},{"category":"section","location":"85-changelog/#[0.7.1](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.7.1)-2026-09-24","page":"Changelog","text":"","title":"0.7.1 - 2026-09-24"},{"category":"section","location":"85-changelog/#New-features","page":"Changelog","text":"Implemented markov_blanket for a PAG.\nenumerate_mags has a selection_bias keyword; selection_bias = false returns only the MAGs without undirected edges, pruning those candidates from the search.\nadjustment_set(::DAG) and adjustment_set(::AbstractPDAG) with type = :optimal now warn when some node of y is not a (possible) descendant of x, since the O-set is not defined there.\nEstimands returned by id/idc/idp/cidp are simplified further.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes","page":"Changelog","text":"markov_blanket on an AbstractAG or ADMG missed nodes reachable through collider paths (e.g. C in A <-> B <-> C), so it could return a set too small to separate node from the rest of the graph.\nmeek_closure's R4 was missing the precondition that a must be adjacent to d, so it could orient a --> b in cases not actually implied by the pattern.\nThe adjustment functions (is_valid_adjustment, all_adjustment_sets, adjustment_set) could reject valid adjustment sets and return a non-optimal set in several cases:\nOn an AbstractPDAG or PAG, nodes were treated as lying on a causal path from x to y when they do not (e.g. B in B --- X --> Y).\nOn an MPDAG, the separation step moralized the proper backdoor graph, which is only correct for CPDAGs. is_valid_adjustment and all_adjustment_sets now check amenability explicitly and block definite-status paths directly.\nWith several treatments on a DAG, ADMG, AG or MAG, a node was treated as causal when it reaches y only through another treatment (e.g. W in X1 --> W --> X2 --> Y with x = [:X1, :X2]). This affected is_valid_adjustment, all_adjustment_sets, and adjustment_set(::DAG; type = :optimal).\nbackdoor_set on a CPDAG could return an invalid set (e.g. [] for X, Y in X --- A --> Y, X --> Y).\nis_valid_iv/all_iv_sets accepted a confounded descendant of x as an instrument (e.g. Z in X --> Z, X --> Y, U --> X + Y).\npossible_joint_parent_sets could return joint parent sets that no DAG in the class realizes.\napply_background_knowledge accepted background knowledge inconsistent with the graph (e.g. A --> B, C --> B on A --- B --- C) and returned a graph with a new v-structure. It now errors in those cases.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#Performance-improvements","page":"Changelog","text":"Improved performance of dag_from_pdag by using the potential-sink worklist algorithm from Wienöbst, Bannach & Liśkiewicz (UAI 2021), instead of rescanning every remaining node after each removal.","title":"Performance improvements"},{"category":"section","location":"85-changelog/#[0.7.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.7.0)-2026-09-19","page":"Changelog","text":"","title":"0.7.0 - 2026-09-19"},{"category":"section","location":"85-changelog/#Breaking-changes","page":"Changelog","text":"plot's visual defaults (colors, linewidths, etc) are no longer configurable via Preferences.jl. Instead, set a project-wide default with a Makie theme, e.g. Makie.set_theme!(CausalGraphPlot = (node_color = :lightblue,)).\nA per-edge style Dict (e.g. edge_color) no longer treats a bare node name as \"every edge touching this node\"; it now only matches a CausalEdge, a (src, dst) tuple, an edge-type symbol, or :default.\nplot's node-label keywords labels, label_color, label_fontsize, and label_font are renamed to node_labels, node_label_color, node_label_fontsize, and node_label_font, for consistency with the node_*/edge_* naming used elsewhere.\ncondition_marginalize's keywords cond_vars/marg_vars are renamed to given/index, matching the given keyword used by idc/cidp/prob and the index argument used by marginal.","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-2","page":"Changelog","text":"Support of Makie themes: plot's edge_color and edge_label_color now inherit the active Makie theme's linecolor/textcolor when not given explicitly, so e.g. Makie.set_theme!(Makie.theme_dark()) keeps edges and edge labels visible against a dark figure.\nAutomatic edge routing via dummy nodes (from Sugiyama) now work with stretch_to_fig=true.\nImplement idp and cidp for identifying causal effects from PAGs (Jaber et al. 2022), generalizing id/idc from ADMGs.\nplot gains edge_linestyle, styling an edge's own line (e.g. dashing <-> edges to mark latent confounding, as is common in the literature).\nplot gains edge_labels, edge_label_color, edge_label_fontsize, edge_label_font, edge_label_shift, and edge_label_distance for drawing text along each edge, following its own angle.\nuniform_dag gains a counts keyword (paired with the new uniform_dag_counts) to reuse a precomputed DP table across repeated draws at the same n (or smaller).","title":"New features"},{"category":"section","location":"85-changelog/#Performance-improvements-2","page":"Changelog","text":"Improved performance of all_frontdoor_sets/frontdoor_set by reusing scratch buffers across recursive calls and scanning only each node's actual neighbors.\nImproved performance of all_frontdoor_sets/frontdoor_set's step that filters out candidate nodes unable to satisfy the front-door criterion's third condition, by replacing its per-candidate reachability search with a single combined pass, for both DAG and ADMG.\nImproved performance of all_iv_sets by testing membership per candidate node instead of per combinatorial subset.\nImproved performance of possible_joint_parent_sets/possible_parent_sets by avoiding an expensive graph rebuild-and-validate per candidate orientation.\nImproved performance of condition_marginalize and ag_to_mag by using a single-pass separator-existence check instead of minimal_separator's full two-pass search.","title":"Performance improvements"},{"category":"section","location":"85-changelog/#[0.6.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.6.0)-2026-09-14","page":"Changelog","text":"","title":"0.6.0 - 2026-09-14"},{"category":"section","location":"85-changelog/#Breaking-changes-2","page":"Changelog","text":"plot's layout-algorithm keywords (e.g. seed, iterations) must now be passed as layout_kwargs = (; seed = 1) instead of directly, since plot is now backed by a proper Makie recipe with a fixed set of attributes.\nplot now returns a FigureAxisPlot instead of a Figure.","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-3","page":"Changelog","text":"Added backdoor_set (Generalized Backdoor Criterion) for ADMG.\nCausalGraph now supports == and hash, comparing graphs structurally (same class, nodes, and edges).\nplot!(ax, cg) draws a CausalGraph into an Axis you already own, so multiple graphs (or a graph and other plots) can share one Figure.\nplot/plot!'s returned plot is fully reactive: plt.node_color[] = :red restyles it in place, and node/label sizing keeps fitting the containing Axis live, including as it's resized.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes-2","page":"Changelog","text":"is_valid_adjustment/all_adjustment_sets on ADMG and PAG could wrongly validate an adjustment set when a node had two or more bidirected/circle edges to distinct confounders.\nplot's edge_paths override for one edge no longer discards automatic per-edge routing (e.g. from layout = :sugiyama) for the rest of the graph's edges.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#Other-changes","page":"Changelog","text":"Improved performance of adjustment_set.","title":"Other changes"},{"category":"section","location":"85-changelog/#[0.5.1](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.5.1)-2026-09-11","page":"Changelog","text":"","title":"0.5.1 - 2026-09-11"},{"category":"section","location":"85-changelog/#New-features-4","page":"Changelog","text":"Node-set arguments across the package (x/y/z, include/restrict, latents, nodes, etc.) now also accept a single Symbol, not just Vector{Symbol}.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes-3","page":"Changelog","text":"is_valid_iv/all_iv_sets on an ADMG could wrongly reject a valid instrument when its own edge into the treatment was bidirected (e.g. Z <-> X).\npossible_parent_sets/possible_optimal_adjustment_sets on an MPDAG only checked for a new collider at the target node, so it could wrongly accept a parent orientation that closes a directed cycle elsewhere in the graph.\nmaximal_local_mag/possible_local_structures: Step 2 of the local-structure algorithm looked for a witness edge pointing out of Vl instead of into it.\nmeek_closure/is_mpdag: R1 wrongly skipped orientations that would create a new collider, and R4 used the wrong pattern, so both could leave a background-knowledge PDAG under-oriented.\npagcauses/maximal_local_mag: could return wrong or spurious adjustment sets. Fixed and validated testing against brute-force approach.\nis_valid_backdoor/all_backdoor_sets on a DAG wrongly accepted any set when y was itself a parent of x.\nThe generalized adjustment criterion's forbidden set (is_valid_adjustment/all_adjustment_sets on DAG/ADMG/AG/MAG/AbstractPDAG/PAG) excluded y instead of x from the causal-path nodes before taking descendants.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#Other-changes-2","page":"Changelog","text":"Package now compiles under --trim=safe (JuliaC).\nImprove documentation.\npagcauses now parallelizes over Threads.nthreads() once there are enough candidates to be worth splitting across tasks.\nImproved performance of some adjustment set functions.","title":"Other changes"},{"category":"section","location":"85-changelog/#[0.5.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.5.0)-2026-09-05","page":"Changelog","text":"","title":"0.5.0 - 2026-09-05"},{"category":"section","location":"85-changelog/#Breaking-changes-3","page":"Changelog","text":"cgraph(...; class = T) is removed. Construct graphs by calling the graph type directly instead, e.g. DAG(\"A --> B\"), ADMG(\"A --> B, A <-> B).","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-5","page":"Changelog","text":"plot now supports edge_paths, a Dict overriding an edge's drawn route with an explicit polyline instead of curvature/automatic routing.\nAdded a :sugiyama layout method for DAGs. Requries using Sugiyama, and if loaded is the default for DAGs.","title":"New features"},{"category":"section","location":"85-changelog/#[0.4.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.4.0)-2026-08-24","page":"Changelog","text":"","title":"0.4.0 - 2026-08-24"},{"category":"section","location":"85-changelog/#Breaking-changes-4","page":"Changelog","text":"plot's node_shape :round/:box are renamed to :circle/:square.","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-6","page":"Changelog","text":"plot's node_shape now supports :ellipse/:rect for label-fit stretching.\nplot now supports a stretch_to_fig_size to stretch the layout to fill fig_size instead of following the aspect of the layout.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes-4","page":"Changelog","text":"Fixed plot node sizes being shrunk by an unrelated close node pair elsewhere in the graph.\nFixed plot's automatic edge routing to use all nodes.\nFixed plot clipping a curved edge at the canvas edge.\nFixed plot's curvature being unable to force a straight edge to override the automatic routing. curvature now defaults to nothing (auto-route) so any explicit value now works correctly.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#[0.3.1](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.3.1)-2026-08-18","page":"Changelog","text":"","title":"0.3.1 - 2026-08-18"},{"category":"section","location":"85-changelog/#New-features-7","page":"Changelog","text":"d_separated, m_separated, minimal_separator (DAG), and the backdoor/frontdoor/adjustment-set functions now accept a Vector{Symbol} (in addition to a single Symbol) for x/y, for querying sets of treatments/outcomes.\nAdded id/idc (Shpitser & Pearl's identification algorithm) for DAG/ADMG.\nAdded possible_parent_sets, the graph half of the local IDA algorithm, for AbstractPDAG.\nAdded backdoor_set (Generalized Backdoor Criterion, Maathuis & Colombo 2015) for DAG, CPDAG, MAG, and PAG.\nAdded possible_optimal_adjustment_sets (O-set-based IDA) and possible_joint_parent_sets (joint-IDA) for AbstractPDAG.\nAdded possible_d_sep (D-SEP) for AbstractAG.\nAdded possible_local_structures and maximal_local_mag (Wang, Qin & Zhou 2023) for PAG.\nAdded pagcauses (PAGcauses, Wang, Tao, Qin & Zhou 2025), finding every valid adjustment set for a PAG without enumerating MAGs.\nNew plot features:\nplot gained node_shape (:round or :box), per node or for the whole graph. Both shapes are fitted to their label but rounded out to equal sides unless the label is oblong, so short labels give circles and squares while long ones give ellipses and rectangles. Edges clip to (and route around) the shape actually drawn.\nplot gained node_linestyle, for dashed or dotted node borders.\nplot gained labels, overriding the text drawn in each node, so labels can carry spaces, subscripts, or several lines.\nplot's layout keyword now also accepts a Dict of positions keyed by node name, not just a Vector in nodes(cg) order.\nplot gained curvature, bowing an edge into an arc by a signed fraction of its length; per edge, per edge type, or for the whole graph. An explicitly curved edge overrides the automatic obstacle routing and parallel-edge fanning.\nplot's per-edge style Dicts accept a CausalEdge key (e.g. bidirected(:X, :Y)) naming one exact edge, which is what separates the two edges of an ADMG's shared pair. A (src, dst) tuple key now names an unordered node pair, matching either way round, so styling no longer requires knowing which way an edge is stored.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes-5","page":"Changelog","text":"Parallel edges are now rejected on construction for every graph class except UNKNOWN; previously cgraph(\"X --> Y, X --> Y\"; class = DAG) and friends were accepted.\npossible_ancestors/possible_descendants on MPDAG could include nodes only reachable via a partially directed cycle introduced by background knowledge; this also affected is_valid_adjustment/all_adjustment_sets on MPDAG.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#[0.3.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.3.0)-2026-08-10","page":"Changelog","text":"","title":"0.3.0 - 2026-08-10"},{"category":"section","location":"85-changelog/#Breaking-changes-5","page":"Changelog","text":"Removed the built-in :circle layout. layout/plot now require NetworkLayout to be loaded (or bring your own layout to plot), and default to :stress.","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-8","page":"Changelog","text":"Added adjustment_set for ADMG and PAG, returning the smallest valid adjustment set.\nAdded anteriors/posteriors methods for ADMG.\nExtended condition_marginalize to accept ADMG.\nAdded check_cycles and r4 as keyword arguments to meek_closure, which can be set to help speed it up.\nAdded node_padding (and \"plot_node_padding\" preference) to control the padding between a node's label and its circle edge.\nAdded arrow_fill (and \"plot_edge_arrow_fill\" preference) to style arrowheads independently of edge_color, including hollow/outline-only arrowheads.\nAdded fig_size to set the plot's figure size.","title":"New features"},{"category":"section","location":"85-changelog/#Bug-fixes-6","page":"Changelog","text":"dag_from_pdag could pick a sink whose undirected neighbors were adjacent to each other but not to the sink's existing parents, which could make it introduce a v-structure absent from the input PDAG.","title":"Bug fixes"},{"category":"section","location":"85-changelog/#[0.2.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.2.0)-2026-08-04","page":"Changelog","text":"","title":"0.2.0 - 2026-08-04"},{"category":"section","location":"85-changelog/#Breaking-changes-6","page":"Changelog","text":"Dropped the simple keyword argument from cgraph.\nChanged default layout to :stress once NetworkLayout is loaded.","title":"Breaking changes"},{"category":"section","location":"85-changelog/#New-features-9","page":"Changelog","text":"Implement separation for PAGs.\nExtend adjustments to include DAG.\nAdd class = PAG support to generate_graph.\nMake enumerate_mags use several threads if available.","title":"New features"},{"category":"section","location":"85-changelog/#Other-changes-3","page":"Changelog","text":"Improved and extended docs, and uses DocumenterCodeBlocks now.","title":"Other changes"},{"category":"section","location":"85-changelog/#[0.1.0](https://github.com/BjarkeHautop/CausalStructures.jl/releases/tag/v0.1.0)-2026-07-25","page":"Changelog","text":"Initial release.","title":"0.1.0 - 2026-07-25"},{"category":"section","location":"90-bibliography/#bibliography","page":"Bibliography","text":"Ali, R. A.; Richardson, T. S. and Spirtes, P. (2009). Markov equivalence for ancestral graphs. Annals of Statistics 37, 2808–2837.\n\n\n\nBrito, C. and Pearl, J. (2002). Generalized Instrumental Variables. In: Proceedings of UAI 2002, Vol. 18; pp. 85–93.\n\n\n\nChickering, D. M. (2002). Learning Equivalence Classes of Bayesian-Network Structures. Journal of Machine Learning Research 2, 445–498.\n\n\n\nDor, D. and Tarsi, M. (1992). A simple algorithm to construct a consistent extension of a partially oriented graph. Technical Report R-185 (Cognitive Systems Laboratory, UCLA).\n\n\n\nEvans, R. J. (2016). Graphs for margins of Bayesian networks. Scandinavian Journal of Statistics 43, 625–648.\n\n\n\nHauser, A. and Bühlmann, P. (2012). Characterization and greedy learning of interventional Markov equivalence classes of directed acyclic graphs. Journal of Machine Learning Research 13, 2409–2464.\n\n\n\nHenckel, L.; Perković, E. and Maathuis, M. H. (2022). Graphical Criteria for Efficient Total Effect Estimation Via Adjustment in Causal Linear Models. Journal of the Royal Statistical Society: Series B 84, 579–599.\n\n\n\nJaber, A.; Ribeiro, A.; Zhang, J. and Bareinboim, E. (2022). Causal Identification under Markov equivalence: Calculus, Algorithm, and Completeness. In: Advances in Neural Information Processing Systems, Vol. 35, edited by Koyejo, S.; Mohamed, S.; Agarwal, A.; Belgrave, D.; Cho, K. and Oh, A. (Curran Associates, Inc.); pp. 3679–3690.\n\n\n\nJeong, H.; Tian, J. and Bareinboim, E. (2022). Finding and Listing Front-door Adjustment Sets. In: Advances in Neural Information Processing Systems, Vol. 35, edited by Koyejo, S.; Mohamed, S.; Agarwal, A.; Belgrave, D.; Cho, K. and Oh, A. (Curran Associates, Inc.); pp. 33173–33185.\n\n\n\nKahn, A. B. (1962). Topological sorting of large networks. Communications of the ACM 5, 558–562.\n\n\n\nKuipers, J. and Moffa, G. (2015). Uniform random generation of large acyclic digraphs. Statistics and Computing 25, 227–242.\n\n\n\nLauritzen, S. L.; Dawid, A. P.; Larsen, B. N. and Leimer, H.-G. (1990). Independence properties of directed Markov fields. Networks 20, 491–505.\n\n\n\nMaathuis, M. H. and Colombo, D. (2015). A generalized back-door criterion. The Annals of Statistics 43, 1060–1088.\n\n\n\nMaathuis, M. H.; Kalisch, M. and Bühlmann, P. (2009). Estimating high-dimensional intervention effects from observational data. The Annals of Statistics 37, 3133–3164.\n\n\n\nMeek, C. (1995). Causal inference and causal explanation with background knowledge. In: Proceedings of UAI 1995; pp. 403–410.\n\n\n\nNandy, P.; Maathuis, M. H. and Richardson, T. S. (2017). Estimating the effect of joint interventions from observational data in sparse high-dimensional settings. The Annals of Statistics 45, 647–674.\n\n\n\nPearl, J. (2009). Causality: Models, Reasoning, and Inference. 2nd Edition (Cambridge University Press).\n\n\n\nPellet, J.-p. and Elisseeff, A. (2008). Finding Latent Causes in Causal Networks: an Efficient Approach Based on Markov Blankets. In: Advances in Neural Information Processing Systems, Vol. 21, edited by Koller, D.; Schuurmans, D.; Bengio, Y. and Bottou, L. (Curran Associates, Inc.).\n\n\n\nPerković, E.; Kalisch, M. and Maathuis, M. H. (2017). Interpreting and using CPDAGs with background knowledge. In: Proceedings of UAI 2017.\n\n\n\nPerković, E.; Textor, J.; Kalisch, M. and Maathuis, M. H. (2018). Complete Graphical Characterization and Construction of Adjustment Sets in Markov Equivalence Classes of Ancestral Graphs. Journal of Machine Learning Research 18, 1–62.\n\n\n\nPeters, J. M.; Janzing, D. and Schölkopf, B. (2017). Elements of Causal Inference: Foundations and Learning Algorithms: Foundations and Learning Algorithms. *Adaptive Computation and Machine Learning series * (MIT Press, United States).\n\n\n\nRichardson, T. and Spirtes, P. (2002). Ancestral graph Markov models. Annals of Statistics 30, 962–1030.\n\n\n\nRichardson, T. S. (2003). Markov properties for acyclic directed mixed graphs. Scandinavian Journal of Statistics 30, 145–157.\n\n\n\nShpitser, I. and Pearl, J. (2008). Complete Identification Methods for the Causal Hierarchy. Journal of Machine Learning Research 9, 1941–1979.\n\n\n\nTian, J. and Pearl, J. (2002). A General Identification Condition for Causal Effects. In: Proceedings of the 18th National Conference on Artificial Intelligence (AAAI 2002); pp. 567–573.\n\n\n\nVerma, T. and Pearl, J. (1990). Equivalence and synthesis of causal models. In: Proceedings of UAI 1990.\n\n\n\nWang, T.-Z.; Qin, T. and Zhou, Z.-H. (2023). Sound and complete causal identification with latent variables given local background knowledge. Artificial Intelligence 322, 103964.\n\n\n\nWang, T.-Z.; Tao, L.; Qin, T. and Zhou, Z.-H. (2025). Estimating possible causal effects with latent variables via adjustment and novel rule orientation. Artificial Intelligence 347, 104387.\n\n\n\nWienöbst, M.; Bannach, M. and Liśkiewicz, M. (27–30 Jul 2021). Extendability of causal graphical models: Algorithms and computational complexity. In: Proceedings of the Thirty-Seventh Conference on Uncertainty in Artificial Intelligence, Vol. 161 of Proceedings of Machine Learning Research, edited by de Campos, C. and Maathuis, M. H. (PMLR); pp. 1248–1257.\n\n\n\nWitte, J.; Henckel, L.; Maathuis, M. H. and Didelez, V. (2020). On efficient adjustment in causal graphs. Journal of Machine Learning Research 21, 1–45.\n\n\n\nvan der Zander, B. and Liśkiewicz, M. (2020). Finding Minimal d-separators in Linear Time and Applications. In: Proceedings of the 35th Conference on Uncertainty in Artificial Intelligence (UAI 2020), Vol. 115 of PMLR; pp. 637–647.\n\n\n\nvan der Zander, B.; Textor, J. and Liśkiewicz, M. (2015). Efficiently Finding Conditional Instruments for Causal Inference. In: Proceedings of the 24th International Joint Conference on Artificial Intelligence (IJCAI 2015); pp. 3243–3249.\n\n\n\nZhang, J. (2008). On the completeness of orientation rules for causal discovery in the presence of latent confounders and selection bias. Artificial Intelligence 172, 1873–1896.\n\n\n\n","title":"Bibliography"},{"category":"section","location":"95-reference/50-simulation/#simulation-reference","page":"Simulation","text":"","title":"Simulation"},{"category":"function","location":"95-reference/50-simulation/#CausalStructures.generate_graph","page":"Simulation","text":"generate_graph([rng], n; m=nothing, p=nothing, class=DAG, latents=0) -> CausalGraph\n\nGenerate a random graph on n observed nodes named V1, …, Vn.\n\nExactly one of m (exact edge count) or p (edge probability) must be given. Edges are sampled uniformly at random over all possible pairs using Erdős–Rényi and oriented by a random topological ordering, guaranteeing acyclicity. This samples uniformly over graphs of a given size/density, but not uniformly over the space of DAGs (some DAGs correspond to more topological orderings than others). For an exact uniform draw from all labelled DAGs on n nodes regardless of edge count, use uniform_dag instead.\n\nclass may be DAG (default), CPDAG, ADMG, MAG, or PAG. For CPDAG the sampled DAG is converted via dag_to_cpdag. For ADMG/MAG/PAG, latents additional nodes L1, …, Lk are added to the underlying random DAG and then marginalized out via latent_project; m/p are applied to the full n + latents node set before projection. latents must be 0 (the default) for class = DAG or class = CPDAG.\n\nFor class = MAG or class = PAG, each candidate is checked and, if invalid, resampled (up to an internal retry limit) until a valid MAG is found; an error is raised if none is found, which can happen for very dense graphs with many latents. For class = PAG, the resulting MAG is then converted to its equivalence class via mag_to_pag.\n\nrng defaults to Random.default_rng() when omitted; pass an explicit AbstractRNG (e.g. Random.Xoshiro(seed)) for reproducibility.\n\nExamples\n\ndag = generate_graph(7; m = 5)\n\ncpdag = generate_graph(7; m = 5, class = CPDAG)\n\nadmg = generate_graph(15; p = 0.25, class = ADMG, latents = 3)\n\nmag = generate_graph(15; p = 0.25, class = MAG, latents = 3)\n\npag = generate_graph(15; p = 0.25, class = PAG, latents = 3)\n\n\n\n\n\n","title":"CausalStructures.generate_graph"},{"category":"function","location":"95-reference/50-simulation/#CausalStructures.uniform_dag","page":"Simulation","text":"uniform_dag([rng], n::Integer; counts = nothing) -> DAG\n\nGenerate a random DAG on n observed nodes named V1, …, Vn uniformly at random from the space of all labelled DAGs on n nodes.\n\nUnlike generate_graph, which samples edges independently (uniform over graphs of a given size/density but not uniform over the space of DAGs), this uses the recursive-enumeration algorithm of Kuipers and Moffa (2015).\n\nrng defaults to Random.default_rng() when omitted; pass an explicit AbstractRNG (e.g. Random.Xoshiro(seed)) for reproducibility.\n\nCost grows quickly with n (well past O(n^3), since the DP table's BigInt entries also grow in digit-width), unlike generate_graph's roughly edge-linear cost; pass a table from uniform_dag_counts via counts to avoid recomputing it when drawing many DAGs at the same n (or below).\n\nExamples\n\ndag = uniform_dag(6)\n\nReferences\n\nKuipers and Moffa (2015)\n\n\n\n\n\n","title":"CausalStructures.uniform_dag"},{"category":"function","location":"95-reference/50-simulation/#CausalStructures.uniform_dag_counts","page":"Simulation","text":"uniform_dag_counts(n::Integer) -> Vector{Vector{BigInt}}\n\nPrecompute the DP table uniform_dag needs to sample DAGs on n nodes. table[m][k] is the number of labelled DAGs on m nodes with exactly k outpoints, for every 1 <= m <= n; entry m only depends on entries < m, so a table computed for some n is also valid for any n' <= n.\n\nPass the result as uniform_dag(rng, n; counts = table) to avoid recomputing it on every draw when sampling many DAGs at the same (or a smaller) n.\n\nExamples\n\njulia> table = uniform_dag_counts(20);\n\njulia> using Random\n\njulia> dags = [uniform_dag(Xoshiro(i), 20; counts = table) for i = 1:5];\n\nReferences\n\nKuipers and Moffa (2015)\n\n\n\n\n\n","title":"CausalStructures.uniform_dag_counts"},{"category":"function","location":"95-reference/50-simulation/#CausalStructures.simulate_data","page":"Simulation","text":"simulate_data([rng], cg::DAG; samples, standardize=true,\n              coef_range=(-1.0, 1.0), error_sd=1.0)\n    -> Dict{Symbol, Vector{Float64}}\n\nSimulate observational data from a linear Gaussian structural causal model over cg. Each node is a linear function of its parents plus independent Gaussian noise with standard deviation error_sd. Edge coefficients are drawn uniformly from coef_range. If standardize = true (default), each variable is standardized to zero mean and unit variance.\n\nrng defaults to Random.default_rng() when omitted; pass an explicit AbstractRNG (e.g. Random.Xoshiro(seed)) for reproducibility.\n\nReturns a Dict mapping each node name to a length-samples vector.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> data = simulate_data(dag; samples = 100);\n\njulia> haskey(data, :A)\ntrue\n\njulia> length(data[:B])\n100\n\n\n\n\n\n","title":"CausalStructures.simulate_data"},{"category":"section","location":"95-reference/70-plotting/#plotting-reference","page":"Plotting","text":"Plotting is provided by an extension that loads when a Makie backend is loaded. Node placement additionally requires NetworkLayout.jl or Sugiyama.jl (or bring your own layout). See the Plotting guide for full examples.","title":"Plotting"},{"category":"method","location":"95-reference/70-plotting/#Makie.plot-Tuple{CausalGraph}","page":"Plotting","text":"Makie.plot(cg; kwargs...) -> FigureAxisPlot\nMakie.plot!(ax, cg; kwargs...) -> CausalGraphPlot\n\nVisualize a CausalGraph using Makie. Requires loading a Makie backend (e.g. CairoMakie) before calling.\n\nEvery keyword below (except the figure-level ones noted last) is one of its reactive attributes, so e.g. plt.node_color[] = :red restyles the existing plot in place.\n\nSee causalgraphplot for the full list of attributes, all of which are shared by both plot and plot!. Style keywords accept either a scalar (applied to everything) or a Dict for per-node/per-edge overrides; a per-edge Dict may be keyed by a CausalEdge, a (src, dst) tuple, an edge-type symbol, or :default. layout_kwargs is a NamedTuple of extra keywords forwarded to the chosen layout algorithm, e.g. layout_kwargs = (; seed = 1).\n\ntitle, title_fontsize, title_color, title_gap, outer_margin, fig_size, stretch_to_fig_size only apply to Makie.plot.\n\nFor a project-wide default, use a Makie theme, e.g. Makie.set_theme!(CausalGraphPlot = (node_color = :lightblue,)). edge_color and edge_label_color also inherit the active theme's linecolor/textcolor; node_color/node_label_color stay fixed and should be set together if changed.\n\nedge_paths can be used to override an edge's drawn route directly: a Dict (same keying as other per-edge overrides) from an edge to a vector of waypoints running from its source node's position to its destination node's position, in the same coordinates as layout.\n\nSee the Plotting page for the full keyword reference, styling precedence rules, and examples.\n\nExamples\n\nusing CairoMakie\n\ndag = DAG(\"A ---> X, A ---> Y, X ---> Y\")\n\nMakie.plot(dag; node_color = :lightblue, edge_color = :gray40)\nMakie.plot(dag; edge_color = Dict((:A, :X) => :red, :default => :black))\nMakie.plot(dag; edge_color = Dict(directed(:A, :X) => :red, :default => :black))\nMakie.plot(dag; node_shape = Dict(:A => :square, :default => :circle))\nMakie.plot(dag; node_shape = :rect, node_labels = Dict(:A => \"Age at\\nbaseline\"))\nMakie.plot(dag; curvature = Dict((:A, :Y) => 0.3))\nMakie.plot(dag; title = \"My DAG\", layout = :spring, layout_kwargs = (; seed = 1))\n\n# Compose two graphs into one figure:\nfig = Figure()\nMakie.plot!(Axis(fig[1, 1]; aspect = DataAspect()), dag)\nMakie.plot!(Axis(fig[1, 2]; aspect = DataAspect()), dag; node_color = :salmon)\nMakie.hidedecorations!.(fig.content)\nMakie.hidespines!.(fig.content)\n\n\n\n\n\n","title":"Makie.plot"},{"category":"function","location":"95-reference/70-plotting/#CausalStructures.layout","page":"Plotting","text":"layout(cg::CausalGraph, method::Symbol = :stress; kwargs...)\n\nCompute 2-D node positions for cg and return them as a Dict{Symbol,NTuple{2,Float64}} keyed by node name.\n\nmethod Algorithm Requires\n:spring Fruchterman-Reingold force-directed NetworkLayout\n:stress Stress majorization NetworkLayout\n:sfdp Scalable Force-Directed Placement NetworkLayout\n:spectral Spectral layout NetworkLayout\n:shell Concentric shells NetworkLayout\n:squaregrid Square grid NetworkLayout\n:sugiyama Layered/hierarchical (DAG only) Sugiyama\n\nThe default is :stress, except for a DAG with Sugiyama loaded, where it is :sugiyama. Extra kwargs are forwarded to the underlying algorithm.\n\nExamples\n\nusing NetworkLayout\n\ndag = DAG(\"A ---> X, A ---> Y, X ---> Y\")\n\nlayout(dag)\nlayout(dag, :spring)\nlayout(dag, :spring; seed = 1405, iterations = 200)\n\npositions = layout(dag, :spring)\npositions[:A] = (0.0, 2.0)\n\nusing CairoMakie\nplot(dag; layout = positions)\n\n\n\n\n\n","title":"CausalStructures.layout"},{"category":"type","location":"95-reference/70-plotting/#MakieExt.CausalGraphPlot","page":"Plotting","text":"CausalGraphPlot is the plot type associated with plotting function causalgraphplot. Check the docstring for causalgraphplot for further information.\n\n\n\n\n\n","title":"MakieExt.CausalGraphPlot"},{"category":"function","location":"95-reference/70-plotting/#MakieExt.causalgraphplot","page":"Plotting","text":"causalgraphplot(cg::CausalGraph; kwargs...) -> CausalGraphPlot\ncausalgraphplot!(ax, cg::CausalGraph; kwargs...) -> CausalGraphPlot\n\nRecipe-native aliases for Makie.plot/Makie.plot! on a CausalGraph; see that docstring for the full keyword reference.\n\nPlot type\n\nThe plot type alias for the causalgraphplot function is CausalGraphPlot.\n\nAttributes\n\narrow_fill =  nothing  — Arrowhead fill color; defaults to the edge's own color.\n\narrow_size =  nothing  — Arrowhead size; defaults to a fraction of the typical node radius.\n\ncircle_size =  nothing  — Bidirected/undirected-edge circle marker size; defaults to a fraction of the typical node radius.\n\ncurvature =  nothing  — Per-edge curvature amount for curved edge routing.\n\nedge_color =  @inherit linecolor :black  — Edge line color; inherits the active theme's linecolor by default.\n\nedge_label_color =  @inherit textcolor :black  — Edge label text color; inherits the active theme's textcolor by default.\n\nedge_label_distance =  nothing  — Perpendicular offset (in pixels) of an edge label from its edge; defaults to the label's own font size.\n\nedge_label_font =  :regular  — Edge label font.\n\nedge_label_fontsize =  12.0  — Edge label font size.\n\nedge_label_rotation =  nothing  — Edge label rotation angle; defaults to following the edge's local tangent.\n\nedge_label_shift =  0.5  — Fractional position of an edge label along its path (0 = source, 1 = destination).\n\nedge_labels =  nothing  — Per-edge label text.\n\nedge_linestyle =  nothing  — Edge line style.\n\nedge_paths =  nothing  — Explicit waypoints overriding an edge's drawn route.\n\nlayout =  Makie.automatic  — Node position layout: Makie.automatic, a layout method Symbol (see layout), or precomputed positions.\n\nlayout_kwargs =  (;)  — Extra keywords forwarded to the chosen layout algorithm.\n\nlinewidth =  1.5  — Edge line width.\n\nnode_color =  :white  — Node fill color.\n\nnode_label_color =  :black  — Node label text color.\n\nnode_label_font =  :regular  — Node label font.\n\nnode_label_fontsize =  14.0  — Node label font size.\n\nnode_labels =  nothing  — Per-node label text; defaults to the node's name.\n\nnode_linestyle =  nothing  — Node outline line style.\n\nnode_padding =  10.0  — Padding around a node's label used when sizing the node automatically.\n\nnode_radius =  nothing  — Fixed node radius, overriding automatic label-fit sizing.\n\nnode_shape =  :circle  — Per-node marker shape: :circle, :square, :ellipse, or :rect.\n\nnode_strokecolor =  :black  — Node outline color.\n\nnode_strokewidth =  2.0  — Node outline width.\n\n\n\n\n\n","title":"MakieExt.causalgraphplot"},{"category":"function","location":"95-reference/70-plotting/#MakieExt.causalgraphplot!","page":"Plotting","text":"causalgraphplot! is the mutating variant of plotting function causalgraphplot. Check the docstring for causalgraphplot for further information.\n\n\n\n\n\n","title":"MakieExt.causalgraphplot!"},{"category":"section","location":"#CausalStructures.jl","page":"CausalStructures.jl","text":"CausalStructures.jl is a Julia package for causal graphs and causal inference. It provides graph representations for the structures used in causal inference, along with algorithms for graphical queries, causal identification, and transformations between graph classes.","title":"CausalStructures.jl"},{"category":"section","location":"#Supported-Graph-Classes","page":"CausalStructures.jl","text":"Currently implemented classes form the following type hierarchy:\n\nCausalGraph\n    ├─ DAG            Directed Acyclic Graph\n    ├─ UG             Undirected Graph\n    ├─ AbstractPDAG   All Partially Directed Acyclic Graphs\n    │  ├─ PDAG        Partially Directed Acyclic Graph\n    │  ├─ CPDAG       Completed Partially Directed Acyclic Graph\n    │  └─ MPDAG       Maximally Oriented Partially Directed Acyclic Graph\n    ├─ ADMG           Acyclic Directed Mixed Graph\n    ├─ AbstractAG     All Ancestral Graphs\n    │  ├─ AG          Ancestral Graph\n    │  └─ MAG         Maximal Ancestral Graph\n    ├─ PAG            Partial Ancestral Graph\n    └─ UNKNOWN        No structural constraints\n\nUNKNOWN can be used for currently unsupported graph classes. The constraints each class imposes are validated on construction, and an error is thrown if the graph is invalid.\n\nThe following edge types exist:\n\ndirected(:A, :B) for A --> B\nundirected(:A, :B) for A --- B\nbidirected(:A, :B) for A <-> B\npartially_directed(:A, :B) for A o-> B\npartially_undirected(:A, :B) for A o-- B\npartial(:A, :B) for A o-o B\n\nThese same markers can alternatively be used as a string, passed directly to a graph type's constructor, see below.","title":"Supported Graph Classes"},{"category":"section","location":"#Quick-Start","page":"CausalStructures.jl","text":"Construct graphs by specifying edges and the desired graph class. The quickest way is to write edges directly as a string, using the markers above (+ fans a marker out to, or in from, several nodes at once):\n\nusing CausalStructures\n\ndag = DAG(\"U --> X + Y, X --> Y\")\nDAG with 3 nodes and 3 edges:\n  nodes: U, X, Y\n  edges:\n    U --> X, U --> Y, X --> Y\n\n\nEdges can equivalently be built up from constructor calls, which is useful when composing edges programmatically:\n\ndag = DAG(\n    directed(:U, :X),\n    directed(:U, :Y),\n    directed(:X, :Y)\n)\nDAG with 3 nodes and 3 edges:\n  nodes: U, X, Y\n  edges:\n    U --> X, U --> Y, X --> Y\n\n\nYou can then run a variety of causal graph queries, transformations, and causal identification methods such as adjustment-set computations. For example, if U is unobserved, we can project it out to obtain an ADMG:\n\nadmg = latent_project(dag, :U)\nADMG with 2 nodes and 2 edges:\n  nodes: X, Y\n  edges:\n    X --> Y, X <-> Y\n","title":"Quick Start"},{"category":"section","location":"25-pag-causal-effects/#pag-causal-effects-guide","page":"PAG Causal Effects","text":"This guide builds on Equivalence Classes and Causal Identification and covers causal adjustment when the graph is known only as a PAG, i.e. when the underlying MAG is identified only up to Markov equivalence.\n\nusing CausalStructures\nusing CairoMakie\nusing NetworkLayout","title":"PAG Causal Effects"},{"category":"section","location":"25-pag-causal-effects/#Adjustment-with-a-PAG","page":"PAG Causal Effects","text":"Consider the MAG from Equivalence Classes, where A and B share a hidden common cause, and C and B each cause their successor:\n\nmag = MAG(\"A <-> B, C --> B --> D\")\npag = mag_to_pag(mag)\nplot(pag)\nFigureAxisPlot()\n\nThe resulting PAG contains the circle-marked edge A o-> B. Depending on which MAG in the equivalence class is the true graph, this edge can correspond to either A --> B or A <-> B.\n\nConsequently, backdoor_set cannot determine an adjustment set from the PAG alone:\n\nbackdoor_set(pag, :A, :D) === nothing\ntrue\n\nIn particular, whether B needs to be included in an adjustment set depends on how the circle at A is resolved.\n\nNot every effect here is uncertain. For the effect of B on D, the PAG admits an adjustment set that is valid for every MAG in the equivalence class. The generalized adjustment criterion (GAC) can therefore be applied directly to the PAG with all_adjustment_sets:\n\nall_adjustment_sets(pag, :B, :D; minimal=false)\n4-element Vector{Vector{Symbol}}:\n []\n [:A]\n [:C]\n [:A, :C]","title":"Adjustment with a PAG"},{"category":"section","location":"25-pag-causal-effects/#Adjustment-sets","page":"PAG Causal Effects","text":"pagcauses considers MAGs compatible with the PAG and returns adjustment sets that are valid in at least one such MAG (Wang et al., 2025):\n\npagcauses(pag, :A, :D)\n1-element Vector{Vector{Symbol}}:\n []\n\nFor this example, the empty set is valid when A --> B, since there is then no backdoor path from A to D through B. When A <-> B, B must instead be included to block the path through the hidden common cause. Thus {B} is a valid adjustment set in that case; nonminimal supersets such as {B, C} may also be returned when they are valid.\n\nThe same procedure applies when the ambiguity occurs elsewhere in the graph. For example:\n\npagcauses(pag, :C, :D)\n1-element Vector{Vector{Symbol}}:\n []\n\nIf there is no MAG compatible with the PAG in which A can have a causal effect on D, pagcauses returns no possible causal effect:\n\npagcauses(pag, :D, :A)\nVector{Symbol}[]\n\nThis is consistent with possible_ancestors.\n\nnote: No selection bias\npagcauses, possible_local_structures, and maximal_local_mag assume that cg has no selection bias, matching the assumptions in the source papers. Selection bias shows up as an undirected edge in a PAG, and all three functions throw an ArgumentError if cg has one:julia> selection_pag = PAG(\n           undirected(:A, :B), undirected(:B, :C), undirected(:C, :D), undirected(:A, :D))\nPAG with 4 nodes and 4 edges:\n  nodes: A, B, C, D\n  edges:\n    A --- B, B --- C, C --- D, A --- D\njulia> pagcauses(selection_pag, :A, :B)\nERROR: ArgumentError: pagcauses (Wang, Tao, Qin & Zhou 2025) assumes no selection variables (no undirected edges)\n\nnote: Why not just enumerate the MAGs?\nOne could enumerate all MAGs with enumerate_mags and apply backdoor_set to each one. However, the number of MAGs in a Markov equivalence class grows as O(3^(d^2-d)2) with the number of nodes d.pagcauses avoids constructing these MAGs. Instead, it uses graphical conditions to check whether a candidate adjustment set can be valid for a MAG compatible with the PAG. This reduces the complexity to O(5^d d^6) (Wang et al. (2025), Section 3.4).Depending on your graph size, the brute force approach could of course still be faster in practice.","title":"Adjustment sets"},{"category":"section","location":"25-pag-causal-effects/#Local-structures","page":"PAG Causal Effects","text":"pagcauses builds on a smaller graphical primitive: possible_local_structures. For a single node x, this function enumerates which subsets of its circle-marked neighbors could resolve to x <-> v, with the remaining circle-marked edges resolving to x --> v, in some consistent MAG (Wang et al., 2023):\n\npossible_local_structures(pag, :A)\n2-element Vector{Vector{Symbol}}:\n []\n [:B]\n\nA has one circle-marked neighbor, B, so there are two local structures: the empty set, corresponding to A --> B, and {B}, corresponding to A <-> B. Pairing a local structure with maximal_local_mag resolves the edges around A implied by this local background knowledge:\n\nmaximal_local_mag(pag, :A, Symbol[])\nUNKNOWN with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A --> B, C o-> B, B --> D\n\n\nmaximal_local_mag(pag, :A, :B)\nUNKNOWN with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> B, C o-> B, B --> D\n\n\nThese correspond to the two cases considered by pagcauses(pag, :A, :D): the first gives {} as a valid adjustment set for A and D, while the second gives {B}. The result is returned as UNKNOWN rather than MAG, since local background knowledge about one node need not resolve every circle in the graph.","title":"Local structures"},{"category":"section","location":"25-pag-causal-effects/#General-identification","page":"PAG Causal Effects","text":"The methods above are each sufficient, not necessary: an effect can be identifiable even when no adjustment set exists. idp and cidp implement the IDP/CIDP algorithms of Jaber et al. (2022), the PAG analogues of id and idc (see General identification): they decide identifiability of P(y | do(x)) and P(y | do(x), z) from a PAG and, when identifiable, return the estimand.\n\nConsider a MAG where A confounds X, B --> X, and X causes Y, and view the Markov equivalence class:\n\nmag3 = MAG(\"B --> X, A <-> X, A --> Y, X --> Y\")\npag3 = mag_to_pag(mag3)\nplot(pag3)\nFigureAxisPlot()\n\nWe can then check whether the effect of X on Y is identifiable with idp:\n\nidp(pag3, :X, :Y)\nΣ_{A, B} (P(A, B, X, Y) / P(X | A, B))\n\nThe effect is identifiable (through the witnessed backdoor structure), and conditioning on the confounder A also identifies the conditional effect:\n\ncidp(pag3, :X, :Y; given = :A)\n(Σ_{B} (P(A, B, X, Y) / P(X | A, B))) / P(A)","title":"General identification"},{"category":"section","location":"05-quick-guide/#Getting-Started","page":"Getting Started","text":"We will show the basic usage of this package via directed acyclic graphs (DAGs).","title":"Getting Started"},{"category":"section","location":"05-quick-guide/#Constructing-a-graph","page":"Getting Started","text":"Let's build a DAG with a confounder A that affects both X and Y, and a mediator M between X and Y:\n\nusing CausalStructures\nusing CairoMakie\nusing Sugiyama\n\ndag = DAG(\"A --> X + Y, X --> M --> Y\")\nDAG with 4 nodes and 4 edges:\n  nodes: A, M, X, Y\n  edges:\n    A --> X, A --> Y, X --> M, M --> Y\n\n\nnote: Validation on construction\nAll graphs you construct are validated on construction to be a valid graph according to your graph class. For a DAG this means all edges are directed edges (-->), and there are no cycles. Let's try to create a DAG with a cycle:julia> invalid_dag = DAG(\"A --> B --> C --> A\")\nERROR: Invalid DAG:\n  - directed cycles are not allowed in DAG\n\nSince, a picture is worth a thousand words[1] let's plot our DAG:\n\n[1]: Especially with causal graphs\n\nplot(dag)\nFigureAxisPlot()\n\nFor more plotting details and customization options, see the Plotting guide.","title":"Constructing a graph"},{"category":"section","location":"05-quick-guide/#Testing-conditional-independence","page":"Getting Started","text":"A central question in causal inference is whether two variables are conditionally independent. For DAGs, this is determined using d-separation.\n\nAre X and Y independent?\n\nd_separated(dag, :X, :Y)\nfalse\n\nNo, there is a directed path X --> M --> Y.\n\nIf we condition on the mediator M, does that make them independent?\n\nd_separated(dag, :X, :Y, :M)\nfalse\n\nStill no, the backdoor path via A: X <-- A --> Y remains open.\n\nWhat if we condition on both A and M?\n\nd_separated(dag, :X, :Y, [:A, :M])\ntrue\n\nYes, now all paths are blocked.","title":"Testing conditional independence"},{"category":"section","location":"05-quick-guide/#Finding-adjustment-sets","page":"Getting Started","text":"Now suppose we want to estimate the causal effect of X on Y. The backdoor path X <-- A --> Y introduces confounding bias, so we need to block it by conditioning on a valid adjustment set.\n\nLet's find one automatically:\n\nadjustment_set(dag, :X, :Y)\n1-element Vector{Symbol}:\n :A\n\nWe can also verify that a specific set is valid:\n\nis_valid_adjustment(dag, :X, :Y, :A)\ntrue\n\nAnd enumerate all minimal valid adjustment sets:\n\nall_adjustment_sets(dag, :X, :Y)\n1-element Vector{Vector{Symbol}}:\n [:A]\n\nIn this case, {A} is the only minimal set that blocks the confounding.","title":"Finding adjustment sets"},{"category":"section","location":"05-quick-guide/#Next-steps","page":"Getting Started","text":"This small quick guide barely scratched the surface of what you can do with this package:\n\nFor more on working with DAGs in the context of causal identification, see Causal Identification.\nTo learn more about the different graph classes, see Equivalence Classes or the Graph & Edge Types reference.\nIf you're already familiar with causal graphs, you might instead be interested in Plotting or Benchmarks.","title":"Next steps"},{"category":"section","location":"95-reference/#reference","page":"Reference overview","text":"The API reference is split by topic:\n\nGraph & Edge Types – graph classes, edge constructors, background knowledge\nQueries – traversal, separation, and predicate functions\nOperations – graph-to-graph transformations\nCausal Identification – adjustment, backdoor, frontdoor, IV, and id\nSimulation – random graph and data generation\nEditing – adding, removing, and reclassing graph elements\nPlotting – graph visualization","title":"Reference"},{"category":"section","location":"95-reference/#Contents","page":"Reference overview","text":"Pages = [\n    \"10-graph-types.md\",\n    \"20-queries.md\",\n    \"30-operations.md\",\n    \"40-adjustment.md\",\n    \"50-simulation.md\",\n    \"60-editing.md\",\n    \"70-plotting.md\",\n]","title":"Contents"},{"category":"section","location":"95-reference/#Index","page":"Reference overview","text":"<details><summary>Click to Expand</summary>\n\nPages = [\n    \"10-graph-types.md\",\n    \"20-queries.md\",\n    \"30-operations.md\",\n    \"40-adjustment.md\",\n    \"50-simulation.md\",\n    \"60-editing.md\",\n    \"70-plotting.md\",\n]\n\n</details>","title":"Index"},{"category":"section","location":"15-equivalence-classes/#equivalence-classes-guide","page":"Equivalence Classes","text":"Equivalence classes of graphs arise in several settings, for instance, during causal discovery algorithms. This guide covers the graph types that represent such classes and how to work with them.\n\nusing CausalStructures\nusing CairoMakie\nusing NetworkLayout","title":"Equivalence Classes"},{"category":"section","location":"15-equivalence-classes/#Equivalence-classes-of-DAGs","page":"Equivalence Classes","text":"A PDAG (partially directed acyclic graph) is a graph whose edges may be directed or undirected, with no directed cycles. Two DAGs are Markov equivalent if and only if they have the same skeleton and the same v-structures (unshielded colliders).\n\nEach Markov equivalence class of DAGs has a unique CPDAG (completed partially directed acyclic graph) representation. In a CPDAG, an edge is directed precisely when it has the same orientation in every DAG in the equivalence class; otherwise it remains undirected.\n\nIn CausalStructures, all PDAG types are encoded as subtypes of AbstractPDAG.\n\nGiven a DAG, dag_to_cpdag computes its CPDAG:\n\ndag = DAG(\"C --> X, A --> X + Y, Y --> Z\")\ncpdag = dag_to_cpdag(dag)\nplot(cpdag)\nFigureAxisPlot()\n\nThe v-structure C --> X <-- A appears in every DAG in the equivalence class and therefore remains directed. The edges A --- Y and Y --- Z are undirected because no v-structure or Meek rule forces their orientation. count_dags tells us how many DAGs are in the class, and enumerate_dags lists them all:\n\ncount_dags(cpdag)\n3\n\nenumerate_dags(cpdag)\n3-element Vector{DAG}:\n DAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    A --> X, C --> X, A --> Y, Y --> Z\n\n DAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    Y --> A, A --> X, C --> X, Y --> Z\n\n DAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    Y --> A, A --> X, C --> X, Z --> Y\n\n\nWhen you need one concrete DAG to work with, dag_from_pdag picks one:\n\ndag_from_pdag(cpdag)\nDAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    C --> X, A --> X, A --> Y, Y --> Z\n\n\nAdjustment sets can be computed on CPDAGs and the result is then valid for every DAG in the equivalence class:\n\nall_adjustment_sets(cpdag, :X, :Z)\n2-element Vector{Vector{Symbol}}:\n [:A]\n [:Y]\n\nAn MPDAG (maximally oriented partially directed acyclic graph) can, for instance, arise when background knowledge is incorporated during causal discovery, orienting some edges of a CPDAG.\n\nmeek_closure propagates all further orientations implied by Meek's rules on a PDAG to obtain an MPDAG:\n\npdag = PDAG(\"C --- X, A --> X, A --- Y, Y --> Z\")\ncpdag = meek_closure(pdag)\nMPDAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    A --- Y, X --> C, A --> X, Y --> Z\n\n\nHere, X --- C was oriented to X --> C using Meek's rules.\n\nAll the methods shown above work on any subtype of AbstractPDAG:\n\ncount_dags(pdag)\nall_adjustment_sets(cpdag, :X, :Z)\n2-element Vector{Vector{Symbol}}:\n [:A]\n [:Y]\n\nnote: Only CPDAGs are guaranteed to be extendable\nEvery CPDAG represents a non-empty Markov equivalence class, so it always admits at least one consistent DAG extension and dag_from_pdag is guaranteed to succeed. This is not true for a general PDAG or MPDAG: being acyclic (and, for an MPDAG, closed under Meek's rules) is not sufficient to ensure a consistent extension exists. For example, a chordless undirected cycle is a valid PDAG and MPDAG, yet it cannot be oriented into a DAG without introducing a new v-structure:julia> cg = PDAG(\"A --- B --- C --- D --- A\")\nPDAG with 4 nodes and 4 edges:\n  nodes: A, B, C, D\n  edges:\n    A --- B, B --- C, C --- D, A --- D\njulia> is_cpdag(cg)\nfalse\njulia> is_mpdag(cg)\ntrue\njulia> dag_from_pdag(cg)\nERROR: PDAG cannot be extended to a DAG (Dor-Tarsi failed)","title":"Equivalence classes of DAGs"},{"category":"section","location":"15-equivalence-classes/#Equivalence-classes-of-MAGs","page":"Equivalence Classes","text":"Maximal Ancestral Graphs (MAGs) naturally arise when some common causes are unobserved: every latent common cause between two observed nodes becomes a bidirected edge (<->) in the MAG. Two MAGs are Markov equivalent if they encode the same conditional independence structure over the observed variables.\n\nEach Markov equivalence class of MAGs has a unique PAG (Partial Ancestral Graph). Like a PDAG for DAGs, the PAG marks each endpoint with the symbol shared by every MAG in the class. A circle (o) at an endpoint means that mark is not invariant; some MAGs in the class have a tail there and others have an arrowhead.\n\nConsider a MAG where A and B share a hidden common cause, C directly causes B, and B directly causes D:\n\nmag = MAG(\"A <-> B, C --> B --> D\")\nMAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> B, C --> B, B --> D\n\n\nmag_to_pag computes the PAG representing the Markov equivalence class of this MAG, so all MAGs that encode the same conditional independences:\n\npag = mag_to_pag(mag)\nPAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A o-> B, C o-> B, B --> D\n\n\nThe arrowheads at B on the A-B and C-B edges are invariant: A, B, and C form an unshielded collider at B (since A and C are non-adjacent), so every MAG in the class has an edge pointing into B from both sides. The circle marks at A and C are not invariant; the A-B edge could be A --> B or A <-> B, and similarly for C. The B --> D edge is invariant via Zhang's orientation rule R1: since A and D are non-adjacent and A o-> B --> D, the tail at B is forced in all MAGs.\n\nenumerate_mags lets you explore the equivalence class:\n\nenumerate_mags(pag)\n4-element Vector{MAG}:\n MAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A --> B, C --> B, B --> D\n\n MAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> B, C --> B, B --> D\n\n MAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A --> B, B <-> C, B --> D\n\n MAG with 4 nodes and 3 edges:\n  nodes: A, B, C, D\n  edges:\n    A <-> B, B <-> C, B --> D\n\n\nYou can also answer various causal inference questions, see PAG Causal Effects for some examples.","title":"Equivalence classes of MAGs"},{"category":"section","location":"40-plotting/#plotting-guide","page":"Plotting","text":"Plotting requires loading a Makie backend before use. Node placement requires NetworkLayout.jl or Sugiyama.jl (or bring your own layout). Below we use CairoMakie:\n\nusing CausalStructures\nusing CairoMakie\nusing NetworkLayout\nusing Sugiyama\n\nnote: General-purpose by design\nplot deliberately imposes no domain conventions of its own. There are many conventions in the literature, such as boxing conditioned variables, dashing latent variables, representing <-> as a dashed arc, or colouring exposures and outcomes. Rather than supporting any particular convention by default, plot exposes the underlying capabilities (styling, Makie themes, layouts) and leaves the choice of convention to the caller or a downstream package.","title":"Plotting"},{"category":"section","location":"40-plotting/#Basic-usage","page":"Plotting","text":"Pass any CausalGraph to plot. Every edge mark is supported natively:\n\nunknown = UNKNOWN(\"A <-> B o-> C o-- A\")\nplot(unknown)\nFigureAxisPlot()\n\nLet us plot the DAG from Figure 6.5 of Peters et al. (2017):\n\ndag = DAG(\n    \"C --> X, A --> X + K, X --> F + D, K --> Y, D --> Y + G, Y --> H\"\n)\nplot(dag)\nFigureAxisPlot()\n\nplot returns a FigureAxisPlot, so fig, ax, plt = plot(dag) gives you back the Figure, its Axis, and the plot itself - and plt is reactive:\n\nfig, ax, plt = plot(dag)\nplt.node_color[] = :salmon\nfig\nFigure()\n\nStyling breaks down into four areas, covered below:\n\nLayout — where nodes are placed\nStyling nodes — node appearance\nStyling edges — edge appearance\nLabels and titles — text and annotations\n\nFor a project-wide default you can use a Makie theme:\n\nMakie.set_theme!(CausalGraphPlot = (node_color = :lightblue, linewidth = 2))\n\nedge_color/edge_label_color also pick up the active theme's linecolor/textcolor; node_color/node_label_color stay fixed and should be set together if you want a dark node/label pairing, e.g. Makie.set_theme!(CausalGraphPlot = (node_color = :gray10, node_label_color = :white)).","title":"Basic usage"},{"category":"section","location":"40-plotting/#plot-layouts","page":"Plotting","text":"The layout keyword controls node placement and defaults to :sugiyama: for a DAG if Sugiyama.jl is loaded, else :stress.\n\nplot(dag; layout = :spring)\nFigureAxisPlot()\n\nWe provide these short-hand names for convenience. All except :sugiyama come from NetworkLayout.jl, while :sugiyama comes from Sugiyama.jl:\n\nlayout Algorithm\n:spring Fruchterman-Reingold force-directed\n:stress Stress majorization\n:sfdp Scalable Force-Directed Placement\n:spectral Spectral layout\n:shell Concentric shells\n:squaregrid Square grid\n:sugiyama Sugiyama layout (DAGs only)\n\nlayout also accepts explicit positions instead of a Symbol: either a Dict of (x, y) pairs keyed by node name, or a Vector of them in the order returned by nodes(cg):\n\nplot(dag; layout = Dict(\n    :A => (0, 1), :C => (0, -1), :K => (1, 1), :X => (1, -1),\n    :D => (2, -1), :F => (2, -2), :Y => (3, 0), :G => (3, 1), :H => (4, 0),\n))\nFigureAxisPlot()\n\ntip: Tweaking a layout by hand\nYou can compute a starting layout using layout, and then manually adjust a few nodes:positions = layout(dag, :spring)\npositions[:A] = (0.0, 2.0)\nplot(dag; layout = positions)\nFigureAxisPlot()\n\ntip: Sugiyama positions without Sugiyama routing\nSugiyama.jl implements it's own routing via dummy nodes. If you prefer the automatic routing with Bezier curves, you can pass layout = layout(dag_layered, :sugiyama) in plot:dag_layered = DAG(\"A --> X, A --> B, X --> Y, B --> Y, A --> Y\")\nplot(dag_layered; layout = layout(dag_layered, :sugiyama))\nFigureAxisPlot()","title":"Layout"},{"category":"section","location":"40-plotting/#Styling-nodes","page":"Plotting","text":"Each node style argument accepts either a scalar (applied to all nodes) or a Dict{Symbol, <value>} keyed by node name, with :default as a fallback.\n\nKeyword Default Controls\nnode_color :white fill color\nnode_strokecolor :black border color\nnode_strokewidth 2.0 border line width\nnode_linestyle nothing (solid) border line style\nnode_shape :circle node outline shape\nnode_radius nothing (text-fit, per node) size of each node\nnode_padding 10.0 clearance kept around each node's label when node_radius is nothing\narrow_size 0.4 × node-count-based reference length of arrowhead triangles\ncircle_size 0.28 × node-count-based reference radius of open-circle endpoints\n\nCombine color, border, and shape to highlight a node:\n\nplot(dag;\n    node_color = Dict(:A => :salmon, :default => :lightblue),\n    node_strokecolor = Dict(:A => :crimson, :default => :navy),\n    node_shape = Dict(:K => :square, :default => :circle),\n)\nFigureAxisPlot()\n\nnode_shape is one of :circle (the default), :square, :ellipse, or :rect; the latter two mainly exist to fit an oblong label. node_linestyle styles the border, e.g. to mark a latent variable:\n\nplot(dag;\n    node_linestyle = Dict(:X => :dash),\n    node_strokecolor = Dict(:X => :gray50, :default => :black),\n)\nFigureAxisPlot()","title":"Styling nodes"},{"category":"section","location":"40-plotting/#Text-fit-node-sizing","page":"Plotting","text":"By default (node_radius = nothing), each node is sized to fit its own label:\n\nlonglabels = DAG(\"Exposure --> Mediator --> Y_outcome\")\nplot(longlabels; node_shape = Dict(:Exposure => :ellipse))\nFigureAxisPlot()\n\nAlternatively, you can pass node_radius explicitly to control the size yourself:\n\nplot(dag; node_radius = 0.06)\nFigureAxisPlot()","title":"Text-fit node sizing"},{"category":"section","location":"40-plotting/#Styling-edges","page":"Plotting","text":"Each edge style argument accepts either a scalar or a Dict keyed by (and follows this precedence):\n\na CausalEdge for one exact edge, e.g. bidirected(:X, :Y)\na (src, dst) tuple for the node pair, in either order\nan edge-type symbol (:directed, :undirected, :bidirected, :partially_directed, :partially_undirected, :partial)\n:default as a fallback\n\nKeyword Default Controls\nedge_color :black line / marker color\narrow_fill nothing arrowhead fill color\nlinewidth 1.5 line width\nedge_linestyle nothing (solid) line style\ncurvature nothing how far the edge bows\n\nLet's style some edges by type:\n\nadmg = ADMG(\"X --> Y, X <-> Z, Z --> Y\")\n\nplot(admg;\n    edge_color = Dict(:directed => :steelblue, :bidirected => :crimson),\n    linewidth  = Dict(:bidirected => 2.5, :default => 1.5),\n)\nFigureAxisPlot()\n\narrow_fill is the arrowhead's fill color; nothing (the default) matches the edge's own resolved edge_color, so arrowheads render solid. Pass a transparent color for a hollow, outline-only arrowhead:\n\nplot(dag; arrow_fill = :transparent)\nFigureAxisPlot()\n\nedge_linestyle styles the line itself, e.g. to dash <-> edges:\n\nplot(admg; edge_linestyle = Dict(:bidirected => :dash))\nFigureAxisPlot()","title":"Styling edges"},{"category":"section","location":"40-plotting/#Targeting-specific-edges","page":"Plotting","text":"A tuple key can be used to change something for a specific edge:\n\nplot(dag;\n    edge_color = Dict((:A, :X) => :red, :default => :black),\n)\nFigureAxisPlot()\n\nA tuple key uses an unordered node pair. However, an ADMG may carry both X --> Y and X <-> Y, and a tuple key would then apply to both of them. To distinguish them a CausalEdge can be used instead:\n\nshared = ADMG(\"X --> Y, X <-> Y\")\n\nplot(shared;\n    edge_color = Dict(bidirected(:X, :Y) => :crimson, :default => :steelblue),\n)\nFigureAxisPlot()\n\ntip: Symmetric edges\nSymmetric edges are stored in a canonical order, so bidirected(:Y, :X) is the same key as bidirected(:X, :Y).\n\nNotice the automatic routing of the edges above! See more about it below.","title":"Targeting specific edges"},{"category":"section","location":"40-plotting/#Curvature-and-automatic-routing","page":"Plotting","text":"curvature bows an edge into an arc instead of drawing it straight. Positive values bow to the left as seen travelling from src to dst, negative to the right.\n\nplot(admg; curvature = Dict(:bidirected => -0.3))\nFigureAxisPlot()\n\nAn edge whose straight src --> dst line would pass too close to a non-incident node is automatically bent around it as a Bezier curve, rather than being drawn straight through it. Edges with nothing in their way are always drawn straight.\n\ndetour = DAG(\"A --> X + Y, X --> Y\")\n\nplot(detour; layout = [(0, 0), (1, 0), (2, 0)])\nFigureAxisPlot()\n\ncurvature can also be used to disable this behavior:\n\nplot(detour;\n    layout = [(0, 0), (1, 0), (2, 0)],\n    curvature = Dict(directed(:A, :Y) => 0.0),\n)\nFigureAxisPlot()","title":"Curvature and automatic routing"},{"category":"section","location":"40-plotting/#Explicit-edge-paths","page":"Plotting","text":"Normally, edges are drawn as straight lines, except when automatic curvature is needed. You can override the edge path explicitly with edge_paths by providing intermediate points for an edge. For example, the edge K --> Y below is drawn through the point (0.5, -0.25):\n\npositions = layout(dag, :spring)\n\nplot(dag;\n    layout = positions,\n    edge_paths = Dict((:K, :Y) => [positions[:K], (0.5, -0.25), positions[:Y]]),\n)\nFigureAxisPlot()","title":"Explicit edge paths"},{"category":"section","location":"40-plotting/#Labels-and-titles","page":"Plotting","text":"","title":"Labels and titles"},{"category":"section","location":"40-plotting/#Labels","page":"Plotting","text":"Each label style argument accepts either a scalar or a Dict{Symbol, <value>} keyed by node name, with :default as a fallback (same resolution rules as node styling).\n\nKeyword Default Controls\nnode_labels nothing text drawn in each node\nnode_label_color :black node label text color\nnode_label_fontsize 14.0 node label font size\nnode_label_font :regular node label font\n\nBy default each node is labelled with its own name. node_labels can be used to overwrite this; node sizing accounts for multi-line labels, so the nodes grow to fit:\n\nplot(DAG(\"A0 --> L1 --> A1 --> Y, A0 --> Y + A1\");\n    node_labels = Dict(\n        :A0 => \"Treatment\\nat baseline\",\n        :L1 => \"Confounder\\nat time 1\",\n        :A1 => \"Treatment\\nat time 1\",\n    ),\n)\nFigureAxisPlot()\n\nnode_label_color and node_label_fontsize style the label text itself:\n\nplot(dag;\n    node_label_color = Dict(:A => :crimson, :default => :black),\n    node_label_fontsize = 18,\n)\nFigureAxisPlot()","title":"Labels"},{"category":"section","location":"40-plotting/#Edge-labels","page":"Plotting","text":"Each edge label style argument accepts either a scalar or a Dict for per-edge overrides, using the same keying rules as other edge styling (a CausalEdge, a (src, dst) tuple, an edge-type symbol, or :default).\n\nKeyword Default Controls\nedge_labels nothing text drawn along each edge\nedge_label_color :black edge label text color\nedge_label_fontsize 12.0 edge label font size\nedge_label_font :regular edge label font\nedge_label_shift 0.5 position along the edge, 0 (source) to 1 (destination)\nedge_label_distance nothing perpendicular gap (pixels) from the edge; nothing scales with edge_label_fontsize\nedge_label_rotation nothing text angle in radians; nothing follows the edge's own angle\n\nplot(dag; edge_labels = Dict(directed(:K, :Y) => \"hello\"))\nFigureAxisPlot()\n\nBy default the label follows the edge's own angle, while edge_label_shift/edge_label_distance move it along/off that path:\n\nplot(dag;\n    edge_labels = Dict(directed(:K, :Y) => \"hi\"),\n    edge_label_shift = 0.75,\n    edge_label_distance = 12,\n)\nFigureAxisPlot()\n\nFor a steep or curved edge, following the edge's angle can leave the label hard to read; edge_label_rotation overrides it with a fixed angle instead:\n\nplot(dag;\n    edge_labels = Dict(directed(:A, :X) => \"steep\"),\n    edge_label_rotation = 0.0,\n)\nFigureAxisPlot()","title":"Edge labels"},{"category":"section","location":"40-plotting/#Titles","page":"Plotting","text":"Pass title to add a plot title (nothing by default, i.e. no title). title_fontsize and title_color style it; left as nothing, they fall back to the current Makie theme's axis-title defaults. title_gap (default 4.0, points) controls the spacing between the title and the graph.\n\nplot(dag; title = \"My DAG\", title_fontsize = 20, title_color = :navy)\nFigureAxisPlot()","title":"Titles"},{"category":"section","location":"40-plotting/#Figure-size-and-margins","page":"Plotting","text":"Keyword Default Controls\nouter_margin 16 padding (pixels) around the whole figure\ntitle_gap 4.0 gap (points) between title and the graph\nfig_size (600, 450) figure size in pixels (width, height)\nstretch_to_fig_size false stretch the layout to fill an uneven fig_size\n\nplot(dag; fig_size = (800, 600))\nFigureAxisPlot()\n\ntip: Large graphs need a bigger `fig_size`\nThe default (600, 450) is sized for small examples. As the number of nodes grows, labels and edges get cramped and can overlap; increase fig_size (and node_radius/node_label_fontsize if needed) to keep larger causal graphs readable.\n\ntip: Uneven `fig_size` and `stretch_to_fig_size`\nNode positions keep the layout's own aspect ratio by default, so depending on the chosen fig_size you can get a lot of empty space in the plot. Pass stretch_to_fig_size = true to disable this.","title":"Figure size and margins"},{"category":"section","location":"40-plotting/#Composing-into-an-existing-figure","page":"Plotting","text":"So far we've only used plot, which builds its own Figure and Axis for you. If you already have an Axis (say, one panel of a bigger figure), plot!(ax, cg; kwargs...) draws into that instead, with the same keywords as plot above except outer_margin, title_gap, fig_size, and stretch_to_fig_size, since those size the figure plot builds for you.\n\nThis is how you put two graphs side by side, or mix one in with other plots:\n\nfig = Figure(size = (900, 400))\nplot!(Axis(fig[1, 1]; aspect = DataAspect()), dag)\nplot!(Axis(fig[1, 2]; aspect = DataAspect()), admg; node_color = :salmon)\nMakie.hidedecorations!.(fig.content)\nMakie.hidespines!.(fig.content)\nfig\nFigure()","title":"Composing into an existing figure"},{"category":"section","location":"40-plotting/#Combining-options","page":"Plotting","text":"Here we plot a PAG where we combine a bunch of the styling options from above:\n\npag = PAG(\n    \"C o-> X, D --> G + Y, X --> D + F, Y --> H, K o-> X, K --> Y\"\n)\n\nplot(\n    pag;\n    layout           = :spring,\n    node_color       = Dict(:X => :skyblue, :Y => :gold, :default => :whitesmoke),\n    node_strokecolor = Dict(:X => :royalblue, :Y => :darkorange, :default => :slategray),\n    node_shape       = Dict(:K => :square, :default => :circle),\n    node_linestyle   = Dict(:K => :dash, :default => nothing),\n    edge_color       = Dict(:partially_directed => :royalblue, :default => :darkslategray),\n    edge_linestyle   = Dict(:partially_directed => :dash),\n    curvature        = Dict((:K, :Y) => 0.3),\n    edge_labels      = Dict((:K, :X) => \"cool\"),\n    node_label_color = Dict(:X => :navy, :Y => :saddlebrown, :default => :black),\n    title            = \"A cool PAG\",\n    title_fontsize   = 18,\n    title_color      = :navy,\n    fig_size         = (700, 500),\n)\nFigureAxisPlot()","title":"Combining options"},{"category":"section","location":"80-preferences/#Preferences","page":"Preferences","text":"Several defaults can be changed project-wide using Preferences.jl.\n\nThese preferences are stored per-project. After setting a value, restart Julia for changes to take effect globally within the project.\n\nusing Preferences, CausalStructures\n\nset_preferences!(CausalStructures, \"key\" => value)\n\nTo restore a preference to its default value, delete it:\n\ndelete_preferences!(CausalStructures, \"key\")\n\n","title":"Preferences"},{"category":"section","location":"80-preferences/#Traversal","page":"Preferences","text":"","title":"Traversal"},{"category":"section","location":"80-preferences/#\"open\":-open-vs-closed-neighbourhood-definition","page":"Preferences","text":"Default Type Affects\ntrue Bool ancestors, descendants, possible_ancestors, possible_descendants, anteriors, posteriors\n\nWhen true (open definition), the queried node itself is excluded from the result. Set to false to use the closed definition (node included).\n\nset_preferences!(CausalStructures, \"open\" => false)\n\n","title":"\"open\": open vs closed neighbourhood definition"},{"category":"section","location":"80-preferences/#Plotting","page":"Preferences","text":"","title":"Plotting"},{"category":"section","location":"80-preferences/#\"plot_layout\":-default-layout-algorithm","page":"Preferences","text":"Default Type Affects\nnothing (falls back to :stress, or :sugiyama for a DAG once Sugiyama is loaded) Union{Symbol,Nothing} layout, plot(cg; ...)\n\nOne of :spring, :stress, :sfdp, :spectral, :shell, :squaregrid (require NetworkLayout) or :sugiyama (requires Sugiyama); see Layouts.\n\nset_preferences!(CausalStructures, \"plot_layout\" => \"spring\")\n\nFor plot's appearance (colors, linewidths, fonts, node shapes, figure size, ...), use a Makie theme:\n\nusing Makie\n\nMakie.set_theme!(CausalGraphPlot = (node_color = :lightblue, linewidth = 2))","title":"\"plot_layout\": default layout algorithm"},{"category":"section","location":"95-reference/30-operations/#operations-reference","page":"Operations","text":"","title":"Operations"},{"category":"section","location":"95-reference/30-operations/#Skeleton-and-subgraphs","page":"Operations","text":"","title":"Skeleton & subgraphs"},{"category":"section","location":"95-reference/30-operations/#PDAG-orientation","page":"Operations","text":"","title":"PDAG orientation"},{"category":"section","location":"95-reference/30-operations/#Conditioning-and-marginalization","page":"Operations","text":"","title":"Conditioning & marginalization"},{"category":"section","location":"95-reference/30-operations/#Enumeration","page":"Operations","text":"","title":"Enumeration"},{"category":"section","location":"95-reference/30-operations/#MAG-/-PAG-equivalence","page":"Operations","text":"","title":"MAG / PAG equivalence"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.skeleton","page":"Operations","text":"skeleton(cg::Union{DAG,AbstractPDAG}) -> UG\n\nReturn the skeleton of cg: the undirected graph obtained by replacing every directed or partially-directed edge with an undirected edge.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> skeleton(dag)\nUG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\n\n\n\n\n","title":"CausalStructures.skeleton"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.subgraph","page":"Operations","text":"subgraph(cg::CausalGraph, nodes) -> CausalGraph\n\nReturn the subgraph of cg induced by nodes: restricted to the given node set, keeping only edges whose both endpoints are in nodes.\n\nnodes may be a single Symbol or an AbstractVector{Symbol}.\n\nThe return type matches cg for most classes, but two classes are downgraded, because the induced subgraph need not satisfy the stronger class invariant:\n\nCPDAG subgraphs are returned as MPDAG: removing a node can orphan a directed edge that was only strongly protected by that node, but the result is still Meek-closed and therefore a valid MPDAG.\nPAG subgraphs are returned as UNKNOWN: the invariant marks of a Markov equivalence class are not preserved by node restriction, so the result need not be a valid PAG.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C, A --> C\");\n\njulia> sg = subgraph(dag, [:A, :B])\nDAG with 2 nodes and 1 edge:\n  nodes: A, B\n  edges:\n    A --> B\n\n\n\n\n\n","title":"CausalStructures.subgraph"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.moralize","page":"Operations","text":"moralize(cg::Union{DAG,AbstractPDAG}) -> UG\n\nReturn the moral graph of cg. Connects every pair of directed parents that share a common child (adding a \"marriage\" edge), then replaces every edge with an undirected edge.\n\nFor AbstractPDAG, only directed parents participate in marriage edges; undirected neighbors are included in the skeleton but do not form a clique.\n\nExamples\n\njulia> dag = DAG(\"A --> C <-- B\");\n\njulia> moralize(dag)   # A and B are now married (share child C)\nUG with 3 nodes and 3 edges:\n  nodes: A, B, C\n  edges:\n    A --- C, B --- C, A --- B\n\njulia> pdag = PDAG(\"A --> C <-- B, D --- C\");\n\njulia> moralize(pdag)   # A married to B (co-directed-parents of C); D not married\nUG with 4 nodes and 4 edges:\n  nodes: A, B, C, D\n  edges:\n    A --- C, B --- C, C --- D, A --- B\n\n\n\n\n\n","title":"CausalStructures.moralize"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.dag_from_pdag","page":"Operations","text":"dag_from_pdag(cg::AbstractPDAG) -> DAG\n\nExtend cg to a consistent DAG by orienting all undirected edges, using the Dor-Tarsi algorithm.\n\nThe algorithm repeatedly finds a potential-sink node x (no directed children, whose undirected neighbors form a clique, and whose undirected neighbors are each adjacent to every existing parent of x), orients all undirected edges toward x, and removes it. Raises an error if no valid DAG extension exists. The last condition on x's existing parents ensures the extension has exactly the same v-structures as cg.\n\nExamples\n\njulia> pdag = PDAG(\"A --- B --- C\");\n\njulia> dag = dag_from_pdag(pdag)\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\nReferences\n\nDor and Tarsi (1992)\nWienöbst et al. (27–30 Jul 2021)\n\n\n\n\n\n","title":"CausalStructures.dag_from_pdag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.dag_to_cpdag","page":"Operations","text":"dag_to_cpdag(cg::DAG) -> CPDAG\n\nReturn the CPDAG representing the Markov equivalence class (MEC) of cg.\n\nThe algorithm detects v-structures (unshielded colliders) to determine compelled edge orientations, builds an initial PDAG from the skeleton, then applies meek_closure to propagate all implied orientations.\n\nExamples\n\njulia> dag = DAG(\"A --> B\");\n\njulia> dag_to_cpdag(dag)\nCPDAG with 2 nodes and 1 edge:\n  nodes: A, B\n  edges:\n    A --- B\n\njulia> dag = DAG(\"C --> X, A --> X + Y, Y --> Z\");\n\njulia> dag_to_cpdag(dag)\nCPDAG with 5 nodes and 4 edges:\n  nodes: A, C, X, Y, Z\n  edges:\n    A --- Y, C --> X, A --> X, Y --- Z\n\n\n\n\n\n","title":"CausalStructures.dag_to_cpdag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.dag_to_mpdag","page":"Operations","text":"dag_to_mpdag(cg::DAG, bk = BackgroundKnowledge()) -> MPDAG\n\nReturn the MPDAG representing the DAGs that are Markov equivalent to cg and consistent with the background knowledge bk. This is the CPDAG of cg with the bk orientations applied and closed under Meek's rules R1-R4.\n\nbk may be a BackgroundKnowledge or a string accepted by its constructor. Raises an error if cg itself violates bk; this guarantees the restricted equivalence class is non-empty, since it contains cg.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> dag_to_cpdag(dag)\nCPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --- C\n\njulia> dag_to_mpdag(dag, \"A --> B\")\nMPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\nReferences\n\nMeek (1995)\n\n\n\n\n\n","title":"CausalStructures.dag_to_mpdag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.apply_background_knowledge","page":"Operations","text":"apply_background_knowledge(cg::AbstractPDAG, bk) -> MPDAG\n\nOrient the edges of cg according to the background knowledge bk, then close under Meek's rules R1-R4 (see meek_closure), returning the MPDAG that represents the DAGs consistent with both cg and bk.\n\nbk may be a BackgroundKnowledge or a string accepted by its constructor.\n\nPer constraint, against the current state of the edge in cg:\n\nConstraint A --- B A --> B B --> A not adjacent\nrequired A --> B orient A --> B no-op error error\nforbidden A --> B orient B --> A error no-op no-op\n\nErrors are raised because background knowledge cannot add or remove adjacencies, only orient existing edges. The orientations are applied one at a time, each followed by Meek's rules; an error is also raised if a later one contradicts an orientation this implies, i.e. if bk is inconsistent with cg (e.g. A --> B, C --> B on A --- B --- C).\n\nExamples\n\njulia> cpdag = CPDAG(\"A --- B --- C\");\n\njulia> apply_background_knowledge(cpdag, \"C --> B\")\nMPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    B --> A, C --> B\n\njulia> apply_background_knowledge(cpdag, \"C !--> B\")\nMPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --- B, B --> C\n\nReferences\n\nMeek (1995)\nPerković et al. (2017)\n\n\n\n\n\n","title":"CausalStructures.apply_background_knowledge"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.meek_closure","page":"Operations","text":"meek_closure(cg::AbstractPDAG; check_cycles::Bool = true, r4::Bool = true) -> MPDAG\n\nApply Meek's orientation rules (R1-R4) to cg until no further orientations are implied, returning the resulting MPDAG.\n\nThe four rules are:\n\nR1: a --> b --- c, a not adjacent to c –> orient b --> c\nR2: a --- b, directed path a --> w --> b exists –> orient a --> b\nR3: a --- b, two parents c, d of b with c not adjacent to d, and a --- c, a --- d –> orient a --> b\nR4: a --- b, a --- c, c not adjacent to b, a adjacent to d, and a directed path c --> d --> b exists –> orient a --> b\n\nKeyword arguments\n\ncheck_cycles: when true (default), every candidate orientation is verified not to create a directed cycle before being applied. This check is redundant if cg is a genuine Meek pattern – every directed edge already justified by an unshielded collider – since Meek's Theorem 3 then guarantees no cycle can arise. Potentially unsafe downstream, since it always returns an MPDAG without verifying acyclicity.\nr4: whether to apply R4 (default true). R4 is only needed when cg carries directed edges beyond what its own v-structures imply (background knowledge).\n\nExamples\n\njulia> pdag = PDAG(\"A --> B --- C\");\n\njulia> result = meek_closure(pdag)\nMPDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\nReferences\n\nMeek (1995)\n\n\n\n\n\n","title":"CausalStructures.meek_closure"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.latent_project","page":"Operations","text":"latent_project(cg::DAG, latents) -> ADMG\n\nProject out latent (unobserved) variables from cg to produce an ADMG over the observed variables only.\n\nlatents may be a single Symbol or an AbstractVector{Symbol}.\n\nEach latent node v is eliminated by node substitution: directed edges p --> c are added for every parent p and child c of v, and bidirected edges s <-> c are added for every sibling s (bidirected neighbor) and child c of v. All pairs of children of v also become bidirected-connected.\n\nExamples\n\njulia> dag = DAG(\"U --> X + Y, X --> Y\");\n\njulia> latent_project(dag, :U)\nADMG with 2 nodes and 2 edges:\n  nodes: X, Y\n  edges:\n    X --> Y, X <-> Y\n\n\n\n\n\n","title":"CausalStructures.latent_project"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.exogenize","page":"Operations","text":"exogenize(cg::DAG, nodes) -> DAG\n\nReturn a copy of cg with each node in nodes made exogenous: all incoming edges to that node are removed, and its parents are connected directly to its children to preserve reachability.\n\nnodes may be a single Symbol or an AbstractVector{Symbol}.\n\nExamples\n\njulia> dag = DAG(\"A --> B --> C\");\n\njulia> dag2 = exogenize(dag, :B)\nDAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> C, B --> C\n\n\n\n\n\n","title":"CausalStructures.exogenize"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.normalize_latent_structure","page":"Operations","text":"normalize_latent_structure(cg::DAG, latents) -> DAG\n\nNormalize the latent structure of cg while preserving the induced marginal model over the observed variables. Applies the following steps (Evans (2016), Lemmas 1-3):\n\nExogenize all latent nodes (remove their incoming edges, rerouting through their parents).\nRemove latent nodes with at most one active child (they induce no confounding).\nRemove latent nodes whose child set is a strict subset of another latent node's child set.\n\nlatents may be a single Symbol or an AbstractVector{Symbol}.\n\nExamples\n\njulia> dag = DAG(\"A --> U --> X + Y\");\n\njulia> result = normalize_latent_structure(dag, :U)\nDAG with 4 nodes and 4 edges:\n  nodes: A, U, X, Y\n  edges:\n    A --> X, A --> Y, U --> X, U --> Y\n\nReferences\n\nEvans (2016)\n\n\n\n\n\n","title":"CausalStructures.normalize_latent_structure"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.condition_marginalize","page":"Operations","text":"condition_marginalize(cg::Union{DAG,ADMG,AbstractAG};\n                      given = Symbol[], index = Symbol[]) -> AG\n\nReturn the AG over the remaining nodes after conditioning on given and marginalizing out index, following Definition 4.2.1 of Richardson and Spirtes (2002).\n\ngiven and index may each be a single Symbol or an AbstractVector{Symbol}.\n\nTwo remaining nodes are adjacent if and only if they cannot be m-separated by any subset of the other remaining nodes given given. The edge type is determined by the anterior relations: a --> b if a is anterior to b but not vice versa; a <-> b if neither is anterior to the other; a --- b if each is anterior to the other.\n\nAt least one of given or index must be non-empty, and they must be disjoint.\n\nExamples\n\njulia> dag = DAG(\"U --> X + Y\");\n\njulia> condition_marginalize(dag; index = :U)\nAG with 2 nodes and 1 edge:\n  nodes: X, Y\n  edges:\n    X <-> Y\n\njulia> admg = ADMG(\"U --> X + Y, X <-> Z, Y --> Z\");\n\njulia> condition_marginalize(admg; index = :U)\nAG with 3 nodes and 3 edges:\n  nodes: X, Y, Z\n  edges:\n    X <-> Y, X <-> Z, Y --> Z\n\njulia> mag = MAG(\"A <-> X, X --> C, Y --> C, A <-> Y\");\n\njulia> condition_marginalize(mag; index = :A)\nAG with 3 nodes and 2 edges:\n  nodes: C, X, Y\n  edges:\n    X --> C, Y --> C\n\njulia> condition_marginalize(mag; given = :A)\nAG with 3 nodes and 3 edges:\n  nodes: C, X, Y\n  edges:\n    X --> C, Y --> C, X <-> Y\n\nReferences\n\nRichardson and Spirtes (2002)\n\n\n\n\n\n","title":"CausalStructures.condition_marginalize"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.enumerate_dags","page":"Operations","text":"enumerate_dags(cg::AbstractPDAG) -> Vector{DAG}\n\nEnumerate every DAG in the Markov equivalence class (MEC) of cg.\n\nUses Chickering's (2002) recursive listing algorithm: applies Meek closure first to normalize cg, then branches on each undirected edge in lexicographic order, rejecting orientations that would introduce new v-structures or directed cycles, and propagating forced orientations via Meek's rules at each step.\n\nCan call count_dags for sizing the problem before calling this function, as the number of DAGs in a MEC can be very large.\n\nExamples\n\njulia> pdag = PDAG(\"A --- B --- C\");\n\njulia> dags = enumerate_dags(pdag)\n3-element Vector{DAG}:\n DAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\n DAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    B --> A, B --> C\n\n DAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    B --> A, C --> B\n\nReferences\n\nChickering (2002)\n\n\n\n\n\n","title":"CausalStructures.enumerate_dags"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.count_dags","page":"Operations","text":"count_dags(cg::AbstractPDAG) -> Int\n\nCount the number of DAGs in the Markov equivalence class (MEC) of cg, without materializing them. Useful for sizing the problem before calling enumerate_dags.\n\nUnlike calling length(enumerate_dags(cg)), this shares the same Chickering recursion but skips building a DAG (edge list, backend, validation) at every leaf of the search, only incrementing a counter instead.\n\nExamples\n\njulia> pdag = PDAG(\"A --- B --- C\");\n\njulia> count_dags(pdag)\n3\n\n\n\n\n\n","title":"CausalStructures.count_dags"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.enumerate_mags","page":"Operations","text":"enumerate_mags(cg::PAG; selection_bias::Bool = true) -> Vector{MAG}\n\nEnumerate every MAG in the Markov equivalence class represented by the PAG cg (as produced by mag_to_pag).\n\nBy default the result includes MAGs with selection bias, i.e. with undirected (---) edges. Pass selection_bias = false to keep only the MAGs without undirected edges, as assumed by e.g. pagcauses and backdoor_set on a PAG; this also prunes the search. If cg itself has an undirected edge, no such MAG exists and the result is empty.\n\nEvery member of the class shares cg's invariant (non-circle) endpoint marks, so each circle endpoint is independently resolved to a tail or an arrowhead. For a single representative instead of the whole class, use mag_from_pag.\n\nAlgorithm\n\nThis is a brute-force search. With k circle endpoints in the PAG, it iterates all 2^k tail/arrow assignments; for each it builds the candidate graph, validates it as a MAG (which itself runs an m-separation search for maximality), and keeps it only when mag_to_pag maps it back to cg. The cost is therefore O(2^k) candidates times the per-candidate MAG validation, so it is exponential in the number of circle endpoints. The number of MAGs in a class can likewise be very large. Parallelizes over Threads.nthreads() once there are enough of them to be worth splitting across tasks.\n\nExamples\n\njulia> pag = PAG(\"A o-o B o-o C\");\n\njulia> length(enumerate_mags(pag))\n8\n\njulia> length(enumerate_mags(pag; selection_bias = false))\n5\n\nReferences\n\nZhang (2008)\n\n\n\n\n\n","title":"CausalStructures.enumerate_mags"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.ag_to_mag","page":"Operations","text":"ag_to_mag(cg::AG) -> MAG\n\nConvert an AG to a Markov equivalent MAG by adding edges between every pair of non-adjacent nodes that cannot be m-separated by any subset of the remaining nodes.\n\nFor each non-separable pair (u, v), the edge type is determined by the ancestor relationship in the current graph:\n\nu ancestor of v –> add u –> v\nv ancestor of u –> add v –> u\nNeither –> add u <-> v (bidirected)\n\nEdges are added one at a time and the graph is re-evaluated after each addition, since each new edge can change m-separation and ancestor relationships.\n\nExamples\n\njulia> ag = AG(\"A --> B --> C\");\n\njulia> ag_to_mag(ag)\nMAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, B --> C\n\nReferences\n\nRichardson and Spirtes (2002)\n\n\n\n\n\n","title":"CausalStructures.ag_to_mag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.mag_to_pag","page":"Operations","text":"mag_to_pag(cg::MAG) -> PAG\n\nReturn the Partial Ancestral Graph (PAG) representing the Markov equivalence class of the MAG cg.\n\nThe PAG has the same skeleton (adjacencies) as cg. Each endpoint carries an invariant mark: an arrowhead (>) or tail (-) when that mark is shared by every MAG in the equivalence class, and a circle (o) otherwise. Circle marks are represented with the partial (o-o), partially_directed (o->), and partially_undirected (o--) edge kinds.\n\nThe algorithm starts from the skeleton with all marks set to circles, orients unshielded colliders as they appear in cg, and then applies Zhang's complete orientation rules R1-R10 until no further mark is implied.\n\nExamples\n\njulia> mag = MAG(\"A --> B <-- C\");\n\njulia> mag_to_pag(mag)\nPAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A o-> B, C o-> B\n\nReferences\n\nZhang (2008)\nAli et al. (2009)\n\n\n\n\n\n","title":"CausalStructures.mag_to_pag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.mag_from_pag","page":"Operations","text":"mag_from_pag(cg::PAG) -> MAG\n\nReturn one MAG belonging to the Markov equivalence class represented by the PAG cg. This is a left inverse of mag_to_pag: mag_to_pag(mag_from_pag(pag)) recovers pag.\n\nA PAG leaves some endpoints as circle marks (o) that are not invariant across the class. This resolves every circle into a tail or arrowhead, following Zhang's construction (2008, Theorem 2):\n\nA circle opposite a fixed mark always makes the edge directed. An o-> edge becomes --> (the circle turns into a tail), and an o-- edge becomes a directed edge pointing out of the tail end (the circle turns into an arrowhead).\nAn undirected (---) edge carries selection bias and is kept unchanged.\nThe remaining o-o edges form a chordal component, oriented into a DAG with no new unshielded colliders by Dor-Tarsi simplicial elimination.\n\nExamples\n\njulia> pag = PAG(\"A o-> B <-o C\");\n\njulia> mag_from_pag(pag)\nMAG with 3 nodes and 2 edges:\n  nodes: A, B, C\n  edges:\n    A --> B, C --> B\n\njulia> mag = MAG(\"A --- B --- C --- D --- A\");\n\njulia> mag_from_pag(mag_to_pag(mag))\nMAG with 4 nodes and 4 edges:\n  nodes: A, B, C, D\n  edges:\n    A --- B, A --- D, B --- C, C --- D\n\nReferences\n\nZhang (2008)\nDor and Tarsi (1992)\n\n\n\n\n\n","title":"CausalStructures.mag_from_pag"},{"category":"function","location":"95-reference/30-operations/#CausalStructures.maximal_local_mag","page":"Operations","text":"maximal_local_mag(cg::PAG, x::Symbol, c) -> UNKNOWN\n\nReturn the maximal local MAG for the local structure c at x in cg.\n\nc may be a single Symbol or an AbstractVector{Symbol}.\n\nThis is cg with x <-> v for v in c and x --> v for x's other circle-marked neighbors (Algorithm 1 of Wang et al. (2023)), treated as local background knowledge about x and closed under the corresponding rule set.\n\nc should be a valid local structure, i.e. an entry of possible_local_structures(cg, x); passing an invalid one gives an unsound result. The returned graph may still contain circles (unlike a MAG, which is why it is returned as UNKNOWN rather than validated as a MAG or PAG): only what the local background knowledge about x implies is resolved, not necessarily everything.\n\nThrows ArgumentError if cg has selection bias (undirected edges), since both source papers assume none throughout.\n\nExamples\n\njulia> mag = MAG(\"A <-> X, B --> X, A <-> B, X --> Y\");\n\njulia> pag = mag_to_pag(mag);\n\njulia> maximal_local_mag(pag, :X, :A)\nUNKNOWN with 4 nodes and 4 edges:\n  nodes: A, B, X, Y\n  edges:\n    A --> B, A o-> X, X --> B, X --> Y\n\nReferences\n\nWang et al. (2023)\nWang et al. (2025)\n\n\n\n\n\n","title":"CausalStructures.maximal_local_mag"},{"category":"section","location":"70-benchmarks/#benchmarks","page":"Benchmarks","text":"Every graph in CausalStructures precomputes and stores each node's parents, children, spouses, and neighbors directly, so parents, children, spouses, and neighbors are effectively O(1) lookups rather than scans over the whole edge set. Many higher-level algorithms, including d_separated/m_separated, minimal_separator, and adjustment-set search, are built on these primitive operations, so their performance depends directly on the efficiency of these basic queries.\n\nusing CausalStructures\nusing BenchmarkTools\nusing Random","title":"Benchmarks"},{"category":"section","location":"70-benchmarks/#Queries-stay-fast-as-graphs-grow","page":"Benchmarks","text":"Single-hop queries such as children are just slice lookups, so their runtime is essentially unchanged whether the graph has 100 nodes or 10,000:\n\nfor n in (100, 1_000, 10_000)\n    dag = generate_graph(Random.Xoshiro(1), n; p = 5 / n, class = DAG)\n    x = Symbol(\"V\", n ÷ 2)\n    t = @benchmark children($dag, $x) samples = 200 evals = 1\n    println(rpad(\"n=$n\", 10), \"children: \", median(t))\nend\nn=100     children: TrialEstimate(88.000 ns)\nn=1000    children: TrialEstimate(111.000 ns)\nn=10000   children: TrialEstimate(87.000 ns)\n\nThese primitives keep more involved queries fast too, on graphs with hundreds of nodes and across graph classes:\n\ndag = generate_graph(Random.Xoshiro(1), 500; p = 0.1, class = DAG)\nmag = generate_graph(Random.Xoshiro(1), 500; p = 0.01, class = MAG, latents = 20)\nx, y = :V10, :V290\n\nrows = [\n    (\"d_separated (DAG)\", () -> d_separated(dag, x, y)),\n    (\"m_separated (MAG)\", () -> m_separated(mag, x, y)),\n    (\"topological_sort\", () -> topological_sort(dag)),\n    (\"minimal_separator\", () -> minimal_separator(dag, x, y)),\n    (\"markov_blanket\", () -> markov_blanket(dag, x)),\n]\nfor (label, f) in rows\n    t = @benchmark $f() samples = 200 evals = 1\n    println(rpad(label, 20), median(t))\nend\nd_separated (DAG)   TrialEstimate(54.801 μs)\nm_separated (MAG)   TrialEstimate(2.033 μs)\ntopological_sort    TrialEstimate(29.099 μs)\nminimal_separator   TrialEstimate(52.930 μs)\nmarkov_blanket      TrialEstimate(3.750 μs)","title":"Queries stay fast as graphs grow"},{"category":"section","location":"70-benchmarks/#Searching-for-adjustment-sets","page":"Benchmarks","text":"all_backdoor_sets, all_adjustment_sets, all_iv_sets, and all_frontdoor_sets search subsets of the candidate universe up to max_size, checking each one against the relevant validity criterion. Consequently, the runtime grows exponentially with max_size:\n\nadmg = generate_graph(Random.Xoshiro(1), 35; p = 0.25, class = ADMG, latents = 8)\nx, y = :V3, :V33\n\nfor ms in (2, 3, 4)\n    t = @benchmark all_adjustment_sets($admg, $x, $y; minimal = false, max_size = $ms) samples =\n        20 evals = 1\n    r = all_adjustment_sets(admg, x, y; minimal = false, max_size = ms)\n    println(\"max_size=$ms  \", median(t), \"  (\", length(r), \" sets)\")\nend\nmax_size=2  TrialEstimate(2.284 ms)  (0 sets)\nmax_size=3  TrialEstimate(25.536 ms)  (0 sets)\nmax_size=4  TrialEstimate(231.177 ms)  (0 sets)","title":"Searching for adjustment sets"},{"category":"section","location":"70-benchmarks/#Expensive-algorithms","page":"Benchmarks","text":"Here we will show the performance of some of the most expensive algorithms.","title":"Expensive algorithms"},{"category":"section","location":"70-benchmarks/#Exact-uniform-DAG-sampling","page":"Benchmarks","text":"generate_graph uses an Erdős–Rényi model: it samples each edge independently, which is cheap regardless of n but is not uniform over the space of DAGs. uniform_dag instead draws exactly uniformly from all labelled DAGs on n nodes using the recursive enumeration algorithm of Kuipers and Moffa (2015).\n\nfor n in (10, 20, 40)\n    t = @benchmark uniform_dag(Random.Xoshiro(1), $n) samples = 20 evals = 1\n    println(\"n=$n  \", median(t))\nend\nn=10  TrialEstimate(114.163 μs)\nn=20  TrialEstimate(740.356 μs)\nn=40  TrialEstimate(6.279 ms)","title":"Exact uniform DAG sampling"},{"category":"section","location":"70-benchmarks/#Markov-equivalence-class-enumeration","page":"Benchmarks","text":"count_dags and enumerate_dags operate on a CPDAG, PDAG, or MPDAG by considering every DAG in its Markov equivalence class. The size of that class grows combinatorially with the number of undirected edges. For example, a clique on k nodes has k consistent orientations, so the class can grow very large very quickly:\n\nprettyresult(t) =\n    string(BenchmarkTools.prettytime(t.time), \" / \", BenchmarkTools.prettymemory(t.memory))\n\nfor k in (5, 7, 9)\n    names = [Symbol(\"V$i\") for i = 1:k]\n    clique_edges = [undirected(names[i], names[j]) for i = 1:k for j = (i+1):k]\n    pdag = PDAG(clique_edges...)\n    c = count_dags(pdag)\n    tc = @benchmark count_dags($pdag) samples = 5 evals = 1\n    te = @benchmark enumerate_dags($pdag) samples = 5 evals = 1\n    println(rpad(\"k=$k\", 6), \"DAGs=$c\")\n    println(rpad(\"  count_dags:\", 20), prettyresult(median(tc)))\n    println(rpad(\"  enumerate_dags:\", 20), prettyresult(median(te)))\nend\nk=5   DAGs=120\n  count_dags:       547.041 μs / 728.84 KiB\n  enumerate_dags:   618.960 μs / 831.38 KiB\nk=7   DAGs=5040\n  count_dags:       31.374 ms / 38.81 MiB\n  enumerate_dags:   35.109 ms / 45.58 MiB\nk=9   DAGs=362880\n  count_dags:       3.392 s / 3.32 GiB\n  enumerate_dags:   5.272 s / 4.04 GiB\n\nenumerate_dags must materialize every DAG rather than simply count them, so it is slower and uses more memory than count_dags, as the numbers above show.","title":"Markov equivalence class enumeration"},{"category":"section","location":"70-benchmarks/#MAG-equivalence-class-enumeration","page":"Benchmarks","text":"enumerate_mags is the PAG/MAG counterpart to enumerate_dags. While enumerate_dags is fairly efficient via Chickering's recursive pruning, the algorithm for enumerate_mags is simply just brute-forcing every tail/arrowhead assignment for each circle endpoint in the PAG (2^k candidates for k circle endpoints), and checks if it's a valid PAG. Thus, it's considerably more expensive than count_dags/enumerate_dags:\n\nfor k in (3, 4, 5)\n    names = [Symbol(\"V$i\") for i = 1:k]\n    circle_edges = [partial(names[i], names[j]) for i = 1:k for j = (i+1):k]\n    pag = PAG(circle_edges...)\n    m = length(enumerate_mags(pag))\n    t = @benchmark enumerate_mags($pag) samples = 5 evals = 1\n    println(\"k=$k  MAGs=$m  \", median(t))\nend\nk=3  MAGs=23  TrialEstimate(663.481 μs)\nk=4  MAGs=242  TrialEstimate(58.591 ms)\nk=5  MAGs=4457  TrialEstimate(17.466 s)\n\ntip: Parallel search\ncount_dags, enumerate_dags, enumerate_mags, pagcauses, and the whole all_*_sets[1] family all parallelize their search across Threads.nthreads() automatically once the problem is large enough to benefit.\n\n[1]: Except for all_iv_sets, since the candidate check for IV is a trivial lookup, and thus not worth it.","title":"MAG equivalence class enumeration"},{"category":"section","location":"70-benchmarks/#Comparison-to-CausalInference.jl","page":"Benchmarks","text":"CausalInference.jl is another Julia package implementing some of the same identification criteria (DAGs only): the generalized adjustment criterion, the backdoor criterion, and the frontdoor criterion.\n\nOn the tested graphs, CausalStructures enumerates all valid adjustment, backdoor, and frontdoor sets substantially faster than CausalInference.jl. See benchmark/README.md for the full setup and numbers.","title":"Comparison to CausalInference.jl"}]
}
